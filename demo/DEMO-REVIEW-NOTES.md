# 데모 검수 — 보류/후속 노트 (2026-08-21~22 디자인 정련 세션)

지금 바로 다 못 했거나, 정본까지 손봐야 하는 항목. (완료·결정은 `DESIGN-NOTES.md`, 상태 요약은 `HANDOFF.md`.)

> ⚠️ **정본 반영 대기 목록(2026-08-22 사용자 방침)**: 데모를 다 만들어 놓고, **정본(screen-behaviors·목업) 반영은 나중에 이 노트를 근거로 한꺼번에 고려**한다. 아래 A절 항목들이 그 대상 — 데모는 반영됐고 정본만 대기.

## A. 정본(behaviors/목업)까지 손봐야 하는 것 (데모는 완료, 정본 반영만 대기)

### A-1. 이력 날짜 표기 — 데모는 개선함, 정본 미반영
정본 목업(`docs/design/mockups/50-history-list-full.html`)은 **연도 그룹만**(`.yr .rule` 가로줄) 있고 날짜 레일에 월(작게)/일(크게)/요일을 함께 그린다. 사용자 피드백(월이 일보다 너무 작다 · 월도 가로줄로 구분 · 연/월 구분을 중요시함).
- **데모 반영 완료**(커밋 `74ed63c`, 사용자 "확실히 월구분 있는 게 나은듯" 승인): **연/월 2단 그룹 헤더 + 가로줄**을 넣고, 날짜 레일은 **일 + 요일만**(월은 헤더로 올려 중복 제거·월이 크게 보임).
- **정본 미반영 = 할 일**: `screen-behaviors.md` 이력 절 + 목업 `50-history-list-full`에 **월 그룹(연/월 가로줄)·레일에서 월 제거**를 반영해야 데모↔정본이 일치.
- "일 숫자에 '일' 단위 없음(3 → 3일)"은 **월 헤더가 생겨 맥락이 분명**하므로 그대로 둠(정본도 그대로).

### A-2. 이력 옛 기록 탐색 수단 — 데모 완료(연도 바로가기), 정본 반영 대기
지금 이력은 스크롤 + `더 보기`(20건씩)뿐 → **연도 바로가기** 추가(사용자 확정 2026-08-22, `History.tsx`). 이미 있는 **연/월 그룹**을 살려, 화면 위 `바로가기 2026년 2025년…` 칩을 누르면 그 해가 렌더될 만큼 펼친 뒤 해당 연도 헤더로 스크롤한다(연이 2개 이상일 때만 노출). 기각: 기간 조회(어르신 입력 부담)·달력 점프(과함).
- **정본 반영 대기**: 정본 이력 화면(`HIST-*`)에 이 수단이 없음 → 나중에 `screen-behaviors`에 `연도 바로가기` 규칙 추가 고려.

## B. 남은 데모 작업

### B-1. 아이콘 Solid 세트 전면 교체 ✅ 완료(2026-08-22)
정본 `DISP-ICON-03`(채움) → **Phosphor `weight="fill"`** 전면 적용, lucide 제거. 중앙 배럴 `components/icons.tsx`로 매핑(각 화면은 import 경로만 변경). 방향 캐럿만 `bold`. 상세·기각안(heroicons)은 `DESIGN-NOTES.md`.

### B-2. 카드 갤러리 위치 ✅ 완료(2026-08-22)
`CardGallery`(예약 카드 상태 모음 + 빈 상태)를 **PhoneFrame 밖 넓은 QA 시트**로 뺐다(`/cards`). 각 카드는 앱과 같은 폭(~340px)·같은 배경 위에 두고, 넓은 화면에선 그리드로 여러 상태를 한눈에 비교한다. **로그인 화면 QA 버튼**으로 진입(`/cards`). ⚠️ 2026-08-22: 알림 페이지 하단에도 있던 중복 링크는 제거(로그인으로 일원화).

### B-3. 데모 미구현 화면 (스펙엔 있음 · 진짜 갭 아님 · 데모 미구현)
- **회원가입 4단계**(`AUTH-SIGNUP-01~`): 슬라이스1이 로그인만 넣어 범위 밖. 랜딩 `[회원가입]`은 현재 자리표시.
- **전화번호 변경 안내**(묶음1 「번호 변경 안내」, `screen-behaviors:2840`): 이름·생년 직접수정=정본상 직원 웹 담당, 전화번호만 병원 본인확인 안내, 비번만 앱 직접. 데모 미구현.
- 필요 시 추가. 정본에 있으므로 진짜 갭 아님(데모 구현 갭).

## C. 검수 3차 (2026-08-22 후반) — 완료/미결/정본반영

### C-1. ✅ 로그인 화면 신설(진짜 갭이었음)
데모가 랜딩 [로그인]을 눌러도 **전화번호·비밀번호 입력 화면 없이 바로 홈**으로 갔음. 정본 `AUTH-LOGIN-01~09`대로 `/login` 신설(전화번호+비밀번호+눈토글·하이픈 자동·비번찾기/번호변경 링크, 평소 로그인은 OTP 없이 비밀번호). 랜딩=`AUTH-LAND-01`(버튼 2개)는 그대로. **정본 반영 불필요**(이미 정본에 있고 데모가 빠뜨렸던 것).

### C-2. ✅ 홈 카드를 정본 '풍부한' 카드로 승격 + 결정 복구
- 홈이 쓰던 간단 카드(삭제)가 정본보다 단순했음 → 정본 `HOME-CARD-01`·`DISP-CARD-01`·`CARD-OK/IN/WAIT-*`의 **가운데 132px 상태별 카드(StatusCard)**로 통일(홈·QA 동일). 상세=`DESIGN-NOTES.md`.
- **결정 복구**: 카드 교체로 사라졌던 것 되살림 — ⓐ 정본 색코딩 배지(예약확정=딥틸·신청=주황·대기=파랑·접수=보라, 공용 `StatusBadge`) ⓑ QR 눌러 전체화면(`NAV-HOME-02`). 사전문진 줄은 실제 상태(미작성/작성중(n/m)/완료 숨김) 반영.
- **정본 반영 불필요**(정본대로 맞춘 것).

### C-3. ✅ 취소 흐름 세 갈래 구현 (2026-08-22 완료)
~~현재 데모 `[예약 취소]`는 마감 후 상담 연결 화면(`ApptCancel`) 하나뿐~~ → **세 갈래 다 넣음**(`ApptDetail.tsx` 상세 위 팝업 + `mockData.getCancelTier`):
- `CANCEL-NEW-01`: **만든 지 30분 이내 = 마감 무관 바로 취소**(확인창). 데모는 `appt-3`(오늘 예약이지만 '방금 만든 건'으로 표시)로 시연.
- **마감 전 일반 취소**(`CANCEL-PRE-01~07`): 확인창(대상 재기재·`[아니요]`/`[취소합니다]` 빨강) → 바로 취소 → 같은 상세를 '취소됨'으로 다시 그림(`CANCEL-DONE-01~03`: 회색 머리·취소됨 배지·누가·`[새로 예약하기]`). 데모는 `appt-4`(+2일)·`appt-6`(+9일).
- `CANCEL-LATE-01~09`: 마감 후 = **안내 팝업**(시계·`취소 마감 시간이 지났습니다`·`진료 시작 24시간 전까지만`·전화 상자·`[닫기]`/`[상담 채팅 연결]`, 채팅 먼저) → 채팅. 데모는 `appt-2`(오늘).
- ⭐ **데모 정본 대조로 오히려 바로잡음**: 마감 후를 **별도 화면(`ApptCancel`)**으로 뒀던 것이 정본과 어긋남(`LATEFLOW-POP-OPEN-01`·`CANCEL-LATE-01`은 **상세 위 팝업**) → 팝업으로 흡수하고 `ApptCancel.tsx`·`/appt/:id/cancel` 라우트 제거. **정본 반영 불필요**(정본이 이미 옳았고 데모가 화면으로 잘못 냈던 것).
- 데모 등급 판정: `appt-3`=신규(30분), 오늘·내일=마감 후, 그 밖 미래=마감 전. 실제 앱은 `created_at+30분`·설정 마감시간으로 계산.
- ✅ **확정 전(예약신청) 용어 분기 추가**(사용자 지적): 데모가 상태와 무관하게 '예약 취소'로만 보였음 → `예약신청`이면 상세 버튼 **`신청 취소`**·확인창 제목 **`신청을 취소할까요?`**(`APPT-HEAD-04`·`CARD-COMMON-02`·`BOOK-DONE-03`, "용어가 상태를 따라간다"). **흐름은 동일**(확정 전이라고 별도 마감 규칙 없음; 오늘 신청 건은 30분 뒤 마감 후 상담 = `CANCEL-NEW-02` 확정 의도). **정본 반영 불필요**(정본에 이미 있음).
  - ✅ **'신청번호' 카드 용어 전수 확인 완료(2026-08-22)** — 데모가 이미 정본대로였음, **코드 변경 없음**: 홈 카드(`StatusCard`) 헤더는 `예약신청`/`미확정`이면 **'신청번호'** 분기(`StatusCard.tsx:339`)·버튼 **'신청 취소'**(`:243`) / 나의예약 목록은 번호 미표시+상태 **'확인 중'**(`MyAppointments.tsx:31`, `LIST-ST-02`) / 전체화면 QR은 `hasQR`인 확정 예약만 필터해 '예약번호'만 노출(`QrFullscreen.tsx:12`) / `QrPreview`의 '예약번호' 하드코딩(`StatusCard.tsx:93`)은 `예약확정`·`예약부도`에서만 렌더. 상세는 번호를 크게 안 띄우고 취소 버튼만 상태 분기(위).

### C-4. ✅ 확정 — 홈 여러 건도 전부 히어로 (사용자 확정 2026-08-22)
정본 `HOME-CARD-02`: 그날 예약이 **2건 이상이면 '사람별 줄로 묶은 카드'**(각 줄 시각레일+이름+관계+[QR]), 1건만 큰 히어로(`HOME-CARD-01`). → **사용자 확정: 건수와 무관하게 전부 히어로 카드.** 근거: 어르신 대상이라 예약마다 큰 카드로 QR·상태·문진·변경/취소를 한눈에 보고 바로 누르는 게 낫다(줄 목록은 상세로 한 단계 더 들어가야 함). 트레이드오프(3건+ 스크롤 길어짐)는 감수.
- 데모는 이미 전부 히어로(`Home.tsx` `todayAppointments.map(StatusCard)`) → **코드 변경 없음.**
- **정본 반영 대기**(D절): `HOME-CARD-02`를 「2건+ 줄 목록」 → 「전부 히어로」로 뒤집기 + 뒤집힌 쪽 역참조. **사용자 방침대로 데모 완성 후 한꺼번에 반영**(지금은 노트 기록만).

### C-5. ✅ 예약 달력 범위 8주 + 월 이동 (사용자 지적 "한 주밖에 없어", 2026-08-22)
데모 예약 4단계·예약 변경 달력이 ① **이번 달만**(월 이동 화살표 없음) ② 예약 가능일을 **8영업일(≈1주)**만 생성 → 사장님이 "한 주밖에 없다"고 지적. 정본은:
- `BOOK-DATE-01`: 월 달력 `‹ 2026년 8월 ›`(월 이동 화살표) · `BOOK-DATE-06`: **예약 8주(56일)까지**(넘는 달은 비활성+`예약은 8주 뒤까지 가능합니다`) · `BOOK-DATE-07`: 이전 달은 이번 달에서 비활성.
- 수정: `getAvailableDates`를 **8주 이내 평일 전부**로(`BOOKING_WINDOW_DAYS=56`), 공용 **`MonthCalendar`** 신설(월 이동 ‹ ›·범위 밖 비활성+안내)해 두 화면(`Step4Date`·`ApptChange`)이 공유. **정본 반영 불필요**(정본에 이미 있음, 데모 미구현이던 것).

## C-6. 검수 4차 (2026-08-22) — 시각 정련 라운드 (사용자 폰 검수)
사용자가 폰에서 훑으며 준 다수 시각 피드백을 반영. **디자인 시스템 결정·값·기각은 `DESIGN-NOTES.md` 「2026-08-22 시각 정련 2차」가 원본.** 요약:
- **테두리 → 그림자**(카드·버튼·타일·예약탭 행·선택칩) + 그림자 토큰 `--elevation-card`(사방·더 진하게). **입력칸은 테두리 유지.**
- **"쓰는·고르는 것 = 흰색"** 규칙(입력칸 `Input` 기본 흰색·이력 칩·문진 입력·예약 타일). **잠긴 칸=배경색.**
- **홈 카드**: 글자 위계(이름 굵게·시간 진하게)·본문 17px·구분선 안쪽 정렬·안쪽 박스 그림자+진한 딥틸(`primary/10`)·의사 이름 중복+하단선 제거.
- **로고 브랜드 서체**(Do Hyeon) · **의사 일러스트 아바타**(선택·상세·확인) · **QR 화면 재정비**(`QrGraphic` 진짜 QR풍 + 흰 카드 레이아웃).
- **10분 간격 진료 데이터**(이비인후과 정우재) — 정본 3열 격자가 촘촘함 담음 확인(구조 무변경).
- 문진 수정 진입 '이전' 숨김 · 예약탭 문진줄 hover · 기존환자 안내문구 쉬운 말로(경고색→회색, 열거 방지 동작 유지).
- **정본 반영 불필요**(데모 자체 디자인 시스템 · 정본 규칙 위반 아님). 실 프론트엔드가 `DESIGN-NOTES`로 의도를 읽는다.

## C-7. ✅ 환자앱 로그인 로고 통일 (사용자 지적 2026-08-22)
로그인 히어로 로고가 **"가" 글자 타일**이라 직원웹 사이드바·앱 홈(둘 다 `Hospital` 십자 심볼)과 달랐음 → **딥틸 타일 위 흰 `Hospital` 아이콘**으로 통일(`routes/patient/Login.tsx`). **정본 반영 불필요**(심볼 일관성 · 정본 위반 아님).

## E. 직원 웹 데모 (2026-08-22 착수) — 진행 · 정본반영 대기 · 남은 것

> `demo/src/routes/staff/`. `/staff/*` 라우트(폰 프레임 없이 `StaffShell` 데스크톱 레이아웃). 스펙 `2026-07-27-staff-web-design.md` + 규칙 `TODAY-*`·`SHELL-*`·`STAFF-LOGIN-*` 기준. 데모라 백엔드·마이그레이션·React Query·TDD 없이 화면만(가짜데이터·정상흐름).

### E-1. ✅ 진행: 척추 1차 (셸 + 로그인 + 오늘현황)
- **라우팅**: `App.tsx`에 `staffRoutes` spread, `main.tsx`에 `StaffProvider`. 파일: `routes/staff/{routes.tsx,StaffShell.tsx,staffState.tsx,mockData.ts,Placeholder.tsx,auth/Login.tsx,today/Today.tsx}`.
- **StaffShell**: 사이드바 4그룹(진료화면 단독 + 업무/기록/설정/상담봇, 역할 필터 `SHELL-NAV-02~04`)·활성=배경칩+좌측바(`SHELL-NAV-06`)·헤더(역할칩 메뉴·로그아웃 확인창 `SHELL-HDR-03`·구분선·`[새예약][당일방문]` `SHELL-ACT-01`, 의사는 시작버튼 없음 `SHELL-ACT-03`).
- **로그인**(`STAFF-LOGIN-*`): 이메일+비밀번호·눈토글·단일 실패문구("로그인 정보를 확인해 주세요" `STAFF-LOGIN-07`)·재설정 링크·데모 3계정(관리자/접수/의사, 이메일로 역할 매핑).
- **오늘현황**(`TODAY-*`): 「지금 처리할 것」 4카드(문제 우선, 요구사항 3.2)·환자 한 줄씩·시각레일(`TODAY-ROW-02` 미래 옅게)·사유 주의색 → 「오늘 요약」 6타일(전부 `/queue`행, `TODAY-SUM-03`)·의사별 대기(`TODAY-DOC-01` 진료과 생략 안 함). 번호 보기=행 인라인 펼침(`MASK-VIEW-01`).

### E-2. ✅ 직원 콘솔 시각 정체성 확정 (사용자 확정 2026-08-22)
딥틸 잉크 사이드바 + 각진 촘촘 패널 + 업무 밀도. 상세·근거=`DESIGN-NOTES.md` 「직원 웹」 절 + `HANDOFF.md`. **정본 반영 불필요**(데모 시각 시스템 · 실 빌드는 plan Task 0 토큰 별도).

### E-3. ⏳ 정본 반영 대기: 오늘현황 카드 제목 줄임 (사용자 지시 2026-08-22 — 문구 확정 후 반영)
데모에서 카드 제목을 **업무용 짧은 말**로 바꿈(정본은 풀어쓴 말). 데모(`mockData.ts` `problemCards[].title`)만 반영, **정본 대기**:
| 카드 | 정본(풀어쓴 말) | 데모(줄임) |
|---|---|---|
| 오래 대기 | 오래 기다리는 환자 | **장기 대기** |
| 미접수 | 시간이 지났는데 접수가 안 됐습니다 | **미접수 · 시각 경과** |
| 전일 미완료 | 어제 마무리되지 않은 환자 | **전일 미완료** |
| 확인 필요 | 확인이 필요한 예약 | **확인 필요 예약** |
- ⚠️ **미접수 건 = 무책망 원칙 유지**: "미접수"는 "안 왔다"가 아니라 "체크인이 아직 안 됨"이라 중립 — `TODAY-NOSHOW-03`("안 오셨습니다" 계열 금지)를 안 깬다.
- **정본 반영 대기**: 문구 확정 후 `screen-behaviors.md` `TODAY-*` 카드 제목(⚠️ 특히 `TODAY-NOSHOW-01`은 제목 문자열 `시간이 지났는데 접수가 안 됐습니다`를 **명시**함) + 결정로그에 반영(옛 문구 역참조). 사용자 방침: **지금은 정본 반영 말고 이 노트만**.

### E-4. 남은 것 / 정본과 다른 점 (검토 대상)
- ✅ **척추 4/4 완성**(2026-08-22): 로그인 → 오늘현황 → 대기목록 → 접수 → 환자상세 전부 클릭 가능. 커밋 `956cb3d`·`c86d913`·`ef20579`.
  - **대기 목록**(`c86d913`, `QUEUE-*`): 7상태 탭·순번·응급 확인팝업·드래그 순서변경+사유필수·번호보기·빈상태.
  - **접수**(`ef20579`, `CHKIN-*`): QR 주경로(데모=샘플 인식)·6자리 직접입력 보조·같은 화면 [도착 처리]·번호 모르는 환자 안내.
  - **환자 상세**(`ef20579`, `PTDET-*`): 헤더 전체노출·2열 섹션 그리드·현재예약·가족·이력·진료기록(읽기전용)·상담문의·내부메모·사전문진 담당의사만.
  - **다음 = slice2**: 나머지 관리자·상담봇 화면(현재 '곧' 자리표시) — 워커 병렬(규칙대로 개수·범위 보고→승인 후).
  - ⚠️ **데모 간소화(정본 대비, 필요 시 추가)**: 대기목록 의사별 필터(`QUEUE-FILT-*`)·워크인 패널(`QUEUE-WALK-*`, 헤더 [당일방문] 자리표시) 미구현 · 실시간/연결끊김(`*-LIVE-*`) 없음(데모라 정상) · 접수 실제 카메라 없음(샘플 버튼) · 환자상세 가족연결/전화변경 패널은 버튼만(흐름 미구현) · 환자상세는 :id 무관 고정 데모환자. (전체탭 토글 `QUEUE-TAB-09`는 E-5에서 구현 완료.)
- **사이드바 접기**: 토글 안 만듦 = **정본대로**(`SHELL-NAV-01` "그룹 접기 제공 안 함" · `SHELL-NAV-07` 좁으면 **자동 아이콘 모드**, 사용자 스위치 아님). ⏳ **데모 미구현**: `<1280px 자동 아이콘 모드`(`SHELL-NAV-07~10`) — 지금은 스크롤로 둠. 필요 시 구현(정본 반영 불필요, 데모 구현 갭).
- ⚠️ **헤더 왼쪽 = 현재 화면 제목**(데모 선택). `SHELL-HDR-01`은 "병원명"을 헤더 왼쪽에 두라 함 → **정본과 다른 점**. 데모 판단: 사이드바 브랜드가 병원명을 상시 표시하므로 헤더엔 화면 제목이 더 유용. **검토 필요**(정본 따를지, 이 판단을 정본에 반영할지).

### E-5. ✅ 대기목록 상호작용 세공 + 정렬 (2026-08-22 세션 · frontend-design 렌즈 · 사용자 폰 검수)
> `routes/staff/queue/Queue.tsx`. 타입체크·53테스트·빌드 통과. 커밋(이 세션).
- **① 드래그 순서변경 signature**(`QUEUE-ORDER-04·03`): 놓일 자리에 **딥틸 삽입선 + `▸ N번 자리` 칩**(순번=소리 내어 부르는 번호를 이 병원 언어로) · **끌리는 줄 점선 잔상**(정본 `QUEUE-ORDER-04`가 요구했으나 데모가 빠뜨렸던 것) · 맨 끝 드롭존 · 놓으면 사유 팝업(기존 `QUEUE-ORDER-05~08`). 삽입선 순번칩은 정본 명시 없음(ORDER-04 취지 확장) = **데모 개선, 정본 반영 불필요**(취지 내).
- **③ 상태 전이 시뮬레이션**(`QUEUE-BTN-01·02`): `[도착 처리]`→도착 탭, `[진료 대기로]`→진료대기(줄 맨 뒤 순번 자동), **탭 건수 −1/+1**, 퇴장 페이드(`prefers-reduced-motion` 존중). 응급/주의 실제 토글. (진료중 전이 버튼 없음=`DOCTOR-START-01` 정상.) **데모 시뮬레이션, 정본 반영 불필요**.
- **전체탭 묶기 토글 구현**(`QUEUE-TAB-09·10`): 시각순↔상태별 — **탭 줄 오른쪽 세그먼트**로(세로 한 줄 절약 · 탭 등폭 `QUEUE-TAB-11`은 pill 안에서 유지 · 전체 탭에서만 표시). 상태별=구획 머리+안 접음(`QUEUE-TAB-10`)·**완료·취소 맨 아래로**·진료대기 구획에 "순서는 「진료 대기」 탭에서"(`QUEUE-ORDER-10`, 드래그 불가). → E-4 「전체탭 토글 미구현」 해소.
- **탭별 정렬**(신규 · 정본 빈칸 → D절): 대기 계열(아직 안 옴=시각 이른 순 · 도착=오래 기다린 순 · 진료대기=순번) / 끝난 계열(완료·취소=**최신순**) / **전체 시각순=최신(늦은 예약)부터** — 오전 완료가 위에 쌓이지 않게(사용자 지적).

### E-6. ✅ 오늘의 현황 2열 개편 (2026-08-22 세션 · 넓은 모니터 여백 활용 · 사용자 폰 검수)
> `routes/staff/today/Today.tsx` + `mockData.ts`. 타입체크·53테스트·빌드 통과. 커밋(이 세션).
- **2열 레이아웃**: 주 컬럼(왼쪽 넓게)=「지금 처리할 것」 **전부**(정본 `TODAY-ORDER-02`대로 접기·더보기 없음) / **오른쪽 sticky 사이드 레일** = ① **지금 처리할 것 점프 버튼**(카드별 건수, 누르면 해당 카드로 스크롤 — 목록이 한 화면 넘쳐도 바로가기, `prefers-reduced-motion` 존중) ② 오늘 요약 6타일(전부 →/queue, `TODAY-SUM-03`) ③ 의사별 대기(`TODAY-DOC-01`). **좁은 화면=레일이 아래로 스택**(정본 세로 위계 유지).
- **페이지 상단 「지금 처리할 것」 제목 제거 → 레일 첫 카드 제목으로 흡수**(+건수 배지). **레일 카드 3종 제목 폰트 통일**(사용자 지시).
- **데모 데이터 확장**: 지금 처리할 것 8→**14건**(장기 대기 4·미접수 3·전일 2·확인 5) — 스크롤/점프 시연용. (요약 타일·의사별 대기 숫자는 별개 데모값이라 정합 안 맞춰도 무방.)
- ⚠️ **탭바 안(ⓒ) 기각**: 오늘현황을 대기목록처럼 상단 탭으로 나누는 안은 정본 `TODAY-ORDER-02`(*"전부 보여준다·접지 않는다·길어지는 것 자체가 경고"*)·`TODAY-RESCHED-11`(*"이 카드가 곧 목록"*)과 정면 충돌 → 안 함.
- ⚠️ **정본과 다른 점(D절)**: `TODAY-LAY-01`은 요약을 「지금 처리할 것」 **아래**(세로)로 못박음 → 데모는 넓은 화면에서 **오른쪽 사이드**로. **위계(문제 우선)는 유지**, 위치만 다름(정본 취지 안). 헤더 제목 건(E-4)과 같은 성격.

### E-7. ✅ 접수(QR·예약번호)를 헤더 패널로 + 대기목록 되돌리기 (2026-08-22 세션)
> `routes/staff/checkin/{CheckinForm,CheckinPanel,Checkin}.tsx` · `StaffShell.tsx` · `queue/Queue.tsx`. 타입체크·53테스트·빌드 통과. 커밋(이 세션).
- **왜(정본 대조)**: "도착 처리 = 접수(도착 접수)"임을 확인 — `CHKIN-RESULT-01`·`QUEUE-BTN-01`·`TODAY-BTN-02`·`SEARCH-ACT-02`가 모두 같은 [도착 처리]다. 사이드바 "접수"는 QR/번호 전용인데 "모든 접수"처럼 **과대 표기**였음(사용자 지적). 접수는 창구에서 '빈손 시작'하는 도착 접수 도구라 새 예약·당일 방문과 같은 **헤더 묶음**이 맞고, **패널로 열면 `SHELL-ACT-04`(화면 안 옮기고 패널만)에 부합** — 대기목록 보다가 그 자리에서 접수→닫으면 화면 그대로.
- **구현**: 헤더 `[QR 접수]` 버튼(접수직원·관리자, 의사 제외 `SHELL-ACT-03`) → 오른쪽 **360px 슬라이드 패널**(백드롭 읽기전용 `PANEL-BACK-01`). 접수 폼 = **`CheckinForm`으로 분리**(라우트·패널 공유). **사이드바 "접수" 항목 제거**(문 하나 `QUEUE-WALK-01`), `/checkin` 라우트는 딥링크용 유지.
- **되돌리기**(`UNDO-*`): 도착 줄 `[되돌리기]`(→아직 안 옴), 진료대기 줄 `[되돌리기]`(→도착, **순번 보관** `UNDO-ORDER-01`). 한 칸씩(`UNDO-SCOPE-01·04`), 접수직원 구간(`UNDO-ROLE-01`), 회색 테두리(`UNDO-BTN-02`).
- **"도착 후 환자 이탈" = 되돌리기로 처리(사용자 확정 2026-08-22)**: 정본에 별도 이탈 상태 없음. 되돌리기(도착→아직 안 옴) 후 안 돌아오면 부도 흐름 — **결과가 부도와 같아 별도 상태·문구 안 만듦.** 정본 변경 불필요.
- ⚠️ **정본과 다른 점(D절)**: 접수 진입을 **사이드바 전체화면 라우트 → 헤더 패널**로. `CHKIN-HEAD-01·03`은 전체화면 전제 → 헤더 패널 전제로 검토 필요. `SHELL-HDR-01`/`SHELL-ACT-01`의 헤더 버튼 2개도 **3개**로.

### E-8. ✅ 캘린더 규모·종일·지금 선·자동 스크롤·시간축 확대 (2026-08-22 세션 · 사용자 검수)
- **규모(요구사항 line 13)**: 의사 3→**8명(상한 `CAL-WEEK-08`)**·진료과 4곳(내과 3·정형외과 2·이비인후과 2·가정의학과 1, +소아청소년과 중지)·하루 최대 인원 합 ≈106(≈100 안팎). 캘린더·직원 관리·진료 일정·문진표·의사 콘솔이 **같은 의사 명단** 공유. mockData 확장(정본 반영 불필요).
- **하루 종일(09–18)**: 전엔 13:30까지만 보였음 → 종일 창으로. 오전·오후 예약 골고루.
- **지금 선**(`CAL-PAST-05`): 오늘을 보는 중이면 현재 시각(실제 시계)에 붉은 가로선 + 시각 라벨. **정본대로**(반영 불필요).
- **현재 시각으로 자동 스크롤**(사용자 요청 "오후엔 그 시각이 보이게 밑으로"): 열 때 지금 선이 화면 위 1/3에 오도록 스크롤. ⚠️ **정본에 없는 새 동작** → D절.
- **시간축 확대**(`CAL-ZOOM-01~08`): 시간축을 위아래로 **드래그**해 1시간 높이 조절(0.5~4px/분) + `[기본 배율]`·`[+]/[−]`. 넘치면 세로 스크롤. **정본대로**(반영 불필요). ⚠️ 데모는 배율 기억(`CAL-ZOOM-05`)·주간 미구현.
- **블록 한 줄**(사용자 지적 "두 줄이라 좁으면 잘림"): 환자명+상태를 **한 줄**로, 기본 배율 **2px/분**(가장 짧은 10분 슬롯도 한 줄이 안 잘림). "처음에 왜 글자가 잘리나" 방지. (`CAL-TIME-02` 길이 비례는 유지 — min-height 안 줌.)

### E-9. ✅ 헤더 양식 통일 — 화면 제목을 셸 헤더로 (2026-08-22 세션 · 사용자 지적)
- **문제**: 척추 페이지는 헤더에 화면 제목(`titleFor`), 1차 codex·2차 화면은 본문 `PageHead`에 제목 → **헤더 양식이 갈림**(사용자 지적).
- **해결**: `titleFor`를 **사이드바 라벨을 단일 출처**로 모든 라우트에 확장(헤더가 항상 그 화면 제목). `PageHead`는 **제목 제거**(부제+액션만, 둘 다 없으면 빈 띠 안 남김 → 그 공간을 본문 정보에 넘김, 사용자 제안). `title` prop은 호환용 미사용.
- ⚠️ **정본과 다른 점(D절)**: `SHELL-HDR-01`은 헤더 왼쪽=**병원명**(병원명은 지금 딥틸 사이드바 워드마크에 있음). 데모는 헤더=화면 제목. **E-4와 같은 건** — 함께 정본 검토.

### E-10. ✅ 상담 대화 좌우 분리 + 글자 위계 (2026-08-22 세션 · 사용자 지적)
- **티켓 전체 대화 좌우 분리**(사용자 지적 "환자랑 봇이 좌우로 나뉘어야 구분됨"): **환자=왼쪽 / 답하는 쪽(AI·직원)=오른쪽**. 색으로도 구분(환자 회색·AI 보라·직원 딥틸). 정본 `TICKET-DETAIL-CONV-01`("발신 주체가 구분되게")의 취지 강화 = **정본 반영 불필요**.
- **글자 위계 강화**(사용자 지적 "폰트가 비슷해 구분이 안 됨"): 인계 요약 등 **라벨↔값** 대비를 확실히(라벨 작고 옅게·값 굵고 진하게). 콘솔 전반의 시각 위계 개선(품질기준 #1). 정본 반영 불필요.

## F. 검수 5차 (2026-08-22 밤, frontend-design 세션) — 논의·결정·착수 대기

> 이 세션은 **먼저 논의로 방향을 잡고**, 확정된 것부터 착수하는 방식. 각 항목은 정본 근거를 grep으로 확인함(추측 금지).

> **✅ 구현 진행(2026-08-22 순차 반영, tsc·build·56테스트 통과):**
> - 완료: 티켓 unread 해제 · 중복환자(대표 검토·박서준/최유나 목데이터·전화번호 카드별·병합불가 환자상세열기) · 병원이꼭확인 체크박스 제거 · chatlog 기록그룹 이동 · Messages(자동발송 실목록·전환자 보조버튼·발송행 상세) · 상담봇(기간 선택기·참고예시 편집/재활성화·새 안내자료 배선·목데이터 확대·감사메모 저장 제거) · Stats 상세명단 36줄+총건수 · 문진표 버전 미리보기 문항+편집기로 복사.
> - **남음: F-4(세 문 예약/등록/접수, 큰 구조) · F-7(설명문 제거) · 전역 폴리시(X 크게·텍스트버튼·사이드패널 배경클릭/접기닫기·칩탭 일관성).**

### ⭐ F-1. 전체 상담 기록 → 사이드바 「기록」 그룹으로 이동 (관리자 전용) — **사용자 확정 2026-08-22, 특별 메모**
- **결정**: `전체 상담 기록`(`/chatlog`)을 **업무 그룹 → 기록 그룹**으로 옮긴다. 기록 그룹은 관리자 전용이므로 **접수직원에게는 더 이상 안 보인다**(사용자가 (가) 관리자 전용으로 확정).
- **왜**: 티켓함=**처리하는 곳**(답을 씀·배정·상담 종료), 전체 상담 기록=**읽는 곳**(AI가 끝낸 것까지 전수 열람·오답 신고만). 통계·접근기록·오류 옆(기록 그룹)에 두면 **위치 자체가 "돌아보는 곳" 신호**가 된다. 겹치는 유일한 지점은 "직원 연결" 갈래지만 티켓함=처리 / 기록=열람으로 역할이 다름 → **통합 안 함**.
- ⚠️ **정본 충돌 = 반영 대기**: `SHELL-NAV-01`(업무 그룹에 전체 상담 기록 명시)·`SHELL-NAV-02`(접수직원 업무 8개에 전체 상담 기록 포함)를 **바꿔야 한다**. 근거 `AD-069`·요구사항 3.1도 재확인 필요. 요구사항 L408("직원이 잘못된 답변을 발견하면…")의 "직원"이 접수직원 포함인지가 쟁점이었으나, 접수직원의 오답 신고는 **자기가 처리한 티켓 안**에서 충분하다고 정리.
- **"읽는 곳"으로 읽히게 하는 장치**(위치 외): ①탭(처리 단계) vs 필터(훑어보기)의 차이 유지 ②작업 도구(답변창·상담종료·배정) 없음 ③행 클릭=단순 열람(상태 안 바뀜) ④부제를 "봅니다/열람"으로 고정 + `읽기 전용` 표시.
- ✅ **② 접수직원 접근(확정 2026-08-22)**: 전역 로그로 겹치지 않는다. 접수직원이 상담 이력을 볼 필요가 생기면 **환자 상세 안**(그 환자의 상담 이력, `PTDET-SUPPORT`)으로 — 화면이 "이 환자의 모든 것" 한 일만 하므로 "한 화면 두 일" 원칙 위반 아님. 요구사항이 접수직원에게 전역 상담 로그 열람을 요구하지 않음.
- ✅ **③ 필터 + 검색 병행(확정 2026-08-22)**: 기록 화면에 필터(채널·갈래=브라우즈)와 **검색(환자·키워드=특정 건 점프)**을 함께. `SEARCH-BOX-01`과 같은 결의 한 칸 검색 추가. 결과 행 클릭은 **열람만**(상태 안 바뀜) — "읽는 곳" 감각 유지. (데모 chatlog엔 현재 필터만 있음 → 검색 추가 착수 대기.)

### ✅ F-2. 캘린더 `[전화 예약]` 버튼 제거 — **완료(2026-08-23)**
- 정본 캘린더 예약 진입 = **버튼이 아니라 빈칸 클릭**(`CAL-SLOT-06`·`CAL-BOOK-02`). `CAL-BOOK-*` 절 머리글이 "빈 시간 칩 등 폐기"까지 명시. 화면 라벨은 헤더 `[＋ 예약]` 문(F-4)뿐. "전화예약"은 정본의 **내부 분류명**이지 버튼 라벨이 아님.
- **처리**: `Calendar.tsx` PageHead의 `[전화 예약]` 버튼 삭제(진입=빈칸 클릭+헤더 예약 문) · 빈칸 클릭으로 열리는 패널 제목 **"전화 예약" → "새 예약"**. 빈칸 클릭 시 의사·날짜·시각이 채워진 채 열리는 것은 정본 `CAL-BOOK-02`대로(이미 그러함).

### F-3. 헤더 예약 = 패널 + 본화면 변신 (정본 결정, 데모 미구현) — 구현 규모 있음
- 정본: `SHELL-ACT-04`(헤더 버튼→화면 안 옮기고 오른쪽 패널만, 왼쪽은 보던 화면) · `PANEL-WORK-01/02`(패널=채우는 것/왼쪽=채우는 도구: 환자칸→검색표, 의사→그 의사 캘린더, 시간→일간) · `PANEL-FIND-01` · `PANEL-HOME-01`(끝나면 출발 화면 복귀). 데모는 캘린더 패널만 있고 본화면 변신·헤더 배선 없음.

### ⭐ F-4. 헤더를 **예약 / 등록 / 접수 세 문**으로 재구성 — **✅ 사용자 확정 2026-08-22 (SB-43 뒤집기)**
- **사용자 제안**: 헤더 세 버튼을 일반 병원 구조 `[＋ 예약]` · `[＋ 등록]` · `[＋ 접수]`로 나란히 → 새 직원도 직관적으로 이해. 지금 데모 헤더도 이미 세 버튼(QR접수·새예약·당일방문)이라 **자리 신설이 아니라 정리**.
  - **예약** = 미래 방문(전화예약) → 캘린더로.
  - **등록** = 환자를 병원에 등록(기존 찾기 + 새 환자). 검색이 여기 산다 → `PANEL-FIND-01`대로 본화면에 검색 결과표. 끝나면 막다른 길 없이 "이 환자로 예약/접수"로 이음.
  - **접수** = 지금 온 사람을 진료대기로. 예약 있으면 QR·예약번호 확인 / 없으면(당일방문) 찾아서 의사배정. → **기존 [QR 접수] + [당일 방문]의 도착 처리를 하나로 흡수.**
  - 기존 **당일 방문** = 등록 + 접수로 분해(신규 방문=등록→[바로 접수] / 기존 방문=접수에서 찾기).
- **정본·요구사항 근거(사용자 편)**: 요구사항 3.1이 **등록·접수를 다른 동사로** 씀(`:37` "직접 등록하고… 도착하면 접수 처리"). `SB-18`이 이미 접수 업무=셋(①현황확인 ②전화예약/워크인 등록 ③도착 접수)이고 **검색이 셋의 공통 출발점**이라 발견.
- **해소된 세부(2026-08-22 논의)**:
  - ⭐ **등록 화면 = 한 화면(검색 위 / 폼 아래)** — 사용자 확정 2026-08-22. 분기("처음이신가요?")·다음 단계 없음. **위=기존 기록 찾기**(입력하면 후보가 바로 아래 짧은 목록, 누르면 확정 → 폼 접힘), **아래=새 환자 등록 폼**(이름·성별·생년월일·전화·담당의사, 항상 보임). 정본 `QUEUE-WALK-02`("한 화면·마법사 금지") 철학과 일치. "처음"은 검색 없이 아래 폼만 채우면 됨.
  - ⭐ 중복 방지 = 관문 아님 — 새 환자 폼의 전화+생년월일이 강하게 겹칠 때만 폼 옆 **소프트 확인**("혹시 이분 아니세요? [기존 기록 보기]", 막지 않음). "처음인데 항상 의심" 문제 해결.
  - 후보 목록은 검색칸 바로 아래 짧은 목록으로 충분(중복 확인 목적) — `/patients` 큰 정렬표는 사이드바 "환자 검색"의 몫.
  - ⭐ 라벨(프레이밍) — "검색" 단어를 숨기는 게 아니라 **목적을 보이게**("오신 적 있어요/기존 기록 찾기"). 등록 문인데 맨 검색칸이 먼저 뜨는 당황을 막음.
  - "현황 확인"(요구사항 ①)은 **이 흐름이 아님** — 오늘의 현황·대기목록·캘린더(사이드바 보는 화면). 세 문은 ②③만 담음.
- **위험 + 해법**: 등록/접수 분리로 **신규 당일방문 = 두 문**이 됨(정본 `QUEUE-WALK-02` "한 단계가 그대로 시간"과 충돌). → 등록에서 **새 환자 저장 직후 [바로 접수]**가 그 환자를 안고 접수로 이음 = 문 둘·손 하나.
- ✅ **확정됨(2026-08-22)**: 이 구조가 `SHELL-HDR-01`·`SHELL-ACT-01`(SB-43 두 버튼)을 뒤집음 → **정본 반영 시** 옛 결정에 역참조(`~~취소선~~ + 해소`) 박기. 근거: 요구사항 동사 일치(`:37`)·병원 멘탈모델·신규 등록 빈틈 해결.

### ✅✅ 구현 로그 — 2026-08-22 밤 세션 (F-4 세 문 구현 + 직원웹 전반 검수 반영) — **전부 tsc 0·build ✓·미커밋**

> 방식: 사용자가 브라우저로 실시간 검수하며 준 피드백을 순차 반영. 세 문(F-4)은 **B(정본 완전형 PANEL-WORK)**로 확정·구현. 정본 대조 후 반영.

**세 문(F-4) — B 정본 완전형 구현 완료** (`demo/src/routes/staff/doors/*` 신설: `DoorContext.tsx`·`doorData.ts`·`surfaces.tsx`·`panels.tsx`; `StaffShell.tsx` 개조; `checkin/CheckinPanel.tsx` 삭제):
- 헤더 세 문 **등록 · 접수 · 예약** 순, **가운데(접수)만 딥틸 강조색**, 양쪽 옅은색 (사용자 지정 순서·색).
- `SHELL-ACT-04`+`PANEL-WORK-01/02/03`: 문을 열면 화면 안 옮기고 **오른쪽 패널**만, 패널 칸을 누르면 **왼쪽이 그 칸의 도구로 변신**(환자칸→검색표 / 의사 고른 뒤·시각칸→그 의사 일간 캘린더 / 날짜칸→월달력 / **환자 고른 뒤→그 환자 정보 카드**[접수 시 환자와 대화용, 사용자 요청]).
- 패널 프리미티브: **접기(«)·닫기(✕) 글자 병기 버튼**(`PANEL-LIVE-05`), 접힘=오른쪽 얇은 띠, ✕=안 묻고 날림(`PANEL-LIVE-06`). 패널 바탕 **흰색(bg-card)** — 회색 딥틸 바탕서 호버/탭 안 보이던 것 교정.
- **예약**: 환자→의사(패널 인라인 목록+대기인원 `QUEUE-WALK-08b`)→날짜·시각(일간 캘린더 빈칸 클릭)→사유→저장 직전 가운데 확인(`QUEUE-SAME-01`).
- **등록**: 신원 폼만(이름·성별·**생년월일=숫자 8자리 자동서식**·전화). "오신 적 있으세요?" 검색칸·담당의사 **제거**(검색은 사이드바 환자검색=직원 재량). 전화·생년 겹치면 **소프트 확인**(안 막음). 등록/확인 뒤 **[예약 잡기]·[바로 접수]로 이음**(F-4 "문 둘·손 하나").
- **접수**: 예약 확인(QR·번호=기존 CheckinForm) / 예약 없이 오신 분(당일방문: 환자→의사→접수). "지금/조금전 도착" 토글 제거(접수 시각=지금).
- ⭐ **왼쪽 가림 해결**: 도구가 떠 있어도 **사이드바로 다른 화면 가면 그 화면 보임**(패널 살아남아 따라옴 `PANEL-LIVE-01`, 칸 다시 누르면 도구 복귀), 뒤 화면 pointer-lock 제거(`PANEL-BACK-02` 취지).
- ⚠️ **정본 반영 대기(SB-43 뒤집기)**: 헤더 두 버튼(`SHELL-HDR-01`·`SHELL-ACT-01`)→세 문. F-4에 확정 기록됨, 정본에 역참조 박아야.

**검수 목록 반영 (사용자 브라우저 검수)**:
- **F-7 부제 일괄 제거** — 직원웹 PageHead `sub` 전 18곳: 순수 설명 삭제, 기능성은 처리(캘린더 "빈 시간" 삭제[없어도 앎]·진료일정 "실제 예약은 캘린더" 삭제+**메뉴명 「진료 일정 관리」로**·안내자료 "승인해야 반영" 삭제).
- **기간 선택기(`PeriodSelect`) 재설계** — **날짜 칸 항상 노출 + 프리셋(7/30/90/전체) 고르면 날짜 자동 채움 + 직접 지정**(사용자 제안). 배치: **안내 보내기·전체 상담 기록·병합 이력·미해결 질문**(+기존 상담봇 현황·품질).
- **미해결 질문(`Unresolved`)** — 기간 선택 + 각 묶음 **[안내자료로 답 만들기(해결)]·[이 묶음 무시]→목록서 사라짐**, 처리 개수 표시, 다 처리 시 빈 상태.
- **병원 설정 알림(1·2)** — 「알림」→**「자동 알림」**, 「문자로도」→**「문자도 발송」**, 문구에 **[환자 이름]·[날짜]·[시각] 칩**(눌러 꽂기 `HSET-MSG-16`) + **저장 전 되묻기·값 채운 미리보기**(`HSET-MSG-12·13`), 기본 문구도 토큰 든 실제 문장으로.
- **문진표(3)** — 현재(최신) 버전도 **미리보기 열림**(현재=배지+안내, 복사 버튼 숨김) · 편집 중 **[되돌리기]**로 원래 자료 복구 · 과거 버전 복사 시 저장 활성화.
- **직원 관리(5·프로필)** — **사진 바꾸기** 로컬 미리보기(업로드 없음) · 프로필 **닫기 버튼 글자 병기**.
- **병합(8)** — **「계정 연결이 대표로 이동」 안내**(`MERGE-COMPARE-04`, 미연결 쪽 대표로 골라도 연결 유지) · **뒤로가기 보이는 버튼**(‹ 후보 목록으로).
- **안내자료(Knowledge)** — 상태명 **공개→「승인됨」·검토 중→「승인 대기」**(사용자: 공개=승인, 검토중 누가 검토? 혼란) · 필터·목록·승인 핸들러·mockData 전부 반영.

**정본 확인 답**: 진료과 관리 **진료일정 유지**(사용자 OK, 일정 구조 꼭대기). 캘린더 색 정본은 **막지 않고 「사용중」 표시+같은 진료과 겹침 경고**(`CAL-COLOR-07`). 5분 자유·겹침 예약 **정본 있음**(`CAL-TIME-03`·`CAL-GAP-05/08`).

### ⚠️ 정본 반영 대기 (이번 세션 새로 생긴 것 — screen-behaviors/결정로그에 반영 필요)
> 데모가 확정·선반영했으나 정본은 아직 옛 상태. 반영 시 **뒤집힌 옛 항목에 역참조**(`~~옛 서술~~ ✅ 해소(날짜) — 결과`) 박기.
1. **⭐ 세 문(SB-43 뒤집기)** — `SHELL-HDR-01`·`SHELL-ACT-01`(헤더 두 버튼: 새 예약·당일 방문)을 **세 문(등록·접수·예약)**이 뒤집음. `QUEUE-WALK`(당일 방문)=등록+접수로 분해. F-4에 확정 근거 기록됨 → 정본 규칙표·역참조 필요.
2. **환자 고른 뒤 왼쪽=환자 상세 카드** — `PANEL-WORK-02`엔 환자칸→검색표 / 의사·시각→캘린더만. "환자 고른 뒤 그 환자 정보를 왼쪽에"(접수 시 대화용)는 사용자 지시로 추가 → 정본 반영 검토.
3. **병원 설정 라벨** — 「알림」→**「자동 알림」**(`HSET-MSG-01`은 "알림"), 「문자로도」→**「문자도 발송」**(`HSET-MSG-02` 열 이름) → 정본 라벨 반영 검토.
4. **메뉴명 「진료 일정 관리」** — 사이드바 라벨 "진료 일정"→"진료 일정 관리"(부제 제거 대체). `SCHED-*` 사이드바 라벨 확인.
5. **안내자료 상태명** — 공개→**승인됨**, 검토 중→**승인 대기**. `KBADM-LIST-03`은 상태 enum "확인 필요"(계약 근거 없음)라 데모가 정한 이름 정본화 가능.
6. **안내자료 키워드 검색**(F-8, 미구현) — 정본 KBADM은 분류·상태 필터만. 추가 시 `SEARCH-BOX-01` 결로 정본 반영.
7. **기간 선택기 프리셋**(2026-08-23 갱신) — 데모 `PeriodSelect` = 최근 7/30/90일·**최근 1년**·전체 + 날짜 직접 수정(고치면 「직접 입력」 표시, 드롭다운엔 없음). 정본에 기간 옵션 규정 있으면 대조.
8. **⭐ 「봇 상담 기록」 이름**(2026-08-23, 사용자 제안) — 정본 `SHELL-NAV-01/02`·`CHATLOG-*`는 **"전체 상담 기록"** → 데모가 **"봇 상담 기록"**으로 변경(상담봇이 나눈 대화 조회 화면이라 더 구체적). 정본 라벨 반영 검토(대안: "상담봇 대화 기록").
   - (되돌림) 앞서 넣었던 진료일정 「요일별 총 정원」 합계 행은 **사용자 지시로 제거** — 정본 반영 불필요.

### ✅ 이 세션 남은 것 — **전부 완료 (2026-08-23, tsc 0·build ✓·vitest 57)**
- ✅ **4 진료일정 버튼 배선** — `Schedule.tsx`: 진료과 **추가·이름 수정** 다이얼로그(공용 `NameDialog`, `SCHED-DEPT-01·11`) · 특정 날짜 변경 **추가·수정** 다이얼로그(`ExceptionDialog` = 날짜·누가[병원 전체/의사]·종일휴진/시간변경·메모, **되돌리기=그 줄 삭제** `SCHED-EXC-14`). 사용중지/다시사용은 기존 배선. mockData에 `ScheduleException` 타입·`affected` 추가.
- ✅ **6 전체 현황·문진표 UI 다듬기** — 진료일정 **전체 현황**: 범례 + 진료과 **색점** + 요일 헤더 토/일 색 + **요일별 총 정원 합계 tfoot**(하루 병원 수용력) + sticky 의사 열. 문진표: 왼쪽 목록 **상태 점**(문진표 유무) + 문항 번호 **원형 배지**.
- ✅ **7 전체 상담 기록** — `chatlog/mockData` 4→**20건**(여러 날·채널·갈래·근거유무) · **카드 높이 버그** = 목록 카드에 `self-start`(상세 패널 높이에 안 딸려감) + 패널 `sticky top-4`. 테스트를 하드코딩 ID→필터 속성 검증으로 견고화(57 passed).
- ✅ **B 캘린더** — doors **예약 시간 도구를 비례 캘린더로**(`surfaces.tsx` `DoctorDayCalendar` 재작성: 분 비례 블록 + **5분 스냅** `CAL-TIME-03` + 호버 미리보기 `CAL-TIME-05` + **겹침 경고**[막지 않음] `CAL-GAP`; doorData에 `buildBlocks·snapMin·overlapAt`) · **실제 캘린더 3버그**: ①09:00 라벨이 sticky 헤더에 가려짐 → 첫 라벨 상단 정렬 ②날짜 이동 죽음 → `dayOffset` 배선(이전/오늘/다음, 여러 날·주, 요일별 부분집합·일요일 휴무, 오늘=금 앱과 일치) ③패널 닫기 ✕ → **`✕ 닫기` 글자 병기 버튼**.
- ⚠️ **미커밋**. 승인 시 `git add demo/` 후 커밋.

### F-5. 기타 착수 대기 (방향 확정)
- 티켓 "새 메시지" 배지: 열람 시 `unread` 해제(현재 `Tickets.tsx:40` `openTicket`이 안 지움).
- 자동 발송 "보기": 설명 문장만(`Messages.tsx:73`) → **실제 자동 발송 목록을 표로**(`SEND-LIST-08`은 41건을 접어둘 뿐 안 보여주라는 게 아님).
- "전 환자에게 보내기"(`Messages.tsx:369` btnLink): 너무 안 보임 → **보조 버튼/칩**(주 버튼 X — 되돌릴 수 없고 비용 큼, `SEND-ALL-04` 미리보기가 안전장치).
- 사이드 패널: 배경 클릭으로 닫히지 않게 + `«접기`·`✕ 닫기` **글자 버튼**으로(작은 X 아이콘 대체). 정본 `PANEL-LIVE-01/05/06`.
- 안내 보내기 행 클릭: 지금 무반응 → 행 클릭=상세. ✅ **확정 A(2026-08-22)**: 보낸 것=열람만 / 예약해 둔 것=열람+`발송 취소`. **직접 편집 없음 — 바꾸려면 취소 후 새로 보내기.**

### F-6. 운영 통계 드릴다운 "상세 명단"이 5줄뿐 — "상세인데?" (사용자 지적 2026-08-22)
- `Stats.tsx` 드릴다운 모달 제목 `{지표} 상세 명단` + 부제 `최근 5건`(`DRILL_ROWS`가 5줄). 진료 완료 **329건**인데 5줄 → "상세 명단"이라는 이름과 안 맞음.
- 정본 `STAT-DRILL-03`: 건수 제한 자체는 허용하되 *"일부만 반환하면 `최근 N건`으로 범위를 분명히"* → 규칙 위반은 아님(정직한 표시). **문제는 데모 목데이터가 5줄뿐**이라 "상세"가 빈약.
- **처리 방향**: 목데이터를 **현실적 분량**(스크롤되는 ~30~50줄+)으로 늘리고, 라벨을 "상세 명단"에 맞게(전체 건수 표시 + 스크롤, 또는 페이지). "최근 5건" 프레이밍은 진짜 서버 상한일 때만.

### F-7. 페이지 설명문(PageHead `sub`) 다 제거 (사용자 지시 2026-08-22)
- 제목 아래 회색 설명 한 줄("환자에게 보낸 안내와 예약해 둔 발송을 여기서 봅니다" 류)을 **전부 제거.** `PageHead`는 `title` 미사용(제목=셸 헤더)이라 `sub`만 지우면 됨. 직원웹 sub 19곳.
- ⚠️ **기능성 정보가 섞인 곳은 별도 처리 필요**(설명이 아니라 정보): 상담봇 `Quality`/`Unresolved`/`Overview`의 **"최근 7일"**(기간 범위) · `Questionnaires` **"저장하면 새 버전으로 남습니다"**(경고) · `Knowledge` **"승인해야 답변에 반영됩니다"** · `Schedule` **"실제 예약은 캘린더에서 봅니다"**(혼동 방지) · `Calendar` **"칠해지지 않은 곳이 빈 시간"**(범례). → 이건 sub에서 빼되 **자리를 옮겨(칩·인라인) 남길지, 함께 지울지** 확인 후 실행.

### F-8. 관리자/상담봇 화면 시각·문구·데이터·기능 (사용자 폰검수 2026-08-22, 이미지 10장)
**질문 즉답(확인 완료)**
- 계정 미연결 = 앱 계정 안 붙은 환자기록(`MERGE-LIST-03`). 자동병합 잠금은 **둘 다 연결**일 때(`MERGE-COMPARE-04`, 409).
- 환자 상세 = **사이드패널 아님, 전체화면 route**(`/patients/:id`·`MASK-DETAIL-01`·`NAV-SHELL-10`). 병합화면 안 떠나려면 **새 탭 "환자 상세 열기"**(ExternalLink) — 병합이력 "대상 환자 열기"가 이미 그 패턴.

**데이터 모순(버그)**
- 박서준 오른쪽: `records:1` 있는데 `lastVisit:''`(방문 없음) = 모순(`MergeCandidates.tsx:56`). 정합화. (※ 박서준은 둘 다 미연결이라 **잠기지 않음** — 잠금 문구 안 뜸. 앞서 잘못 적은 것 정정.)
- ⭐ 최유나(잠금 케이스, `:60-61`): 둘 다 `linked:true`인데 **전화번호 동일**(`010-5567-2098`) = 모순. 앱은 **번호=계정 1개**(요구사항 4.1)라 두 계정이 같은 번호 불가. 잠금(둘 다 연결)은 **번호가 서로 달라야** 성립(이름+생년 같고 계정 갈림). → ①목데이터: 최유나 d2 번호 다르게. ②헤더가 `left.phone` 하나만 표시(`:163`)라 번호 차이를 뭉갬 → **전화번호를 기록 A/B 카드 각각에** 표시(번호 차이가 관리자 판단 핵심 근거).
- 상담봇 목데이터 대폭 확대·다양화(품질/현황/신고/미해결/안내자료).

**문구/이름**
- "대표로 검토" → **"대표 검토"**(정본 line 2158 "대표 검토 → 확인창 → 확정"과 일치).
- ~~문진표 "병원이 꼭 확인" → "필수"~~ ❌ **취소(2026-08-22)** — 정본에 이 개념 규칙 없음 + 데모에선 동작 없는 정적 라벨. "필수"(환자 응답 필수)와 뜻이 달라 사용자가 **그냥 두기로 함.**

**버튼/시각(전역)**
- ⭐ 텍스트만 버튼(btnLink) 전수 점검 — "후보 목록으로"(뒤로가기) 등 이동/뒤로 버튼이 텍스트라 안 보임 → 보이는 버튼으로. 뒤로가기 가시성 전반.
- ⭐ 전역 X 닫기 아이콘 너무 작음(F-5와 같은 건, 범위 확대) → 크게/글자 병기.
- 직원관리: 좌 목록·우 초대 패널 카드 윗선 정렬.
- 칩 vs 탭 vs 버튼 affordance 일관성 — 처리전/중/완료가 버튼으로 오인되지 않게, 탭은 탭답게.

**기능/동작(방향+확인)**
- 안내자료 "새 안내자료" 버튼 기능 배선.
- 오답신고 "반려": **삭제 아님 → 처리완료(반려)+사유로 로그 유지** 권장(조용히 사라짐 금지). 정본 명시 규칙 못 찾음 → **확인 대기**.
- 참고 예시 "비활성화": 비활성↔활성 토글 + 편집(그 교정 내용 편집으로 연결).
- 병합이력 "감사메모 저장": 정본 근거 없음·목적 불명 → 제거 또는 뜻 명확화. **확인 대기**.
- 병합불가 카드에 각 기록 "환자 상세 열기"(ExternalLink 새 탭).
- 품질 리포트·상담봇 현황 기간 **7일 고정 → 기간 선택 추가**(운영통계 `STAT-SCOPE-01` from/to처럼). 정본에 7일 고정 규칙 없음. **확인 대기**.

### F-9. 문진표 버전 미리보기가 막다른 길 (사용자 지적 2026-08-22, 이미지 11)
- 과거 버전 누르면 미리보기 모달(`Questionnaires.tsx:192`)에 *"복사해 새 버전으로 저장하세요"* **안내만 있고 복사 버튼 없음** = 막다른 길. 문항 내용도 안 보이고(저장일·직원·수만), 닫기=텍스트버튼.
- 정본 `QADM-VERSION-04`: 되돌리기 버튼은 **일부러 금지**(불변 버전), 되돌림=**그 버전 복사해 새 버전 저장**. → 복사 경로는 있어야 함.
- **처리 방향**: ①미리보기에 **문항 목록 읽기 전용 표시** ②**`[이 버전을 편집기로 복사]`** 버튼(되돌리기 아님·정본의 복사 경로) — 누르면 편집기에 실리고 `[새 버전으로 저장]`으로 확정 ③닫기 제대로 된 버튼(F-8 텍스트버튼 건과 함께).

## D. 정본 반영 대기 종합 (데모는 완료, 나중에 `screen-behaviors`·목업에 반영 고려)
- **F-1** ⭐ 전체 상담 기록 → 기록 그룹(관리자 전용): `SHELL-NAV-01/02` 개정 필요(업무 8개에서 제거, 기록 그룹에 추가).
- **A-1** 이력 월 그룹(연/월 가로줄·레일에서 월 제거)
- **A-2** 이력 연도 바로가기(`HIST-*`에 규칙 추가)
- **C-4** ✅확정(2026-08-22) 홈 2건+ 전부 히어로 → `HOME-CARD-02` 뒤집기(「2건+ 줄 목록」→「전부 히어로」)
- **E-5 탭별 정렬 방향**(2026-08-22): 단일 상태 탭 정렬은 정본 빈칸 → 데모가 정본의 최신순 패턴(`PTDET-VISIT-03`·`SEND-LIST-07` 등) 적용 = 완료·취소 최신순 / 전체 시각순 내림차순(최신 예약 먼저). 정본 `QUEUE-*`에 **탭별 정렬 방향 규칙 추가 검토**(대기 계열=이른 순 / 끝난 계열=최신순).
- **E-6 오늘현황 넓은 화면 2열**(2026-08-22): 정본 `TODAY-LAY-01`은 요약을 「지금 처리할 것」 아래(세로)로 명시 → 데모는 넓은 화면에서 **오른쪽 sticky 레일**(위계 유지, 좁으면 세로 스택). 정본에 **넓은 화면 2열 배치 반영 검토**(위계 규칙 `TODAY-LAY-01`은 유지, 배치만 추가). + 상단 제목을 레일 카드로 흡수한 것도 함께.
- **E-7 접수 헤더 패널화**(2026-08-22): 정본은 접수를 **사이드바 전체화면 라우트**로(`SHELL-NAV-01/02`의 "접수" · `CHKIN-HEAD-01·03`) → 데모는 **헤더 `[QR 접수]` 패널**로(`SHELL-ACT-04` 부합, 창구 시작 동작 묶음). 정본 반영 검토: **사이드바 "접수" 제거 · `CHKIN-*` 패널 전제 · 헤더 버튼 2→3개**. (되돌리기·이탈 처리는 정본 그대로라 반영 불필요.)
- **E-8 캘린더 현재 시각 자동 스크롤**(2026-08-22): `CAL-PAST-05`는 지금 선만 정함 → 데모는 **열 때 현재 시각이 보이게 자동 스크롤**. 정본 `CAL-*`에 「오늘을 열면 현재 시각 위치로 스크롤」 규칙 추가 검토(지금 선의 자연스러운 짝).
- **E-9 헤더 화면 제목**(2026-08-22): `SHELL-HDR-01`은 헤더 왼쪽=**병원명** → 데모는 **화면 제목**을 헤더로(병원명은 사이드바 워드마크). **E-4와 같은 건** — 헤더가 병원명인가 화면 제목인가를 정본에서 확정 검토(척추·전 화면이 이미 화면 제목 쪽으로 통일됨).
- ⚠️ 나머지(로그인 화면·풍부한 카드·배지·QR·아이콘·예약탭 얇은줄·8의사 규모·종일 캘린더·지금 선·시간축 확대·대화 좌우분리·글자 위계 등)는 **정본대로거나 취지 내 개선**이라 반영 불필요.

---

## G. 사용자 폰 검수 라운드 (2026-08-23) — 전체 추적 목록

> ⚠️ 사용자가 항목이 빠질까 걱정 → 여기가 이 라운드의 원본. **tsc 0·build ✓·vitest 57** 유지.

### ✅ 완료 (이번 라운드)
- **기간 선택기**(`_ui.tsx`): '직접 지정' 제거 · **최근 1년 추가**(7/30/90일·1년·전체). 날짜 직접수정=「직접 입력」 자동표시.
- **상담봇 기록**(`chatlog/Chatlog.tsx`): 이름 "전체 상담 기록"→**"상담봇 기록"**(`StaffShell` 라벨) · 필터 한 줄 재설계(갈래=색점+건수 세그먼트/채널 토글/기간 오른쪽) · **폰트 키움**(채널·갈래·표) · 데이터 20건·카드높이 `self-start`(이전).
- **진료일정**(`admin/config/Schedule.tsx`): 요일별 총정원 tfoot **제거** · 진료과 추가/이름수정·특정날짜 추가/수정 다이얼로그(이전).
- **예약 캘린더**(`calendar/Calendar.tsx`): 날짜 클릭→**달력 팝오버(오늘부터 8주)** · 빈시간 **5분 스냅+호버** · `[전화 예약]` 버튼 제거(패널 "새 예약") · **카드 창높이 채움(StaffPage→flex h-full, 잘림 해소)** · 꺽쇠를 **날짜 양쪽**+오늘 왼쪽+`오늘±N일` 배지 제거(칩 안 밀림) · 날짜버튼 `whitespace-nowrap w-[196px]`(줄바꿈 fix) · 새예약 **환자검색 12명**+하이픈/생년 검색 · **돋보기 아이콘 위치 버그**(입력칸만 relative) · 헤더 드롭다운이 캘린더에 가리던 **z-index**(header `relative z-30`).
- **의사 진료화면**(`doctor/DoctorConsole.tsx`): **진료 중 되돌리기 제거**(DOCTOR-START-03 역전이 없음) · **임시저장+자동저장**(1.5s 디바운스, 저장중/저장안됨/저장됨·시각) · **내부 메모 추가** 인라인 배선 · **완료 후 [수정]** 사유 필수(DOCTOR-RECORD-08) · **로그인 직후 자동선택 제거**→"환자를 고르세요"(DOCTOR-START-01, 누르면 진료중 전이) · 진료완료 팝업 문구 수정.
- **역할 정리**(`StaffShell.tsx`·`patients/PatientSearch.tsx`): 의사 사이드바 **"업무" 카테고리 라벨 제거**(2개뿐) · 환자검색 **의사=「환자 상세」만**(대기목록 보기 버그 fix, SHELL-NAV-03) · 헤더 역할칩 **비밀번호 변경 패널**(SHELL-ME-03, Eye토글·조건·완료).

### ✅ 남은 것 (다음 세션) — 2026-08-23 밤 세션에서 전부 해소
- **C. 안내자료(`bot/Knowledge.tsx`) 재작업** — ①~⑦ **이미 반영 완료 확인**(아래 「✅ 반영 완료」 참조). 추가 작업 없음.
- **환자 상세 전화번호 변경 버튼** — ✅ **해소**: 사용자 정정으로 진짜 요지가 **역할 분리**였음. 의사에겐 접수 동작 숨김 + 접수직원·관리자용 인라인 패널 구현(아래 항목 6).
- **과거 진료기록 순서** — ✅ **해소**: (정렬) 최신순은 정본·데모·사용자 기억 일치, "충돌"은 "오른쪽 위" 오독. (배치) 사용자 정정으로 **의사 화면 작성 칸을 위로** 스왑(아래 항목 7, 정본 반영 대기).
- **작은 글자 전역 스윕** — ✅ **선별 상향 완료**(11px 하한, 아래 항목 9).
- (답변 완료) 주의·보행 표시 추가 = 대기목록 환자 행 `[응급/주의 표시]`→확인 팝업 사유입력(QUEUE-URG-02, 이미 있음).

### 🟡 결정 대기 (2026-08-23) — 도착 vs 진료대기 2단계가 필요한가
- 우리 시스템엔 **별도 의사 배정 단계가 없다**(예약·워크인 등록 시 의사 확정). 그래서 요구사항 전제 line 5 "환자 등록→의사 배정→진료 대기"는 레거시 흐름이고, 우리 실제 흐름엔 도착↔진료대기를 가르는 강한 트리거가 약함.
- 남는 유일한 실익 = **일찍 온 환자를 순번(진료대기)에 바로 안 넣어 대기줄·대기시간 왜곡 방지**(요구사항 3.4 "대기 순서 조정").
- **선택지**: (A) 지금처럼 [도착 처리]→[진료 대기로] 2단계 유지 / (B) [도착 처리]=곧바로 진료대기 자동 전이(도착 상태는 enum에 남기되 특수 케이스용). **사용자 결정 필요.** 요구사항은 두 상태의 존재만 요구, 수동 2단계는 요구하지 않음.

- **⭐ 논의 심화(2026-08-23)**: 이 프로젝트엔 **일찍 온 환자 처리 기능이 아예 없다** → 2단계의 유일한 명분이 미구현 예외임. 정상 환자마다 [도착 처리]+[진료 대기로] 2클릭은 과함. 단 도착을 삭제하면 요구사항 3.2("현재 도착한 환자" vs "현재 대기 중" 별도 카운트)를 어김.
- **추천안(기본값 뒤집기)**: 정상 = `[도착 처리]` 1클릭 → **바로 진료대기**. 예외 = `[도착만·순번 보류]` 선택 시에만 도착 유지. 효과: ①2클릭 강제 제거 ②지금 없던 "일찍 온 환자" 처리를 도착-보류가 담당 → "현재 도착한 환자"=순번 보류 중(접수직원이 챙길 대상)이라는 의미있는 숫자. → **사용자 확정 후 상태 전이·QUEUE/TODAY 화면 반영.**

- **⭐⭐ 재논의(2026-08-23, "도착-보류" 안 폐기)**: 순번은 진료대기 진입 시 부여(`QUEUE-ORDER-01`), 의사 화면엔 진료대기만 노출. "도착만 보류 후 시각 맞춰 재클릭"은 **직원이 시각을 기억해야 해서 폐기.** 시각 처리는 시스템이 맡아야 함:
  - **(1) 추천 — 도착 처리 1클릭=바로 진료대기 + 대기줄 예약시각 순 정렬.** 일찍 온 환자도 예약시각 자리에 놓여 새치기 없음. 직원 시간기억·2클릭 없음. 단 도착↔진료대기 실질 통합(3.2 두 카운트→하나).
  - **(2) 도착 유지 + 시각 기준 자동 전환.** 도착 처리 1클릭 → 예약시각 되면 시스템이 자동 진료대기. 3.2 두 카운트 유지("도착"=왔지만 시각 전 / "대기"=시각 도달).
  - 공통 원칙: **직원이 시각 기억해 재클릭하는 설계 금지.** 사용자 결정 대기.

### ✅✅ 해소 — 도착 vs 진료대기 (사용자 확정 2026-08-23 저녁)
> 위 「🟡 결정 대기」의 (A)/(B)/(1)/(2)는 아래 하이브리드로 **대체·해소**.
- **확정 모델(사용자 안)**: 대기목록 「아직 안 옴」에 버튼 둘 — 예약 시각이 됐/지났으면 `[진료 대기]`(바로 순번), 아직 일찍 왔으면 `[도착]`(보류). **도착(보류)한 분은 예약 시각이 되면 시스템이 자동으로 진료 대기로 올린다**(직원 재클릭·시각 기억 없음).
- **환자앱 대기시간**: 정본이 이미 도착 상태엔 숫자를 안 띄우고(`CARD-IN-02`) 진료대기에서만 예약시각 기준으로 계산 → "일찍 온 사람이 정시 환자 때문에 갑자기 늘어나는" 문제 없음. (정본 반영 시: 도착→진료대기 **예약시각 자동 전환** 규칙 신설 + `QUEUE-ORDER`에 예약시각 우선 명시 검토.)

### ✅ 완료 (2026-08-23 저녁 세션 · tsc 0 · vitest 58 · 미커밋 · 크롬 미확인)
- **① 도착/진료대기** (`queue/Queue.tsx`·`today/Today.tsx`·`mockData.ts`): 「아직 안 옴」 두 버튼(시각 됨→진료 대기 딥틸 / 일찍 옴→도착 딥틸) · 데모 기준시각 `NOW='10:00'` · 도착 목데이터를 일찍 온 분(미래 예약)으로 · 「도착」탭 안내 배너 + 예약시각(자동 전환 시각, 보라) 표시 · 미접수 카드 [도착 처리]→[진료 대기].
- **③ 그대로 두기 가시성** (`today/Today.tsx`): 처리 상태를 Row 지역→Today `Set`으로 올림 · 카드 다 처리 시 **"모두 처리했습니다 · 화면을 떠나면 사라집니다" 빈 상태** + 남은 건수 카운트다운(제목 옆 숫자).
- **④ 예약·상담 보기 = 캘린더 컨텍스트 패널** (`calendar/Calendar.tsx`·`today/Today.tsx`): 오늘의 현황 상담/옮기기 버튼 → 캘린더로 이동(`location.state.panel`) → 오른쪽 **읽기전용 컨텍스트 패널**(상담 요약 + [상담 전체 보기]→문의함 / 옮길 예약 + 빈칸 클릭 안내) · 넘어온 의사 열로 자동 필터. `TodayContextPanel` 신설.
- **⑦ 버튼색 = 의미 유지**: 딥틸=한 번에 끝나는 동작 / 흰=다른 화면으로 가서 처리. 코드 주석으로 규칙 명시(통일/번갈아 아님).
- **② 상담봇 문의함** (`tickets/Tickets.tsx`·`mockData.ts`): 담당(배정) 후보에서 **의사 제거**(접수직원·관리자만, 의사는 문의함 미접근 `SHELL-NAV-03`) · 의료 판단 표식 **"직접 답하지 말고 담당 의사에게 확인한 뒤 답변"**(안전장치 유지) · 이름 **문의 티켓함→상담봇 문의함**.
- **⑤ 메뉴** (`StaffShell.tsx`): 순서 업무→기록→**상담봇 관리→설정**(설정 맨 아래) · "상담봇"→"상담봇 관리".
- **⑧ 표 헤더 글자** (`Schedule.tsx` 2표·`Messages.tsx`·`MergeHistory.tsx`): 11px→`text-sm`. ⚠️ 사용자 정정: "넘침"이 아니라 "작다"였음.
- **ChatEngine.tsx tsc 오류(다른 세션 작업물)**: 유니온 분배 Omit(`DistributiveOmit`)으로 3건(bubble/card/text) 해소.

### ✅ 반영 완료 (2026-08-23 밤 세션 · tsc 0 · vitest 58 · build ✓ · 미커밋)
1. **버튼 라벨 "진료 대기로" → "진료 대기"** — `Queue.tsx`(미도착·도착 케이스 버튼 + 배너 `[진료 대기]`), `Today.tsx` noshow 버튼.
2. **버튼 위치 고정** — `Queue.tsx` not_arrived: 항상 `[진료 대기][도착]` 순서, `early` 여부로 **색(딥틸=추천)만 이동**. 조현우(10:20, 일찍 옴)도 위치 동일·[도착]만 딥틸.
3. **탭 이름 "아직 안 옴" → "미도착"** (사용자 확정) — `QUEUE_TABS`·`STATUS_LABEL.not_arrived`·doorData status·`_ui.tsx` STATUS_TONE(`미도착:'slate'`)·코드 주석까지 일괄.
4. **그대로 두기 라벨** (사용자: 동작별 문구) — `Today.tsx` `processed` Set→**Map(id→문구)**. 도장이 동작별로: 그대로 두기→**"확인함"**, 취소→"취소함", 마감→"마감함", noshow→"진료 대기로 보냄". 동작·사라짐은 정본대로 유지.
5. **표 제목 정렬** (사용자: 현행 유지+어긋난 것만) — 6개 표 전수 점검 결과 **헤더↔데이터 방향 어긋남 0건**. 글자열=왼쪽/숫자열=오른쪽·가운데로 이미 정확히 대응. **변경 없음**(왼쪽 정렬이 의도된 표준).
6. **④ 전화번호 변경 = 역할 분리** (사용자 정정: 애초 의도가 이것) — 의사 계정이 접수화면 재사용해 접수 동작이 다 보이던 문제. `PatientDetail.tsx`: `canEditContact=!isDoctor`로 **[전화번호 변경]·[가족 연결 추가]를 의사에게 숨김**(`SHELL-NAV-03`·`ROLE-DOC-02`·`DOCTOR-CONTEXT-01`). 의사에겐 이 화면=**과거 진료기록 열람**용(읽기 `ROLE-READ-01`, 내부 메모는 `PTDET-NOTE-02`대로 의사도 추가 유지). + 접수직원·관리자용 **인라인 전화번호 변경 패널**(새 번호→인증→변경, `PTDET-ACTION-02`) 구현해 자리표시 해소.
7. **⑤ 의사 화면 진료기록 배치** (사용자 정정: 순서 아니라 배치) — `DoctorConsole.tsx` 오른쪽 열에서 **진료기록 작성 칸을 위로, 과거 기록 목록을 아래로** 스왑. ⚠️ 정본 `DOCTOR-HISTORY-01` 「오른쪽 위」와 어긋남 → **정본 반영 시 함께 갱신·역참조** 필요.

> 참고 해소: **진료기록 정렬(최신순)** = 정본(`DOCTOR-HISTORY-01`·`PTDET-RECORD-01` 날짜 역순)·데모·사용자 기억 모두 최신 위로 일치 → 변경 없음(핸드오프의 "충돌"은 "오른쪽 위" 오독). **RTK 삭제** = 사용자 C(그냥 둠), 현 버전 한글 정상.

### ✅ 남은 작업도 이어서 완료 (같은 세션 · tsc 0 · vitest 58 · build ✓ · 미커밋)
8. **⑥ QR 접수(CheckinForm) 정합** — 결과 카드의 단일 [도착 처리] → **대기 목록과 같은 두 버튼 모델**([진료 대기][도착] 순서 고정, `early`=예약시각보다 이른가로 색만 이동). `NOW` import·`arrived` bool→`outcome`('arrived'|'waiting'). 처리 후 상태 배지(도착=보라/진료 대기=하늘)·안내문(도착=시각에 자동 전환 / 진료 대기=순번 부여)·[대기 목록에서 보기]. 데모 예약 3건이 두 경우 다 커버(09:00·09:30=시각 됨 / 10:20 조현우=일찍 옴).
9. **작은 글자 스윕** (사용자: 밀도 타당한 것과 구분해 선별) — **11px 하한 확립**: 독립 배지·마커 `text-[10px]`→`text-[11px]` 5곳(Tickets 새 메시지·Questionnaires 현재 사용·Schedule 슬롯 요약·Calendar 지금선/호버 시각). **캘린더·도어 블록 내부 3곳**(Calendar 블록 라벨·surfaces 블록 sub/시각)은 좁은 칸 넘침 위험으로 **유지**. `text-[0.7rem]`(≥11px)·`text-[11px]`(밀도 표준 74곳)은 하한 이상이라 그대로.

> **C. 안내자료(Knowledge.tsx) 재작업 = 이미 완료 확인** — 핸드오프 「🔜 남은 것」의 7개 항목(①빈 편집기 ②진입 시 고르세요·프리필 제거 ③칩 위·상단줄 정렬 ④수정이력 보이는 버튼 ⑤아래 꺽쇠 ⑥"이 버전 불러오기"+되돌리기 아님 명시 ⑦승인 대기 상태 제거→승인됨/임시저장 2상태)이 전부 반영돼 있음(mockData `KnowledgeStatus='승인됨'|'임시저장'`). 핸드오프 상단이 낡았던 것. **추가 작업 없음.**

### ✅ 배포 후 추가 검수 (커밋 `31ae819`, 재배포 완료)
10. **grid-div 표 헤더 글자** — 상담봇 기록(Chatlog)·자동알림(HospitalSettings)·품질 상담목록(Quality)의 열 헤더가 `text-xs`/`text-[11px]`로 남아 있던 것 → **`text-sm`**. ⚠️ 앞선 ③ 표 정렬 점검이 `<th>`만 봐서 grid-div 헤더 3곳을 놓쳤음(사용자 지적).
11. **예약 캘린더 날짜 팝오버 8주 잘림** — 날짜 선택 팝오버에서 8주 뒤가 안 보이던 문제. 원인=StaffShell 본문 `<main overflow-y-auto>`(한 축만 auto여도 다른 축도 auto로 계산돼 박스 밖을 자름)가 `absolute` 팝오버 아래쪽을 클립. → 팝오버를 **버튼 rect 기준 `fixed`**(z-40/50)로 전환해 해소. (격자 로직 자체는 0~56일 정상이었음.)

> **배포**: Vercel `iansoft/demo`, 공유 URL(고정) https://demo-pi-inky-72.vercel.app · `demo/vercel.json`=SPA 리라이트 · 재배포 `cd demo && npx vercel --prod --yes`. 상세=메모리 `reference-demo-vercel-deploy`.
