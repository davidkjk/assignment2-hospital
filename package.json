{
  "name": "hospital-erd-easy-guide",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "mkdir -p public && cp docs/erd-easy.html public/erd-easy.html",
    "build": "vinext build",
    "postbuild": "mkdir -p dist/.openai && cp .openai/hosting.json dist/.openai/hosting.json"
  },
  "dependencies": {
    "react": "19.2.6",
    "react-dom": "19.2.6",
    "vinext": "1.0.0-beta.4"
  },
  "devDependencies": {
    "@vitejs/plugin-rsc": "0.5.32"
  }
}
