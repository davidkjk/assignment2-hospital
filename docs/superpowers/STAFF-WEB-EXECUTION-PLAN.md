# 직원웹 구현 실행 플랜 (paseo + Opus 4.8)

> **무엇**: 직원웹 화면·백엔드를 **무엇을·어떤 순서로·어떤 파일에** 만드는지의 정본. "어떻게 굴리나"(모델·심박·바통·알림)는 `IMPL-ORCHESTRATION-PLAYBOOK.md`.
> **이 문서의 뿌리**: 2026-08-26에 폐기한 `CODEX-3DAY-EXECUTION-SHEET.md`의 태스크 원천(§2 상태·§4 웨이브·§5 태스크표·병렬 규율·함정)을 **paseo/Opus 4.8 기준으로 옮긴 것**. Codex/Orca 색채는 걷어냈고, "무엇을 만드나"는 그대로 살렸다.
> **정본 관계**: 규칙 = `docs/design/screen-behaviors.md`(무엇이 그렇게 동작하나). 마이그 번호 = `docs/design/spec-index/MIGRATION-LEDGER.md`. 태스크 상세 = `docs/superpowers/plans/2026-08-15-staff-web.md`의 `## Task N` 절.

---

## 0. ⛔ 방향 전환 (2026-08-27) — Wave A~C 태스크 트랙은 끝났다

> **이 문서의 §2 웨이브·§4 태스크표(Task 4~30)는 「실 frontend를 규칙서에서 새로 짓는」 트랙이고, 그 트랙은 2026-08-27에 종료됐다.**
> 브라우저 대조 결과 실 frontend의 **프론트 규칙 충실도가 데모보다 낮았다**(원인: 워커 합격기준이 jsdom 테스트라 시각·인터랙션·레이아웃을 못 잡음).
>
> **지금의 구현 정본 = `docs/superpowers/plans/2026-08-27-staff-web-demo-first-merge.md`** — 데모(`demo/src/routes/staff/**`)를 뼈대로 `frontend/`에 들여오고 실 api·인증·마스킹을 배선한다.
> 화면별 이식 체크리스트 = `docs/design/spec-index/DEMO-PORT-RULE-MAP.md`.
>
> **이 문서에서 계속 유효한 것**: §5~§8 **워커 규율·코디 자기점검·함정 모음** · §9 **살아있는 갭 3건**(#128 · 백엔드 계약갭 2건 · `SEARCH-LOG-06`) · §1 커밋 이력 · §9 진행 로그(무엇이 이미 지어졌는지의 사실 기록 = 새 계획의 「실 원본(로직 참조)」이 어디 있는지 알려준다).
> **더 이상 따르지 않는 것**: §2 웨이브 순서 · §3 Task 4 재구현 지침 · §4 태스크표의 실행 순서.
>
> ⭐ **프론트 합격 판정은 「브라우저로 데모 대조 + 규칙ID 체크」다.** jsdom 테스트 통과는 회귀 가드일 뿐 판정자가 아니다.

---

## 1. 지금 상태 (2026-08-26)

- **커밋됨**(전부 `merge/design-integration`): Task 0(시각 토큰 `e86d705`) · Task 1(마이그 00033~35 `2099738`) · Task 2(일정 변경 판정 서비스 + 00036 `8c1a59c`) · Task 3 **부분**(진료문구 서비스+라우터+me, `8c1a59c`) · seam 배선(schedule_change 라우터 2종 `fcb5808`).
- **Task 4 = 재구현 대상(사용자 결정 2026-08-26).** 옛 Codex 트랙이 Task 4(React 스캐폴딩·직원 인증·공통 셸·역할 라우팅)를 만들었으나 **독립 재검증에서 FAIL**(HIGH 3·MEDIUM 6). 이후 4개 커밋(`349ee6a`·`35dc7b7`·`0b7c401`·`d90a1f9`)으로 부분 패치됐지만 **전수 해소가 확인되지 않았다.** → 조각 패치를 쌓지 말고 **Opus 4.8로 Task 4를 깨끗이 다시 구현**한다(§3).
- **다음 빈 마이그 번호**: `00037`. (직원웹 밴드 `00033~00051`.)
- **결번**: **Task 16·Task 23은 없다**(플랜에서 흡수·삭제). 남은 것 = **Task 4(재구현)·5~15·17~22·24~30**.
- **로컬 DB**: 로컬 supabase 기동 필요(`supabase status`, DB_URL `…54322`). 테스트 전 Docker 켜져 있어야 함.

## 2. 실행 순서 — 3파(wave)

```
Wave A  (직렬 — 모든 것의 관문)
  Task 4  React 스캐폴딩 + 직원 인증 + 공통 셸 + 역할 라우팅  ← 재구현. 한 워커가 신중히(구조 1회)

Wave B  (Task 4 후 병렬 — 서로 독립 파일 = 백엔드/기반)
  Task 5  API 클라이언트 레이어         Task 6  patients 라우터 + 마스킹
  Task 13 조회 전용 백엔드              Task 17 일정 백엔드 + 운영시간 판정기 (어려움)
  Task 25 통계 대기지표 백엔드          Task 7  공통 컴포넌트 + 되돌리기(00037) (시각, Task 4 셸 후)

Wave C  (Task 4·5·7 후 — 화면 15개, route 폴더 격리 병렬)
  8·9·10·11·12·14·15·18·19·20·21·22·24·26·27·28·29·30
  (Task 18은 17 후 · Task 30은 28 후 · Task 26은 21 후)
```

**선행 하드 규칙**:
- **Task 4 → 모든 프론트**(스캐폴딩 없으면 화면이 못 뜬다). 직렬, 한 워커가 신중히.
- **Task 5(API 클라)·Task 7(공통 컴포넌트) → 모든 화면**이 소비. Wave C 전에 끝낸다.
- **Task 13 → 화면 8·10·11·12** · Task 17 → 18 · Task 6 → 10·24 · Task 25 → 12.
- 한 파 **병렬 워커 5개 이하**. 화면 15개는 3배치로.

## 3. Task 4 재구현 지침 (첫 태스크)

> 옛 Task 4의 실패 지점을 반복하지 않도록, 재구현 시 아래를 규칙으로 덮는다. 상세 규칙 ID는 `plans/2026-08-15-staff-web.md`의 `## Task 4` 절 + 그 절이 가리키는 behaviors 규칙.

- **범위**: `/login` 직원 인증 + 공통 셸(`StaffShell`: 사이드바 4그룹·헤더 세 문) + 역할 라우팅 + React 스캐폴딩.
- **옛 재검증이 잡은 것(재구현 시 반드시 덮기)**:
  - **HIGH**: ①복구 세션이 아닌 일반 로그인 세션은 **새 비밀번호 화면을 허용하면 안 됨** ②헤더 왼쪽은 **화면명이 아니라 병원명** ③아이콘 경로가 **SVG를 반환**해야 함(프로덕션 빌드에서 SPA HTML 반환 금지).
  - **MEDIUM**: JWT `sub` 누락/invalid UUID → **401 정규화**, 역할표 중복 제거, 권한 차단 기본화면은 **link가 아닌 button**, 축소 내비 focus tooltip/active 딥틸 일치, 비밀번호 변경 `SET-PW-*` 규칙 완전 구현, 복구 redirect는 **frontend origin**(신뢰 `STAFF_WEB_ORIGIN`).
- **개인정보 열거 방지**: 로그인 실패는 맞든 틀리든 같은 화면으로.
- **재구현 방식 판단(Task 4 착수 시 지휘자가 결정)**: 기존 4개 패치 커밋을 살릴지(그 위에서 규칙 워크로 전수 덮기) vs 되돌리고 새로 짤지. **되돌림(revert)은 되돌릴 수 없는 판단이 아니므로**, 착수 전 현재 코드 상태를 규칙 대비로 훑어 "패치가 규칙을 얼마나 덮었나"를 먼저 재고 결정한다. 큰 revert면 사용자에게 텔레그램으로 확인.

## 4. ⭐ 태스크별 실행 표

> **마이그 번호는 이 표 + `MIGRATION-LEDGER.md`가 정본.** 플랜 산문 옛번호(00017류)는 **+16 시프트**로 무시하고, 플랜의 `Create: supabase/migrations/…` 줄만 믿는다.
> **모델(사용자 확정 2026-08-26 개정)**: 구현·검증 **전부 Opus 4.8 · 노력 전부 `high`.** ⬅️ 아래 표의 `노력` 열 `xhigh`/`max`는 **전부 `high`로 대체**(이 줄이 우선). 비가역 병합(21·26)은 별도 적대리뷰 에이전트 없이 **코디 리뷰를 더 꼼꼼히** 보는 것으로 보완(이상 징후 시에만 추가). Sonnet 5는 기계작업에만. **독립 검증 PASS 전 완료 주장 금지.**

| Task | 화면/내용 | 종류 | 마이그 | 노력 | 데모 레퍼런스 (`demo/src/routes/`) | 의존·주의 |
|---|---|---|---|---|---|---|
| **4** | React 스캐폴딩+직원 인증+공통 셸+역할 라우팅(`/login`) | 구조/시각 | — | **high**(구조 1회, §3) | `staff/auth/Login.tsx`·`staff/StaffShell.tsx` | **재구현. 모든 프론트의 관문. 직렬.** |
| **5** | API 클라이언트 레이어 + 오프라인·오류 계약(`OFFX-STAFF-*`·`ERR-*`) | 로직 | — | high | — | 모든 화면 선행 |
| **6** | `patients` 라우터 + 마스킹·열람 기록(`MASK-*`·`SEARCH-LOG-*`) | 로직 | — | high | — | Task 10·24가 소비 |
| **7** | 공통 컴포넌트 + 되돌리기 계약(`PANEL/UNDO/BTN/PICK-*`) | 시각 | **00037** `status_undo` | high | `staff/_ui.tsx`·`staff/doors/*` | 화면들 선행. Task 4 셸 후 |
| **8** | `/today` 오늘의 현황(`TODAY-*`) | 시각 | — | high | `staff/today/Today.tsx` | Task 13 소비 |
| **9** | `/queue` 대기 목록 + 당일 방문 등록(`QUEUE-*`) | 시각 | 00038 `walkin_visit_time`* | high | `staff/queue/Queue.tsx` | |
| **10** | `/patients/:id` 환자 상세(`PTDET-*`) | 시각 | — | high | `staff/patient/PatientDetail.tsx` | Task 6·13 |
| **11** | `/doctor/console` 의사 진료 콘솔(`DOCTOR-*`) | 시각(밀도) | — | **xhigh** | `staff/doctor/DoctorConsole.tsx` | Task 3·13 |
| **12** | `/admin/stats` 운영 통계(`STAT-*`) | 시각(밀도) | — | **xhigh** | `staff/admin/record/Stats.tsx` | Task 13·25 |
| **13** | 조회 전용 백엔드(today·대기·이력·콘솔·통계) | 로직 | — | high | — | 화면 8·10·11·12 선행 |
| **14** | `/calendar` 예약 캘린더 + Realtime(`CAL-*`·`SUPPORT-CAL-*`) | 시각(최고밀도) | 00039 `appointment_time_range_realtime` | **xhigh** | `staff/calendar/Calendar.tsx` | Realtime 주의 |
| **15** | `/admin/access-logs` 열람 기록(`ALOG-*`·`SEARCH-LOG-*`) | 시각 | 00040 `access_audit_log_index` | high | `staff/admin/record/AccessLogs.tsx` | Task 6 |
| **17** | 진료과·일정 관리 백엔드 + **운영시간 단일 판정기**(`SCHED-DEPT/SLOT-*`) | 로직(어려움) | 00041 `hospital_hours_closures` | **xhigh** | — | Task 18 선행. **하드 판단** |
| **18** | `/admin/schedule` 화면 + 라우트 조립(`SCHED-TAB/GRID/WEEK/SAVE/EXC/HOURS-*`) | 시각 | — | high | `staff/admin/config/Schedule.tsx` | **Task 17 후** |
| **19** | `/admin/staff` 직원 관리 + 의사 프로필·캘린더 색(`STAFF-*`·`CAL-COLOR-*`) | 시각 | 00042 `staff_profile_palette` | high | `staff/admin/config/StaffAdmin.tsx` | |
| **20** | `/checkin` QR·예약번호 접수(`CHKIN-*`) | 시각 | 00043 `fix_booking_code_length`(6자리 버그수정) | high | `staff/checkin/Checkin.tsx`·`CheckinForm.tsx` | |
| **21** | `/admin/patient-merge-candidates` 중복 병합(`MERGE-*`) | 시각(밀도)+**비가역** | 00044 `patient_merges` | **max + 적대리뷰** | `staff/admin/record/MergeCandidates.tsx` | **파괴적 — 신중** |
| **22** | `/admin/questionnaires` 문진표 관리(`QADM-*`) | 시각 | 00046 `questionnaire_versions` | high | `staff/admin/config/Questionnaires.tsx` | 불변 버전 |
| **24** | `/patients` 전역 환자 검색(`SEARCH-*`) | 시각 | — | high | `staff/patients/PatientSearch.tsx` | Task 6 |
| **25** | 운영 통계 — 오래 대기 건수·명단(`STAT-METRIC-04`) | 로직 | 00047 `search_audit_counts` | high | — | Task 12 소비 |
| **26** | `/admin/merge-history` 병합 이력·되돌림(`MHIST-*`) | 시각+**비가역** | — | **max + 적대리뷰** | `staff/admin/record/MergeHistory.tsx` | **Task 21 후. 파괴적** |
| **27** | `/admin/errors` 시스템 오류(`ERRADM-*`) | 시각 | 00048 `system_error_safe_summary` | high | `staff/admin/record/Errors.tsx` | 안전 요약(redaction) |
| **28** | `/messages` 발송 만들기 — 제1문·패널·enqueue(`SEND-*`·`MSGX-*`) | 시각+로직 | 00049 `scheduled_notifications_cancel` | high | `staff/messages/Messages.tsx` | Task 30 선행 |
| **29** | `/admin/settings` 병원 설정(`HSET-*` 71 + `HSETX-*` 19) | 시각(최고밀도) | 00051 `hospital_settings_full`(공유칸 `if not exists`) | **xhigh** | `staff/admin/config/HospitalSettings.tsx` | **⚠️ 없는 칸 저장값처럼 노출 금지**(`HSETX-DATA-01`) |
| **30** | `/messages` 발송 결과·실패·재시도(`SEND-RESULT/RETRY/FAIL/DEAD-*`) | 시각+로직 | 00050 `notification_log_dispatch` | high | `staff/messages/Messages.tsx` | **Task 28 후**(세로 분할 2/2) |

\* 라벨 없는 번호는 원장 「Create 줄 기준」. 새 마이그가 필요하면 **직원웹 밴드 다음 빈 번호**를 쓰고 원장 갱신.

> **📌 상담봇 그룹 화면**(문의함·상담기록·안내자료·미해결·오답·품질·현황)은 이 플랜 밖 — **`ai-chatbot` 플랜(4단계) 소유**. 손대지 말 것.

## 5. ⚠️ 포팅의 핵심 함정 — 데모는 규칙을 다 반영하지 않는다

> **정본은 `screen-behaviors.md` 규칙이다. 데모는 「어떻게 보이나」의 시각 참고일 뿐, 「무엇이 있어야 하나」의 목록이 아니다.** 데모만 보고 옮기면 데모가 빠뜨린 규칙이 그대로 빠진다(사용자 검수를 거쳤어도 규칙의 부분집합).

**데모가 규칙을 놓치는 4형태**: ①한 상태만 그림(빈/로딩/오류/전이는 `*-EMPTY/STATE/ERR/LOAD/RACE` 규칙에 있고 데모엔 없음) ②옛 라벨/값(예 기간 「30일·90일」 vs 정본 `PERIOD-BOX-02` 「1개월·3개월」) ③규칙 자체 누락 ④요구사항 좁힘(검색을 「이름만」으로 좁힘 vs 규칙은 전화번호도).

**작업 방법 = 「규칙 워크」**: 화면의 규칙을 한 줄씩 훑으며 각 규칙의 시각 짝을 데모에서 찾는다. ①데모에 있고 규칙과 맞으면 → 포팅. ②데모에 없거나 규칙과 다르면 → **규칙이 이긴다.** 규칙대로 구현하고, 새 시각은 데모 디자인 시스템(`tokens.css`·간격 리듬) 위에서 그린다(=「신규 요소」, frontend-design 스킬 렌즈). **"다 됐나"의 판정 = 데모와 닮았나가 아니라 「규칙 ID를 다 덮었나」.**

## 6. 병렬 워커 규율 (어기면 가짜 실패)

1. **같은 브랜치 기반·다른 파일**(화면별 route 폴더 격리) 또는 worktree 격리. **공용 파일**(`routes.tsx`·`StaffShell`·`_ui`·공용 mockData·`main.py`)은 **워커 금지 → 코디가 병합 때 배선**.
2. **커밋은 코디네이터가** 완료 회수마다. 워커는 **자기 파일만 `git add`**(`git add -A` 금지).
3. 워커는 **`supabase migration up`만**(`db reset` 절대 금지 — 공유 DB). **focused 테스트만.**
4. **전체 회귀는 코디가 클린 DB(`supabase db reset`)에서 1회.** ⚠️ 코디가 검증을 반복하면 공유 DB 오염→가짜 실패.
5. 격리 워크트리면 브리프에 `python -m venv .venv && .venv/bin/pip install -r backend/requirements.txt` / `cd frontend && npm install` 먼저.
6. 창구는 브리프에 `Consumes:`/`Produces:`로 **이름 명시**. DB 칸 나오면 서버층 짝 확인.

## 7. 코디네이터 자기점검 (커밋·병합 전마다)

- [ ] 워커가 **자기 파일만** 손댔나? (`git status`로 공용 파일 오염 확인)
- [ ] 마이그 번호가 **이 표/원장과 일치**·중복 없나? (`python3 docs/design/spec-index/plan-migration-check.py` = exit 0)
- [ ] **전체 회귀를 클린 DB에서 1회** 돌렸나? (`supabase db reset` 후 `pytest`)
- [ ] 시각 화면이면: 데모 대비 **구조·간격 포팅**·하드코딩 색이 `tokens.css`를 쓰나? **규칙 ID를 다 덮었나(규칙 워크)?**
- [ ] 선행 의존이 먼저 끝났나? (Task 5·7 없이 화면 병합 금지 / Task 17 없이 18 금지)
- [ ] 비가역 화면(21·26)이면: 빨간 버튼이 **확인창 안에만**·막다른 길 없나·되돌림 경로 있나?
- [ ] **독립 검증(fresh Opus 4.8) PASS** 받았나? (자기 코드 자기 리뷰 금지)
- [ ] 커밋 메시지에 Task·마이그 번호. 병합은 `git merge --no-ff`.
- [ ] `HANDOFF.md` 최상단 심박 + 이 문서 §9 진행 로그 1줄 갱신.

## 8. 함정 모음 (계속 적용)

- **마이그 번호**: 플랜 산문 `00017`류 낡음(+16). `Create:` 줄과 원장만. 다음 빈 번호 **00037**.
- **Task 16·23은 결번** — 찾지 말 것.
- **공유 DB 오염 = 가짜 실패**: 리셋하면 사라지는 실패는 버그 아님.
- **`git add -A` 금지**, 공용 파일은 코디만.
- **환자 노출 문구**: "취소 요청 접수/등록" 금지 → **"상담(직원 확인)으로 연결"만**.
- **되돌릴 수 없는 동작은 눈에 덜 띄게**(빨간 버튼은 확인창 안). **막다른 길 금지**. **끌 수 없는 스위치 금지**. **개인정보 열거 방지**.
- **이모지 금지** — 아이콘은 SVG `<symbol>`+`<use>`.
- **낡은 `⏳`/미결** 그대로 믿지 말 것 — 주제어 grep 재확인.

## 9. 진행 로그 (세션 간 이어쓰기 — 각 Task: `Task N — 커밋 — 미결로 판단한 것`)

- **2026-08-26**: Codex/Orca 체제 폐기 → paseo/Opus 4.8로 이 플랜·플레이북 재작성. 텔레그램 알림봇 연결(`.claude/orchestration/notify.sh`).
- **Task 4 — `c08bea9` — PATCH-FORWARD로 완주**: 독립 재검증(`.claude/orchestration/task4-verification.md`) 결과 옛 코드 8/9 해소·핵심 로직 정상이라 revert 아닌 patch-forward 판정. 남은 갭 2건만 수정 — G1(헤더 왼쪽 화면명→**병원명** 비링크, 화면제목은 본문 `<main><h1>`, `HOSPITAL_NAME` 상수 신설 `shell/brand.ts`) + G2(Sidebar active 배경 하드코딩 hex→`--color-primary-wash` 토큰). 약화됐던 `Header.test.tsx` 2건(SHELL-HDR-01·NAV-SHELL-12) 정본대로 복원. 프론트 57/0·백엔드 24/0 GREEN. **판단: 병원명은 Task 29(병원설정) 붙기 전까지 상수(`가온병원`)로 둠 — 되돌리기 쉬운 임시.** ⚠️ 플랜 Task 4 절의 테스트 스펙 중 그룹순서 `['업무','기록','설정','상담봇']`·접수직원 8항목은 **낡음**(정본 behaviors 2026-08-24 개정 = `업무→기록→상담봇 관리→설정`·6항목). 코드가 정본을 따름 — 후속 워커는 플랜 낡은 값에 맞추지 말 것.
- **Task 5 — 커밋대기 → 커밋 — 갭 #14는 이미 닫혀 있었다**: `apiFetch`/`ApiError`(서버 문장 그대로·`BTN-TIME-01` 시간제한 없음)·`useConnectivity`(연결판정 단일지점)·시각3(`OfflineBanner`/`EmptyState`/`InlineError`, frontend-design 렌즈+토큰만) + api 래퍼 5(`appointments/patients/medicalRecords/quickPhrases/schedule`). **판단/발견**: 플랜이 "백엔드 6곳 `AppError(str(exc))` 고쳐라"였으나 커밋 `e40a67f`에서 이미 `pg_error_to_app_error()` 경유로 마스킹 완료 → 백엔드 프로덕션 무변경, `test_error_messages.py`는 회귀 가드로만 신설. 프론트 95/0·백엔드 9/0 GREEN. ⚠️ **코디 배선 TODO**(공용 파일이라 워커가 안 함, 셸/App 붙일 때 필요): ①온라인 401→세션만료(`isSessionExpiry`→`rememberReturn(path, staffId)`→logout→/login, 키 `staff-session-return`) ②`apiFetch` 성공 시 `markServerOk()` ③`ConnectivityProvider`+`OfflineBanner` 셸 마운트. `patients.ts`는 Task 6 라우터 계약 경로로 미리 얇게 감쌈.
- **Task 6 — 절반 완료(커밋대기)**: Steps 1–5(=`core/masking.py` `mask_phone`/`mask_birth_date`·`routers/patients.py` `GET /patients`·`/{id}`·`/{id}/contact`·`audit_service.log_access` 확장(검색어·nullable patient_id)·`patient_service` search/detail/reveal) 완료, pytest 14/0. ⚠️ **Steps 6–10 = 가족 연결 저장 창구(R5-01)는 미완** — 내가 브리프에 Files 블록만 인용해 빠뜨림(플랜 Files 블록 자체가 Step 6~10과 불일치). **후속 Task 6b 필요**: `00045_family_link_verification.sql` + `patient_service.link_family_member`/`unlink_family_member` + `POST/DELETE /patients/{id}/family`(라우터) + `test_family_link.py`. Task 10·13의 가족 저장이 이걸 소비. ⚠️ **코디 배선**: `main.py`에 `patients.router` 등록. **판단**: 검색 audit는 서비스역할 풀로 씀(00004가 authenticated UPDATE 미grant), staff_id는 검증세션값이라 위조위험 없음 — RLS로 바꾸려면 별도 마이그 필요(미생성, 후속 검토).
- **Task 6 `b2fad2e` · Task 7 `e9796de` (Batch2 병렬 커밋)**. Task 7: `00037_status_undo.sql`(역전이표, 한 칸 뒤로만) + `undo_status`/`UNDO_TRANSITIONS`/`can_undo`/`reason_required`(DB예외 `pg_error_to_app_error` 마스킹) + 공통 컴포넌트 10(`PanelHost`+Provider·`ConfirmDialog`·`IdentityConfirmDialog`·`ReasonPromptDialog`·`BusyButton`·`UndoControl`·`StatTile`·`SelectableList`+`PickBar`+`useSelection`, 딥틸 토큰만, 캘린더 PICK 제외). 프론트 134/0. **판단**: `test_error_messages` 가드 `==6`→`>=6`(undo_status가 7번째 정당 마스킹, 개수 아니라 원문노출 회귀가 목적). **코디 배선 TODO**: PanelProvider/PanelHost 셸 마운트·undo HTTP 엔드포인트(응답에 서버판정 `reason_required` 실어 `UndoControl.requiresReason` 채움). ⚠️ **전체 회귀서 `test_doctor_cannot_invite_staff_via_api` 1건 실패는 공유 DB 순서 오염**(격리 PASS) — 클린 DB 회귀 때 재확인.
- **Task 6b `151546c`**: 가족연결 저장(00045, link/unlink + POST/DELETE /patients/{id}/family, 의사 403, 재연결 허용). Task 6 완결. focused 6/0.
- **Task 13 `f666fba`**: 조회 백엔드(`core/pagination.py` 공용커서·`core/dto.py` 마스킹경계·dashboard/history/stats 서비스·라우터 2). focused 54/0. **BLOCKED 이월**: ①`/calendar`는 Task 17 `resolve_day`(빗금 유일판정기) 없어 인터페이스+`NotImplementedError`만, 라우트 미등록 → **Task 17 후 배선** ②`ALOG-LIST-13` 상세 payload는 `access_audit_log`에 payload 칸 없어 미룸(마이그 필요) → **Task 15에서 처리** ③리스케줄 영향분은 calendar와 묶여 미룸. **코디 배선**: main.py에 dashboard.router·stats.router 등록.
- **모델정책 개정(2026-08-26)**: 노력 전부 high. **병렬 축소 → 앞으로 한 번에 하나씩**(중간 한도컷 시 콜드 재읽기 낭비 회피, 사용자 논의). 200k/1M은 비용차 미미(태스크가 200k 미만)라 유지.
- **Task 25 `d53cfdc`**: long_wait 지표(백엔드 절반, 프론트=Task12와). **Task 17 `3c988b4`**: `resolve_day` 단일 판정기(의사예외>병원휴무>요일규칙, source)·`00041 hospital_hours_closures`·schedule_admin_service(save_week_rules 원자·overview_grid·진료과 CRUD, 진료과중지는 활성의사 있으면 막고 /admin/staff)·slot_generator·department is_active. `core/errors.py` 공유변경(AppError detail, 하위호환). **전체 회귀 349/0.**
- **✅ Wave B 백엔드 완료**(4·5·6·6b·7·13·25·17) + **✅ 코디 통합 배선 완료** + **✅ 독립검증 PASS**(fresh Opus 4.8 `d081f171`, 계약·규칙 위반 0건) + **✅ 클린DB 회귀 PASS**(백엔드 358/0·프론트 141/0). **다음 = Wave C(화면 15개, 하나씩)**.
  - 검증 지적(비기능, 처리): ①`get_calendar` docstring 「BLOCKED」 낡음 → **역참조로 해소**(커밋). ②`affected_appointment_ids`가 `[from,to]` 창 밖 id 포함 가능 — 무해(프론트가 그리는 막대만 ⚠ 표시), 최적화 여지만 → HAND-REVIEW 이월.
- **Task 8 `/today` 화면 `51fa001`**(시각): TODAY-* 규칙 화면 + 라우터 배선(App.tsx `/today`→TodayPage). 프론트 17/0·tsc0. **⚠️ 백엔드 갭 = Task 13b 필요**: `get_today_summary`가 tiles·long_wait·needs_attention만 제공 → **`TODAY-NOSHOW`(미접수·시각지남)·`TODAY-YDAY`(전일 미완료, #37 자정경계)·`TODAY-DOC-01`(의사별 대기)** 데이터 미제공으로 미구현(스펙 `ui-design-decisions.md:3602`이 TODAY-DOC-01 「요약API에 추가 vs /queue서 프론트 셈」 **미결**로 기록). `TODAY-RESCHED`(리스케줄 영향)·`TODAY-LIVE`(realtime)는 **Task 14(calendar/realtime)와 함께**. → **13b: get_today_summary에 NOSHOW/YDAY/DOC 목록 추가**(DOC-01 기본=요약API 단일소스) 후 /today 카드 완성.
- **✅ Task 9 `/queue` `b5db19e`**(시각+로직): 대기목록 7탭·순번 필터이전(ORDER-03)·도착처리·순서변경(사유필수)·긴급 + **당일 방문등록(갭#85 해소)**: `00038_walkin_visit_time`(공유DB up 적용) + `create_appointment(walkin_visit_time)` 미래시각 서버가드(WALK-16) + `WalkinVisitTimePicker` 위젯. 백엔드 46/0·프론트 180/0·tsc0. **이월 3건**: ①**워크인 패널 전체 조립**은 Task 6 환자검색 **프론트**(`searchPatients` 스텁)+PanelHost 배선 후(=Task 24/공통패널) ②**`QUEUE-ARRIVE-02` [진료대기] 원클릭**: `VALID_TRANSITIONS`에 `예약확정→진료대기` 직행 없어 **도착→진료대기 2단 체이닝**으로 구현(직행 원하면 전이표 마이그 필요, 임의 스키마 안 만듦) ③`WALK-08` 다음자리=갭#87·DnD 드래그물리=시각폴리시 이월. ⚠️ **00038은 공유DB엔 적용됐으나 클린DB 회귀(reset) 미검증** — 다음 체크포인트에서.
- **✅ Task 13b `873af07`**: `get_today_summary` +3필드(not_arrived/yesterday_unfinished/doctor_waiting) + /today 카드 완성. 백엔드 24/0·프론트 22/0·tsc0. **/today 완성**(단 `TODAY-RESCHED`·`TODAY-LIVE`는 Task 14 realtime과 함께). ⚠️ **미접수/전일미완료 행 action은 네비게이션 배선**(도착·원문공개 mutation 엔드포인트 부재) → **Task 9 `/queue`가 `?tab=not_arrived&action=…` 인라인 도착/접수 mutation 완성**. ⚠️ 이 과정에서 **Max 한도컷 1회**(7:30pm 밴쿠버 리셋 후 재개 성공).
- **코디 통합 배선 목록**(main.py·셸 공유파일, 코디가): ①main.py 라우터 4 등록: `patients`(T6)·`dashboard`·`stats`(T13)·`schedule_admin`(T17) ②`resolve_day` 소비처 연결: T13 `/calendar`(NotImplementedError 해제)·`appointment_service` 예약생성 ③셸: `ConnectivityProvider`+`OfflineBanner` 마운트·`PanelProvider`/`PanelHost` 마운트·401 세션만료(`isSessionExpiry`→`rememberReturn`→logout)·`markServerOk` ④undo HTTP 엔드포인트(`undo_status` 호출, 응답에 `reason_required`).
- **✅ 코디 통합 배선 완료(epoch2, 배선 워커 `54a09667`)** — 커밋 예정. ①main.py 라우터 4 등록 ②(a) `get_calendar` 구현(`resolve_day` 유일판정기로 예약막대+빗금(점심·휴진)+⚠affected id 조립, `dashboard.py`에 `GET /calendar`) ③셸: `main.tsx`에 `ConnectivityProvider`·`AppShell`에 `PanelProvider`+`OfflineBanner`+`PanelHost`+`ServerEffects`(`serverEffects.ts` 신설: 성공→`markServerOk`/온라인401→`isSessionExpiry`→`rememberReturn`→`logout`) ④`POST /appointments/{id}/undo`(서버판정 `reason_required`, 사유 없으면 막지 않고 `executed:false,reason_required:true` 반환=막다른 길 금지). 백엔드 43/0·프론트 35/0.
  - **②(b) `appointment_service` 예약검증 = Task 14로 이월**(임의 구현 안 함): `SCHED-SLOT-11`이 요구하는 `resolve_day` 예약가드는 **격자 밖 5분 단위 슬롯 예약(`CAL-TIME-09`)** 대상인데, 현재 `create_appointment`는 시각을 **slot_id로만** 받는다(slot 경로는 slot_generator가 이미 `resolve_day`로 보호=암묵). 오프그리드 예약 경로 자체가 없어 지금 붙일 소비처가 없다 → **Task 14(/calendar 5분 예약)에서 오프그리드 예약칸과 함께 `resolve_day` 가드 추가.** 워크인(slotless)은 `QUEUE-WALK-18`=갭 #85(시각 칸 없음)라 별개이고, "휴진 의사 워크인 차단" 규칙은 원문에 없음(default=차단 안 함, 상태 유지).
- **✅ Task 10 `/patients/:id` `7db6cf3`(epoch4 시각)**: 환자 상세 — 헤더 요약 + 2열 섹션 그리드(㉰안, 결정 #1), 섹션별 독립 쿼리(`useQuery`/`useInfiniteQuery`/`useQueries`)로 한 섹션 실패해도 나머지 유지(PTDET-LOAD-02). `PTDET-QNR-03`은 화면분기 + **관리자·접수직원 role일 때 문진 요청 자체를 안 보냄**(`questionnaireHit===false` 검증, AD-050/결정 #14). 신규 10파일+`format.ts`(공용 표시헬퍼·SectionState, 순환참조 회피) + 테스트 4종, `api/patients.ts` 상세 fetch 추가(경로 전부 백엔드 실측 일치: dashboard 라우터 prefix 없음, `/patients/{id}/visits·medical-records·family·notes`·`/appointments/{id}/questionnaire`). **App.tsx `/patients/:id` 배선=코디**(Placeholder→PatientDetailPage, RequireRole 역할셋 유지). 프론트 22/0·tsc0. **이월/BLOCKED**(서버 계약 부재, 프론트 흐름만 완성): `HEAD-05` sms_dead(상세응답에 필드 없음, 갭#123 — `sms_failed_at` 오면 표식) · `VISIT-03·07` 서버 정렬·페이징 동점키(Task 13) · `FAMILY-03·04·05` OTP·예외 서버판정(Task 13) · `SUPPORT-*` 전체(support_tickets=4단계 상담봇) · `ACTION-02` OTP/Auth API(갭 #19, `onRequestCode`/`onConfirm`이 501 거절). **판단**: 라우트 역할가드는 기존 `[receptionist,doctor,admin]` 유지 — `PTDET-ACTION-06`(의사/비활성 403+복귀경로)은 컴포넌트가 API 403을 받아 자체 처리(테스트가 그렇게 검증), 라우트에서 doctor 배제 시 QNR-03(담당의사 문진열람)과 충돌하므로 변경 안 함. ⚠️ VisitSection/RecordSection의 **진료과·담당의사**는 `patient_history_service` 방문응답이 아직 미포함 → 필드 오면 그리도록만 대기.
- **✅ Task 11 `/doctor/console` `46c1f7e`(epoch4 시각·xhigh)**: 의사 진료 콘솔 — 3단 패널(큐·컨텍스트/문진/이력·진료기록) + 브라우저 초안보관(`DOCTOR-DRAFT-01~03`)·**3초/30초 자동저장**(`DOCTOR-RECORD-04`)·상용구 칩·관리(`AD-064`)·열폭 조절(`AD-062`). **환자 행 여는 것 자체가 `진료대기→진료중` 전이**(`DOCTOR-START-01~02`, 버튼 없음). 문진 담당의사만(`ROLE-DOC-01`). 내부메모=`/patients/:id`와 같은 저장소(`AD-063`, NoteSection 재사용). 신규 21파일(소스14+테스트7, `useDraftStore`/`useAutoSaveDraft`/`ColumnResizer`/`api/doctorConsole.ts` 포함). **코디 배선**: App.tsx `/doctor/console`(pageFor) + 딥링크 `/:appointmentId`(결정 #9) + `navItems.normalizeNavPath`에 `/doctor/console/` 접기(사이드바 강조·가드 일관). 프론트 **전체 251/0**·tsc0. **⚠️ 후속 배선 2건(BLOCKED, 통로만 만듦)**: ~~①**START-01 자동전이 서버계약 갭** — `get_doctor_queue` 응답에 `updated_at` 없어 `PATCH /appointments/{id}/status`의 `expected_updated_at` 못 채움→실서버 422. **큐 응답에 `updated_at` 추가 필요**(Task 13 dashboard 후속·작은 백엔드 수정)~~ ✅ **해소(2026-08-29, 커밋 `12ca74e`, S6 데모정렬)** — `get_doctor_queue` SELECT·row에 `a.updated_at` 추가. 클릭→진료중 전이 성공, 3단 실채움 shot 검증** ②**DRAFT-03 ②③ 로그아웃/자동로그아웃 시 초안삭제** — 워커가 `clearConsoleDraftsOnSignOut` 통로만 export, 셸 `signOut`/`useIdleLogout`에 호출 배선은 코디 후속(①완료 즉시 삭제는 동작). ⚠️ cwd에 무관 개인문서 `복습_전략.md`(미추적) 존재 — 손대지 않음.
- **✅ Task 12 `/admin/stats` `e37130c`(epoch4 시각·xhigh)**: 운영 통계 — 기간(공용 `PERIOD-*` `PeriodPicker` 자기완결, 이후 화면 재사용)·지표 카드·차원별 표·시간대 방문·유입원·챗봇 지표·드릴다운 모달·CSV. **개인정보 억제 핵심**: `exportCsv.ts`의 `suppressForExport(rows,k=5)`(직접+보완추론 차단)를 **CSV 경로에만** 연결, 화면 렌더는 공개 집계 그대로(1건 셀도 표시·클릭, `STAT-MASK-01` 두 경로 각각 테스트, 결정 21). 드릴다운 명단=서버 마스킹, 행→`/patients/:id`(결정 24). **드릴다운 감사는 서버가 `/stats/detail` 안에서 남겨 이중기록 방지**, CSV만 프론트 `POST /audit/stats`(payload PII 없음, 결정 22). 신규 14파일(`api/stats.ts` 포함). **코디 배선**: App.tsx `/admin/stats`(pageFor). 프론트 29/0·tsc0. **넘김**: `STAT-SHELL-01·02`(nav·역할게이트)=`RequireRole`+`AppShell` 책임(배선으로 적용) · `STAT-METRIC-02·07`(표시명 DTO·유효전이 재집계)=Task 13 소관, 프론트 계약만 · `STAT-METRIC-04` 오래대기 상세목록=계약 미존재로 버튼 미표시(플랜대로). BLOCKED 없음.
- **✅ Task 14 `/calendar` 완료(epoch4, 최대 태스크 — 순차 4분할)**: `14a` 백엔드 `3c70e63`(마이그 00039: start_at/end_at 신설[00038 아님·브리핑정정]·allow_overlap 사실기록·GiST 배제제약 WHERE NOT allow_overlap·같은시각 unique·realtime publication 3테이블·취소계열 제외 + `create_phone_appointment` 과거/5분스냅/닫힌시간 resolve_day 가드=400·겹침/같은시각=409·end_at=slot_duration + `POST /appointments/phone` 배선. ⚠️00038도 미적용이라 `up --include-all`) · `14b-1` 기반유닛 `81a53cf`(snap·useZoom·useCalendarRealtime·SlotBlock·TimeAxis·DoctorChips·CalendarNav+api, 46/0) · `14c` 카탈로그 `9d52df3`(get_calendar에 doctors={id,name,department_name,palette_index=null}, staff.department_id 존재확인, palette=Task19 00042가 채움) · `14b-2` 격자조립 `4c80b2b`(CalendarPage·DayGrid/WeekGrid[주간=DayGrid재사용]·MiniCalendar·PhoneBookingPanel[환자찾기 자체구축=WalkInPanel 부재]·AppointmentPanel·GapWarningDialog+gridModel/layout, doctors 소비·palette null→i%10 폴백, App.tsx CalendarRoute 래퍼 배선). 프론트 전체 385/0·tsc0·백엔드 14/0+7/0. **⚠️ 이월(contract/jsdom BLOCKED, 구체적)**: ①`CAL-VIEW-04`(한명선택 시 생년월일·초진/재진) — **캘린더 막대 DTO에 birth_date·visit_type 없음** → 후속 백엔드 DTO 추가 필요 ②`CAL-NAME-02`「N분」·`CAL-TIME-05` 예약 미리보기 종료시각 — **doctors 카탈로그에 slot_duration_minutes 없음**(막대길이서 기본15 도출) → 카탈로그에 slot 추가 시 정확 ③`CAL-COLOR-15`(팔레트 ink ΔE≥20) — Task 0 tokens.css 검증 소관 ④`CAL-TIME-05` 호버 px·`CAL-ZOOM` 드래그 px — jsdom rect=0(로직은 14b-1 유닛검증 완료) ⑤`NAV-STFSUP-08·14` 왕복복원 — 티켓모델=ai-chatbot 플랜. **⚠️ 00038·00039 적용됐으나 클린DB 전체회귀 미검증**(다음 체크포인트, 사용자 신호 후 reset).
- **✅ Task 15 `/admin/access-logs` 완료(epoch4, 백엔드/프론트 2분할)**: `15a` 백엔드 `569a7a8`(마이그 00040 안정정렬 인덱스 `(accessed_at desc, id desc)` B-19 · `audit_query_service.list_access_logs`[관리자만·patient_row_dto 마스킹·from포함/to제외·커서200] · `GET /admin/access-logs`+main.py 등록 · `paginate` page_size 인자 · 검색적재는 patient_service가 이미 `log_access` 호출[중복 안 함] · **ALOG-LIST-13 payload 불필요 판정**, 10/0) · `15b` 프론트 `70735a7`(AccessLogPage·LogFilterBar[PeriodPicker 재사용]·LogTable·LogKindBadge·BulkRevealRow+logRows[대량열람 한 줄 접기→개별 ALOG-GROUP-01·02] + PeriodPicker `applyLabel` 하위호환 prop, App.tsx 배선, 프론트 전체 415/0·tsc0). **계약**: `GET /admin/access-logs?patient_id&from&to&cursor`→`{rows[{id,accessed_at,resource_type,search_term,staff_name,patient{masked_*}|null}],next_cursor,total_hint}`. ⚠️ **BLOCKED(적재쪽 미완, 표시층 준비됨)**: `ALOG-LIST-12` 사유(reason 필드=Task 21) · `ALOG-AUDIT-02`/`GROUP-03` 저장 전수·미열람 무기록(쓰기=Task 6·28). ⚠️ 코디: BulkRevealRow 3000행 테스트 timeout 15s로 상향(jsdom 렌더속도, 로직 아님). ⚠️ 00040 로컬 --include-all 적용·원격 미적용.
- **✅ Task 18 `/admin/schedule` 완료(epoch4 시각·`2e9c9dd`)**: 진료 일정 관리 — 왼쪽 세로줄4(전체현황·진료과·의사별스케줄·특정날짜, `SCHED-TAB-01`)+오른쪽. OverviewGrid(행=의사·열=요일7)·DoctorWeekTable(7행 상시 비교·6칸)·DepartmentList(⛔삭제없음·활성의사 있으면 중지막고 `[직원 관리로 가기]` SCHED-DEPT-03/05)·DateExceptionPanel·HospitalHoursTable·**useDirtyMap(의사별 미저장 분리 갭#106)**·DirtyDot(세층 공용 `●`). 신규 19파일+`api/scheduleAdmin.ts`. **코디 배선**: App.tsx `/admin/schedule`(pageFor). 프론트 전체 474/0·tsc0. ⚠️ 워커 진행 중 일시적 timeout 1회→같은 워커 `send_agent_prompt` 재개(손실 없음). 토큰 `--color-warn`(규칙/플랜의 `--color-warning`은 낡음). **⚠️ BLOCKED/이월(구체적)**: ①`SCHED-WARN-01~11` 다이얼로그 본체 — **Task 2 `AffectedAppointmentsDialog`가 저장소에 없음** → `ConfirmDialog`로 로컬 조립(건수·자리 미리보기), Task 2 생기면 교체 ②**백엔드 조회 GET 부재** — Task 17이 hours/closures/date-exceptions에 PUT/POST만 있고 **GET 없음** → `scheduleAdmin.getHours/listClosures/listDateExceptions` 얇게 뚫어 prop 구동(테스트 GREEN), **라이브 배선 시 이 GET 엔드포인트 백엔드 추가 필요**(부제목 「다음 휴무·평일 운영시간」도 의존) ③`SCHED-HOURS-15·16` 결합팝업=Task 2 의존 ④`SCHED-EXC-16·18`·`NAVX-STAFF-02`=Task 29(`/admin/settings`).
- **✅ Task 19a 백엔드 완료(epoch4·`699c883`)**: `/admin/staff` 백엔드 절반 — 마이그 `00042_staff_profile_palette`(staff에 `specialty·bio·photo_url·calendar_color_index` + `doctor-photos` 버킷[공개읽기/관리자쓰기 `private.is_admin()`]·색 unique 없음[CAL-COLOR-07 막다른길 방지]·0~9 range·의사전용 check). `staff_service`: list_staff 안정정렬(id 동점키 STAFF-LIST-02)·auth.users **1회 조회**로 last_sign_in_at/invited_at 합성(LIST-07·09)·invite_staff 의사 색 0번부터 자동배정/다차면 최소사용번호(CAL-COLOR-03·07·13)·비의사 색없음(08)·deactivate_staff `impact_version` 불일치 409 + 신규 `get_deactivation_impact`(건수·시각·version[영향예약ID집합 해시]). `staff_profile.py` 신설(update_doctor_profile·upload/delete photo, 의사 서버선검사 STAFF-PROFILE-02). `staff.py`: `PATCH /{id}/profile`·`POST|DELETE /{id}/photo`·`GET /{id}/deactivation-impact`·`PATCH /{id}/deactivate` 본문 `{impact_version}`. focused **30/0**. **⚠️ 확정/이월**: ①`schedule_change`의 `deactivating_doctor_id` 원인은 **이미 완전 구현·테스트됨**(test_schedule_change_service.py:208~223) → 무수정 검증만(플랜 Files의 "수정"은 이미 된 것) ②`requirements.txt`에 `python-multipart==0.0.32` 추가 → **배포/타 환경 `pip install -r` 재실행 필요** ③마이그 `up --include-all` 적용(00045가 먼저 적용돼 out-of-order, CLI 권장 플래그·db reset 안 함) ④**00042 클린DB 회귀 미검증**(00038·00039와 함께 다음 체크포인트, 사용자 신호 후 reset). **다음 = Task 19b 프론트**(api/staff.ts + StaffAdminPage·StaffList·InviteForm·DoctorProfilePanel·PalettePicker·DeactivateDialog, 데모 `staff/admin/config/StaffAdmin.tsx`, 위 계약 소비) → 완료 후 코디가 App.tsx `/admin/staff` 배선.
- **✅ Task 19b `/admin/staff` 프론트 + 배선 완료(epoch4·`bab7266`)**: StaffAdminPage(2칸 그릇, 오른쪽 단일상태 `{kind:invite}|{kind:profile,staffId}`·초대폼 언마운트 않고 숨김 PROFILE-10)·StaffList(필터 **클라이언트측만** LIST-04, 건수 유지)·InviteForm·DoctorProfilePanel·PalettePicker(재사용·인덱스참조 CAL-COLOR-09·**사용중 색도 선택 가능** 07)·DeactivateDialog(건수·시각만 이름/전화 없음 DEACT-04·0건이면 정책안내 안열고 일반확인 05·정책선택/잠금 없음[결정10 A안] 06·409 그자리 사람문장[막다른길 금지] 03·09 [다시 확인] 최신재조회·갈길 /today NAVX-STAFF-01·확정이 환자알림 안부름 08)·사진삭제→회색원+첫글자 07. 비의사엔 [프로필]·색 **아예 안그림**(회색비활성 아님 SHELL-ACT-03)·[다시 사용] 없음(G-04). `staffFormat.ts`(시각어휘)·`testUtils.tsx`. **코디 배선**: App.tsx `pageFor`에 `/admin/staff`→StaffAdminPage(NAV_ITEMS ADMIN_ONLY 기존이라 route·guard·사이드바 하이라이트 자동). **73/73·전체 547/547·tsc0**(배선 후 코디 재확인 73/73·tsc0). **⚠️ 데모차이(규칙이 이김)**: 진료과 조회 실물경로=`/admin/departments?include_inactive=false`(schedule_admin 라우터, 브리프 `/departments` 아님). **⚠️ 이월(구체)**: ①`STAFF-PROFILE-09` **사이드바-이동 이탈가드** — 화면내 두 경로(다른의사줄·[닫기])는 `guardLeave` 한 함수로 덮음, 그러나 사이드바 클릭 이동은 **라우터레벨 차단 필요**한데 App.tsx가 `<Routes>`(data router 아님)라 `useBlocker` 불가 → **data router 전환은 전 라우트 영향 리팩터라 미루고 후속 처리**(되돌릴 수 있는 UX, 진행 안막음) ②CAL-COLOR-04·05·14·15=Task14(칠하는쪽)·Task0(토큰) ③LIST-09·PROFILE-13·CAL-COLOR-03·10·13=19a 백엔드 소관(소비만). **Task 19 완결**(19a 백엔드+19b 프론트). **다음 = Task 20 `/checkin`**(CHKIN-*, 마이그 00043 fix_booking_code_length 갭#127 6자리버그).
- **✅ Task 20a `/checkin` 백엔드 완료(epoch4·`642d5ea`)**: ①마이그 `00043_fix_booking_code_length` — `generate_booking_code()` `round→floor`(갭 #127: `(random()*32)::int` 반올림이 33번째 글자를 찾아 빈문자열 → 6자리 미달 8.7%[5자리 8.3%·4자리 0.4%], `CHKIN-CODE-04`가 그 멀쩡한 번호를 거절하던 막다른길) + 속성테스트 2건(길이집합={6}·32글자 고르게 min>max*0.7). ②`find_by_booking_code(code,staff,conn=None) -> BookingLookupResult|None`(appointment_id·patient_name·slot_at·department_name·doctor_name·status·**updated_at**[도착처리 낙관잠금열쇠]) — **만료(`booking_code_expires_at<=now()`)·미존재·종료상태 불구분 None**(개인정보 열거방지). ⛔ **phone/birth_date 아예 안담음**(먼저정한것#1·MASK-SRV-01) ⛔ **access_audit_log 안남김**(먼저정한것#2·ROLE-READ-02, 접수조회는 감사 3길목[검색실행·상세진입·번호펼치기] 아님) ⛔ 도착처리 API 신설 안함(기존 `PATCH /appointments/{id}/status` 사용)·만료정리배치 안만듦. ③`GET /appointments/find-by-code?code=`→`{appointment:{...}|null}` 역할가드 접수직원·관리자. focused **9/0**·회귀 20/0(코디 재확인 9/0). **⚠️ 판단/이월**: ①`find_by_booking_code`에 `conn=None` 추가(플랜 서명엔 없음, 파일 공통 idiom — 서비스는 `acquire_as` 별도 커넥션이라 롤백 트랜잭션 테스트가 별도풀 데이터 못봄. **HTTP경로[20b 소비]는 conn없이 호출**하므로 계약 무영향) ②플랜 Step4 라우터테스트 `(db_conn,client)` 서명 성립불가라 403은 `committed_conn`에 doctor 커밋해 검증 ③마이그 `up --include-all`(00045 먼저 적용됨·db reset 안함) ④00043 클린DB 회귀 미검증(00038·00039·00042와 함께 다음 체크포인트). **다음 = Task 20b 프론트**(CheckInPage·QrScanner·BookingCodeField·LookupResultCard, plan Step 9~16, 위 계약 소비) → 완료 후 코디가 navItems.ts(`QR·예약번호 접수` 접수직원·관리자)+App.tsx `/checkin` 배선.
- **✅ Task 20b `/checkin` 프론트 + 배선 완료(epoch4·`1d7c2d5`)**: CheckInPage·QrScanner(html5-qrcode 프로덕션 의존성, 카메라 주입경계로 테스트)·BookingCodeField·LookupResultCard + `api/appointments.ts findByCode` 추가. **결과 카드에서 도착처리 완료**(화면 안 떠남, 결정#5·#6). **⭐ 규칙 워크로 정본 개정 3건 반영**(plan 2026-08-15은 낡음): ①`CHKIN-RESULT-01` 단일 `[도착 처리]`→**두 버튼 `[진료 대기]`·`[도착]` 하이브리드**(2026-08-24 QUEUE-ARRIVE 두 갈래, QueuePage와 동일방식·전이표 예약확정→도착→진료대기 체이닝) ②`/queue?highlight=`→**`?appointment=`**(QueuePage:45 실제 키) + `?tab=arrived|waiting|not_arrived`(막힌길 방지) ③CODE-07 [대기 목록으로]→`?tab=not_arrived`. 카드에 전화/생년월일 없음(서버가 안보냄)·access_audit 안남김·만료/미존재/종료 불구분(열거방지). **27/0·셸 58/0·tsc0**. **⭐⭐ 코디 배선 중대 발견·해소(원문확인)**: 워커·plan·`CHKIN-HEAD-01/02`는 「사이드바에 `QR·예약번호 접수` 항목」이라 했으나 — **`SHELL-NAV-01/02`(✅개정 2026-08-24)가 업무 그룹을 정확히 6개로 못박고 ⛔ "접수는 사이드바에 두지 않는다(헤더 세 문 `SHELL-DOOR-04`로 이동)"** 라고 명시. 처음에 navItems에 넣었다가 **Sidebar 테스트(SHELL-NAV-02 6개)가 올바르게 잡음** → **navItems·qr아이콘 되돌리고 App.tsx `/checkin` route만 유지**(정본 route #2). `CHKIN-HEAD-02`(옛 「사이드바 항목」)에 **역참조 박음**(screen-behaviors.md:1740). **진입은 헤더 `[＋ 접수]` 문**(현재 AppShell 문 패널은 placeholder=공통 패널 태스크에서 CheckInPage 연결)·route는 URL로 도달 가능. **⚠️ 이월**: ①`CHKIN-HEAD-01/02/03`·`CODE-06`·`LOAD-02/03`=셸 소유(AppShell 제목·세 문·오프라인띠·RequireRole 차단·401 ServerEffects) — 통합에서 성립, 조회·전이를 react-query에 태워 셸 중앙처리 전제 지킴 ②접수 문 패널↔CheckInPage 연결=**공통 패널 태스크**(SHELL-DOOR-04) ③QR 카메라 실물=jsdom 불가, 수동검증 몫 ④00043 클린DB 회귀 미검증(누적). **Task 20 완결**(20a 백엔드 `642d5ea` + 20b 프론트 `1d7c2d5`). **다음 = Task 21 `/admin/patient-merge-candidates`**(MERGE-*, 마이그 00044 patient_merges, **비가역·파괴적 — 코디 리뷰 꼼꼼히**).
- **✅ Task 21a 백엔드 완료(epoch4·`2c9d34f`, 비가역 태스크·코디 꼼꼼검토)**: 중복 환자 병합 원장·계보·감사. ⭐ **원본 보존(결정 #15) 검증됨** — 코디가 서비스 grep으로 예약·문진·진료기록 이동 UPDATE/DELETE **없음** 확인, 병합 UPDATE는 `auth_user_id` 이동·`is_active=false`뿐. `00044`: `patient_merges`(원장=계보 단일표: primary/merged_patient_id·performed_by/at·counts_snapshot·undone_at/by/reason + CHECK 3필드일관성)·**부분 unique 2종**(`live_pair`·`live_merged` where undone_at is null → 계보 분기 방지·되돌리면 재병합 가능)·`patient_lineage(uuid)->uuid[]` 재귀(undone 제외)·`access_audit_log.resource_id` FK→patient_merges·RLS admin. `patient_merge_service`: `list_merge_candidates`(마스킹·counts_snapshot)·`merge_patients(primary,duplicate,staff,expected)`(한 트랜잭션·낙관잠금·**MERGE-STATE-04 두 행 모두 auth_user_id면 409**). `GET/POST /admin/merge-candidates`(admin)+main.py. focused **29/0**·전체 460/0(재확인 29/0). ⛔ **undo API 안만듦**(Task26 소유, 스키마 `undone_at`만). **⚠️ 이월/판단**: ①**`patients.auth_user_id` 신설**(플랜 Step6이 쓰나 스키마에 없었음=3단계 미구현, nullable unique·auth.users FK 없이 순수 uuid) → **환자앱 3단계에서 로그인 모델·FK와 맞출 때 00044와 중복 add column 충돌 없게 조정 필요** ②감사 거절 사유 payload=칸 부재로 성공=resource_id=merge.id·거절=null로만 구분(풍부한 사유는 별도 컬럼 마이그 이월) ③계약: `CandidateGroup={key,rows:[{patient_id,name,masked_birth_date,masked_phone,account_linked,is_primary:null,counts,last_visit_at}]}`(21b는 마스킹만·원본 전화/생일/UUID 못받음)·POST body `{primary_id,duplicate_id,expected_counts}`·읽음체크는 서버가 안받음(MERGE-CONFIRM-04) ④00044 클린DB 회귀 미검증(누적). **다음 = Task 21b 프론트**(MergeCandidatesPage·CandidateGroup·ComparePanel·MergeConfirmDialog, plan Step 9~15. **⚠️ 비가역: 빨간버튼 확인창 안만·3단계 확인+읽음체크[결정#18]·되돌림 여기 없음→Task26 갈길만[MERGE-UNDO-01·02]·막다른길 금지**) → 완료 후 코디 App.tsx `/admin/patient-merge-candidates` 배선(navItems엔 기존 「중복 환자」 기록그룹).
- **✅ Task 21b 프론트 + 배선 완료(epoch4·`2320087`, 비가역·코디 안전검증)**: `/admin/patient-merge-candidates` — MergeCandidatesPage(3단계 상태기계 목록→비교→확인창→성공)·CandidateGroup·ComparePanel·MergeConfirmDialog + `api/patientMerge.ts`. **⚠️ 비가역 안전 코디가 grep 검증**: ①빨간 [병합 확정]은 `MergeConfirmDialog`(확인창)에만·본문 danger 0개 ②되돌리기 버튼 이 화면 없음(MERGE-UNDO-01) + 성공 후 "병합 이력 화면에서 되돌릴 수 있습니다" 갈길(막다른길 없음, MERGE-UNDO-02) ③3단계 게이트+읽음체크(결정#18) ④서버 마스킹만. 낙관잠금 409[다시확인]·STATE-04 이중계정 409 사람문장. **29/0·셸 60/0·tsc0**(NAV 회귀 없음=merge-candidates가 navItems에 기존). 코디: App.tsx route(admin 가드 navItems ADMIN_ONLY 자동). **⚠️ 이월**: DATA-01~03 계보조회=백엔드/Task13(문구만 노출). **Task 21 완결**(21a `2c9d34f`+21b `2320087`). ⭐ **Task 26(merge-history) 의존 해소**(patient_merges·undone_at 준비됨, undo API는 26이 만듦). **다음 = Task 22 `/admin/questionnaires`**(QADM-*, 마이그 00046 questionnaire_versions, 불변 버전).
- **✅ Task 22a 백엔드 완료(epoch4·`fd3e49d`)**: 문진표 **불변 버전**(결정 12). `00046`: **00008 `unique(department_id)` DROP**(이유 주석=결정12가 R5-09 뒤집음, 안 적으면 upsert 회귀) + 버전칸(version_no·is_active·created_at·created_by) + **부분 unique 활성 하나**(one_active_per_dept) + **불변/삭제금지 트리거**(questions·version_no·created_at UPDATE·DELETE 차단) + `save_questionnaire_version` 원자함수. service 4함수(list_departments_with_status·get_department_form·save_version·get_version) + `QUESTION_TYPES('short_text','long_text','yes_no')`·`SHOW_TO('all','female','male')`(**3단계 환자앱 공유**) + next_question_id. 라우터 4개 GET/POST(admin) — ⛔ **PUT·DELETE 없음**(AD-065/066 삭제·이름·환자답변 안만듦, DB·서비스·라우터 **3층 방어** 코디 검증). 기존 dedup/schema 테스트 2개를 불변모델로 재작성(**역참조 주석** ~~옛 upsert/dedup~~ ✅뒤집힘 결정12, 껍데기 아님=unique제거·delete차단 검증). focused **27/0**·전체 481/0. **⚠️ 계약(22b 소비)**: `save_version`/`get_version`→`{id,department_id,version_no,is_active,created_at,created_by_name,questions[]}` · questions=`[{id,text,type∈QUESTION_TYPES,required:bool,show_to∈SHOW_TO}]` · `get_department_form`→`{department_id,department_name,active_version:{id,version_no,questions}|null,versions:[{id,version_no,is_active,created_at,created_by_name,question_count}]최신순}` · `list_departments_with_status`→`[{id,name,active_version:int|null,question_count}]`(이름asc) · POST body `{questions[],base_version_id:uuid|null}`·201·첫저장 base=null·409 낙관충돌("최신 불러오기"). **⚠️ 이월**: 델타 3건(version_no default 1·FK on delete set null·created_by null허용) 파일+실DB 동기(클린DB reset 재현됨)·00046 클린DB 회귀 미검증(누적). **다음 = Task 22b 프론트**(QuestionnaireAdminPage·DepartmentList·QuestionEditor·VersionHistory·SaveConfirmDialog, plan Step 11~15, 목업 81=3칸 그리드) → 코디 App.tsx `/admin/questionnaires` 배선(navItems엔 기존 설정그룹).
- **✅ Task 26 완결(epoch5·26a `a5abd47` + 26b `6cda41c`, 비가역·파괴적 태스크·코디 정밀검증)**: `/admin/merge-history` 병합 이력·되돌림. **26a 백엔드**: `undo_merge`(결정15~17) — `for update` 행잠금→이미되돌림 409(중복감사 없음)→락 판정→`undone_at/by/reason` 채우고 `patient_merge_undo` 감사를 **같은 트랜잭션**에 직접 insert(`log_access`는 `resource_id` 인자 없어 못 씀, Task21 `_log_merge` 미러) + `get_merge_history`/`get_merge_event`(admin·paginate·마스킹 DTO·보존 스냅샷). **새 마이그레이션 없음**(00044에 `undone_*`·`resource_id` 이미 존재). 라우터 3개(main.py 등록은 후속 배선). **⭐ 플랜↔코드 드리프트 정정**: ①타임스탬프 칸=`performed_at`(플랜 `merged_at`은 틀림, 출력 DTO 필드명만 merged_at) ②`medical_records`엔 `patient_id` 없음(00006) → 락 판정은 `appointments.for_patient_id` 조인(Task21 `_counts_for` 관례 일치) ③`Forbidden`/`ValidationError` 클래스 부재 → `AppError(403/400)`. focused 9/0·Task21 회귀 16/0=**25 passed**(코디 venv 재실행 확인). **26b 프론트**: MergeHistoryPage·MergeEventDetail(사유→확인창→완료 상태기계)·UndoReasonStep·UndoConfirmDialog(읽음 체크 후 danger·중복클릭 차단·409/401)·LockedEventPanel(대상환자·감사메모, 막다른길 금지) + `api/mergeHistory.ts`. vitest 17·전체 **676/676**·tsc0·plan-coverage MHIST 0미반영. **⭐ 코디 정정**: 워커가 권한거부를 커스텀 `AdminDenied`("관리자만 볼 수 있습니다"/"대기 목록으로")로 만들었으나 RequireRole이 nav 경로에서 admin 강제라 **실앱 목록 라우트 도달 불가** → 규칙 MHIST-SHELL-02/EXC-01은 특정 문구 안 정하고 형제 MergeCandidatesPage는 페이지가 `<RequireRole roles={ADMIN_ONLY}>` 자기감싸 표준 문구를 씀 → 두 페이지 그 관례로 정렬·테스트 2줄 표준화. 워커 #2(BusyButton className 못받음)·#3(ReasonPromptDialog '확인' 고정·글자수/maxLength 없음)은 재사용 부품이 요구 못 채워 커스텀 정당. **코디 배선**: App.tsx import·pageFor·상세 param 라우트(ADMIN_ONLY). **⚠️ BLOCKED/이월**: ①**main.py에 `patient_merge.router` 미등록** — 백엔드 HTTP 미노출, 통합 배선 TODO ②**감사메모 POST 본문 필드 불일치**(워커 #4) — `saveMergeAuditNote`가 `{body}` 전송, 기존 `addPatientNote`는 `{content}`. `POST /patients/{id}/notes` 서버계약은 Task 10/13 영역(B-14 BLOCKED-BEFORE-MERGE 미확정) → **실서버 연결 시 필드명 정합 확인 필수** ③00044 클린DB 회귀 미검증(누적). **다음 = Task 27 `/admin/errors`**.
- **✅ Task 27 `/admin/errors` 완료(epoch6·`543b2bb`)**: 시스템 오류 기록 — **저장시점 redaction**(결정#20). `00048` safe_summary 칸 + `(occurred_at desc, id desc)` 인덱스. `errors.py`: `redact()`(전화·주민·JWT·`sk_live`·`키=값`·Bearer 제거, **오류종류·제약명 보존**) + `log_error(feature, message=None, *, safe_summary=None, exc=None)` 하위호환(원문→`logger.error` 서버로그, DB `message`=redact본, `safe_summary` 별칸). `app_error_handler` **미적재**(SCOPE-02)·`unhandled_exception_handler`만 적재(SCOPE-01). `GET /error-logs`(admin·`acquire_as` RLS·`{id,occurred_at,feature,summary}`·**message 미노출**·정렬·기간·limit200). `ErrorLogPage`(/admin/errors)+`api/errorLogs.ts`. **코디 배선**: main.py `error_logs.router` 등록·App.tsx `pageFor`(navItems `시스템 오류`는 기존 line42 ADMIN_ONLY). 백엔드 18/0(회귀 `test_error_masking`·`test_error_handling` 포함)·전체 프론트 689/689·tsc0. **⭐ 워커 판단(정당)**: 플랜 redaction 정규식이 자기 테스트의 `pw=hunter2`를 못 지우고 `secret` 부분매칭으로 `idx_internal_secret`를 훼손해 **기존 masking 원문보존 계약을 깸** → 단어경계+구분자 요구로 재작성, 회귀테스트로 못박음(플랜이 경고한 태스크간 문자열 드리프트를 잡음). 플랜 스캐폴딩의 가상 헬퍼(`bearer`·풀앱 `client`) 대신 실제 `test_dashboard_router` 패턴으로 재작성(운영코드 불변 monkeypatch로 커넥션 고갈 회피). **⚠️ 유의(코디용)**: `test_error_admin` autouse fixture가 `system_error_log` 전체를 매 테스트 앞뒤로 삭제 → **클린DB 전체 회귀는 병렬 오류-테스트 세션 없을 때 단독으로**. ⭐ **발견**: main.py에 `patient_merge.router` **이미 등록됨**(line33) — Task26 「미등록 TODO」는 낡음·해소됨. **다음 = Task 28 `/messages` 발송 만들기(SEND-*·MSGX-*, 00049, Task30 선행)**.
- **✅ Task 28 `/messages` 발송 만들기 완료(epoch6·`1c3231a`)**: 제1문·발송 패널·enqueue. `00049`: `scheduled_notifications`에 `cancelled_by/at`(결정#29)·`channel`·`target_count` + **`scheduled_notification_recipients` 명단고정표**(결정#5 ⓐ, `00016` 주석 ⓑ「발송시점 해석」을 뒤집음, RLS 부모표 패턴). `message_service`: `enqueue_send`(권한 이중방어·광고면 `(광고)`접두+무료수신거부 body에 지울수없게 박음 ADS-04·야간(21~08시) 광고즉시발송 차단→내일 08:00 제안 NIGHT-01/02·`_validate_scheduled_at` 5분/KST/`REGENERATION_WEEKS`(slot_generator=8주, 하드코딩금지)·즉시=notification_log '발송중' 행·예약=pending 행+명단고정)·`resolve_recipients`(광고=동의자만이나 consent 원천=환자앱이라 지금 전체+excluded=0 자리)·`list_messages`(예약 pending + 사람이 보낸 것만, 자동은 auto_count로 접음)·`cancel_scheduled`(pending만·`for update` 경합재검사·취소자/시각·409 사람문장). `GET/POST /messages`·`DELETE /messages/scheduled/{id}`. MessagesPage(두 구역)·SendPanel·RecipientField(PatientSearch가 id만 줌→「N명 선택됨」 인원표시, PII열거 방지)·KindChannelFields·NightRescheduleDialog·AllPatientsPreviewDialog. **코디 배선**: main.py `messages.router`·App.tsx pageFor(navItems `안내 보내기`는 기존 line36 RECEPTION_AND_ADMIN). 백엔드 30/0·전체 프론트 703/703·tsc0. **⭐ 워커 판단(정당)**: `ValidationError` errors.py에 없어 message_service에 정의(AppError파생 422)·`require_role_ctx` 없어 서비스 staff.role 검사+라우터 require_role 이중방어·`paginate(rows,cursor,order)` 행리스트·`seed_patient` 없어 인라인+conn=None 주입·플랜 테스트 날짜 2026-08-20이 과거라 `_future()` 재작성. **⚠️ Task 30 이월(구체·중요)**: ①**`_norm_channel`이 `push_sms`→`push`로 접어 저장**(00011·00049 channel check가 `push|sms`만 허용) → Task 30이 「앱+문자폴백(push_sms)」과 「앱만(push)」을 **구분 못 함** = SEND-CH-01 폴백 정보 손실. **Task 30이 요청채널 보존칸(requested_channel 등)을 추가하고 enqueue를 갱신**해야 폴백 가능(같은 화면 후반이라 message_service 되짚기 정당) ②실제 배달·`send_now`·상태콜백·재시도·발송결과 칸(`—` 자리)·명단열람·배지·`MSGX-SCHED-03` claim = Task 30 ③`sms_count`는 상한(대상수) 계약만·실값=환자앱. **⚠️ Task 29 이월**: `SEND-CH-05`(문자 off 잠금)·`SEND-ADS-05`(수신거부 번호 없음 잠금)=`hospital_settings` 의존이라 SendPanel 미구현(설정화면 소유). SEND-WHO-01(환자상세 프리필)·PICK 목록연동=슬라이스 밖. ⚠️ 00049 클린DB 회귀 미검증(누적). **다음 = Task 29 `/admin/settings` 병원 설정(HSET-* 71·HSETX-* 19, 00051, 최고밀도 xhigh→high)**.
- **✅ Task 29 `/admin/settings` 병원 설정 완료(epoch6·`032e1b8`)**: 한 화면·원자 저장(90규칙). `00051`: `hospital_settings` 칸5종(`auto_confirm_app_bookings`·`hospital_address`·`hospital_phone`·`sms_enabled`·`sms_recipients`·`sms_opt_out_number`·`version`, **공유칸 auto_confirm/sms_enabled는 `if not exists`** 환자앱 00020/00023 충돌방지) + `version` 낙관잠금 + `settings_audit_log`(비밀계열은 '변경됨'만·admin RLS). ⭐ **알림 문구표는 신설 안 하고 기존 `00013 notification_type_settings`(notification_type·body·also_sms) 재사용**(플랜 `notification_settings 신설`은 이 표 못 본 드리프트, body=override·also_sms=send_sms 매핑) — 코디 검증: 00013이 정확히 「종류별 문구 override+문자여부」 = 정당한 DRY(프로젝트 "새 표 전 기존 장치 확인" 원칙). `settings_service`: `get_settings`(scalar+알림 override 10줄, 없으면 `DEFAULT_MESSAGES` 코드기본+예정휴무 읽기전용+항목별 최근변경)·`save_settings`(**한 트랜잭션** 스칼라+알림+감사+version bump·같은txn 감사 AUDIT-02·409 버전충돌 STATE-03·**스칼라 키 화이트리스트 `_SCALAR_COLUMNS`로 f-string 주입방어**)·`preview_cancellation_deadline`(건수만·이름/전화 없음 API-03)·범위검증(#32 취소마감 0~168·오래대기 1~180). `GET/PUT /admin/settings`·`GET preview-cancellation`(admin). SettingsPage 5메뉴 셸+저장+dirty + BookingRules·WaitingRoom·SmsSettings·HospitalInfo·NotificationSettings·SaveConfirmDialog·MessagePreview + api/settings.ts. **코디 배선**: main.py `settings.router`·App.tsx pageFor(navItems `병원 설정`은 기존 line51 ADMIN_ONLY). 백엔드 14/0·전체 프론트 721/721·tsc0. **⭐ 워커 판단(정당)**: 4번째 메뉴 라벨=behaviors 개정 `자동 알림`(플랜 산문 `알림`은 구라벨, 규칙 이김)·`ValidationError` 로컬정의(422)·`require_role_ctx` 없어 `_require_admin`(StaffContext/dict 수용)·마이그 00051(플랜 「00035」 낡음). **⚠️ BLOCKED/이월(플랜명시)**: 문자 제공자 비밀키=배포 env·`MESSAGES` 기본원본=환자앱 3단계(렌더용 DEFAULT_MESSAGES 코드상수로 세움)·휴무/의사일정 편집=Task18·실제 발송소비=Task28/30·이력 더보기=Task15·오래대기 표시토글=전용칸없어 로컬 UI상태(분 기준값 보존). ⚠️ 00051 클린DB 회귀 미검증(누적). **다음 = Task 30 `/messages` 발송 결과·실패·재시도(SEND-RESULT/RETRY/FAIL/DEAD-*, 00050, Task28 후반·세로분할 2/2)**. ⭐ Task30은 Task28 이월(push_sms→push 채널 접힘)을 풀어야=요청채널 보존칸 추가+enqueue 갱신.
- **✅ Task 30 `/messages` 발송 결과·실패·재시도 완료(epoch6·`812ccad`) — 🎉 Wave C 완주**: ⚠️**플랜 절 없음** — behaviors 발송결과 절(434~493)에서 코디가 범위 도출해 브리프. `00050`: notification_log에 `requested_channel`(3값 보존 SEND-RESULT-09/#120)·`next_retry_at`·`provider_message_id`·`delivered_at/failed_at`·`batch_id`·`handled_at` + 배지(실패&미처리)/배치/콜백 인덱스. **`device_tokens` 신설**(코디가 `if not exists`로 하드닝=3단계 소유 충돌방지). `dispatch_service`: `send_now`(채널해석·죽은푸시토큰 삭제 03b·문자폴백 채널뒤집기 03c/09)·`mark_delivered/failed`(일시실패만 2회 재시도[1분·5분]·영구/미상=재시도 안 함=돈 안 씀 RETRY-02/03·없는번호→patients.sms_dead 표식 DEAD-07/08)·`_sms_eligible`(공용 HSET-SMS-05)·`claim_due_retries`(for update skip locked MSGX-SCHED-03). **제공자 호출은 주입 경계 `_provider_push/_provider_sms`(기본 NotImplementedError, 실제=배포env)**. `message_service`+: `enqueue`가 requested_channel·batch_id 저장(Task28 채널접힘 해소)·`list_messages` 배치별 결과집계·`badge_count`(비광고·번호살아있음·미처리실패 BADGE-02/03)·`failed_list`(지금전화/번호고쳐야함 두무리·탭별 마스킹·already_known FAIL-09)·`mark_handled`·`handle_status_callback`(서비스롤·provider_message_id로만·모르는값 무시). `POST /messages/status-callback`(무인증 제공자용)·`GET badge-count`·`GET {batch}/failed`·`POST {id}/mark-handled`. 화면: ResultCell(RESULT-11~14)·FailedListPanel(탭 둘·건수·[환자 열기]·⛔[다시 보내기] 없음)·useMessagesBadge·Header 죽은번호 표식. **코디 배선**: AppShell이 useMessagesBadge(접수/관리자만 enabled)→Sidebar counts(기존 NavBadge/counts 인프라 Task4). 백엔드 66/0·전체 프론트 731/731·tsc0. **⭐ 워커 판단(모두 정당, 코디 원문확인)**: ①`device_tokens` 실존 안 함(00012는 주석만)→00050 신설 ②`patients.sms_dead`는 `00014`에 이미 존재→재추가 안 함 ③SEND-RESULT-07 dedup 유니크는 00011이 이미 `<>'실패'`→재정의 안 함. **⚠️ BLOCKED/이월(구체)**: ①**실제 Twilio/FCM 전송·status-callback 서명검증 = 배포env**(⚠️**배포 전 서명검증 필수** — 무인증 콜백이 provider_message_id로 상태 변조 가능, gap #122) ②**번호 고치기 실제(OTP·Auth)= 갭 #19**([환자 열기]/[번호 고치기]는 /patients/:id로만) ③**SEND-FAIL-11/13 마스킹 풀기+열람기록** = SEND-OPEN 재사용 이월(기본 마스킹본) ④**스케줄 워커 cron 런타임 = 배포**(claim 쿼리만) ⑤**Task 10 상세응답에 `sms_dead`·`sms_dead_checked_at` 포함 필요**(Header 표식은 준비됨, 필드 오면 그림) ⑥예약발송 경로 requested_channel 보존은 즉시발송만(예약 발화 worker 미구현). ⚠️ 00050 클린DB 회귀 미검증(누적). **🎯 Wave C 완주. 남은 완주조건 = ①클린DB 전체회귀 1회(00038~00050 통합) ②손검수(사용자 약속대로 직원웹 완주 후)**.
- **✅ 클린DB 전체회귀 1회(epoch6, 2026-08-28) — 마이그 통합 PASS · 2건은 테스트 하네스 부채**: 사용자 신호("가") 후 `supabase db reset` → **34개 마이그(00033~00051, 00050 before 00051) 전부 순서대로 깨끗이 적용**(reset exit 0, 누적 미검증분 00038~00050 통합 확인 완료). 전체 백엔드 **593 passed / 2 failed**, 전체 프론트 **731/731**·tsc0. **⚠️ 2 failed = 제품/마이그 버그 아님, pre-existing 테스트 하네스 커넥션 누수**: `tests/test_patients_router.py::test_기록에_실패하면_번호도_주지_않는다`·`test_검색은_검색어와_함께_남는다`. **근본원인**: `db/pool.py get_pool`이 `asyncpg.create_pool` 크기 미지정→기본 **min_size=10**. 동기 `TestClient`가 요청마다 새 이벤트 루프를 만들어 그 루프에서 생긴 풀이 `close()` 실패로 **고아**가 됨(conftest:20-27 주석이 이미 인지) → **테스트당 ~10 연결 누수** → 7~8테스트 만에 Postgres 100슬롯 소진("remaining connection slots reserved for SUPERUSER") → `/patients` 검색이 DB연결 실패로 감사행 미기록 → `row` None → TypeError. **증거**: 두 테스트 **격리 실행은 항상 PASS**, 파일 단독도 #8에서만 실패(누적), db 재시작(연결 정리)해도 동일=런타임 누적. **수정 옵션(별도 태스크·Wave C 무관)**: ①`get_pool`에 min_size/max_size를 env로(테스트=1~2) ②conftest autouse가 매 테스트 후 앱역할 idle 연결 terminate ③`max_connections` 상향(supabase local은 ALTER SYSTEM 권한거부). `max_connections=500` 시도했으나 권한거부. **판정: Wave C 화면·백엔드·마이그는 클린DB에서 정상. 이 2건은 손검수·배포와 무관한 테스트 격리 부채**로 이월.
- **미결(사용자 결정 대기, 진행 막지 않음)**: **갭 #94** — 특정 날짜에 그날만 점심시간 이동, 실제 필요한가? (요구사항에 없음)
- **살아있는 갭·미결**(HANDOFF에서 이관, 잃으면 안 됨):
  - **갭 #128**: 의료판단 이관 티켓의 **의사 도착 화면이 없다**(`SHELL-NAV`=의사는 진료·환자검색만). Task 17은 이관 드롭다운에 활성 직원 전부 넣되 도착 화면은 안 만듦. 해소는 이후 `SHELL-NAV`·의료 escalation 모델과 함께.
  - **백엔드 계약 갭 2건**: ① `GET /staff/chat/tickets/{id}`(요약5+대화, Task 9 Produces 미명시) ② `reassign_ticket(ticket_id,to_staff_id)`+`POST .../reassign`(Task 2 목록에 없음). 부수: `.../read`(UNREAD-02)·`GET /staff/active`.
  - ~~**`SEARCH-LOG-06` N 판정** — 「조각 하나로 N명 이상」의 N(병원 감사 정책). 데이터는 이미 쌓고 판정만 미룸.~~ ✅ **해소(2026-08-29 구현, 사용자 지시 「정책 확정된 걸로 하고 만들라」)** — ⚠️ **문서의 「데이터 이미 쌓임(00031)」은 오류였다**(원문 확인: `search_patients`가 건수·조각을 안 남김). **적재부터** 만듦: 마이그 `00072`(`result_count·fragment_count` + 설정값 `wide_search_threshold_count` 기본 20), `_log_search` 저장, `is_wide_search` 서버 판정, `LogTable` ⚠ 배지, 시드 여러 종류. N은 관리자 설정값이라 정책 확정 시 조정 가능.
  - ~~**갭: stats 드릴다운 `booked` 미구현**(2026-08-29 S15 발견): …예약 `[상세 목록]`이 빈 명단…~~ ✅ **해소(2026-08-29 확인)** — `get_stats_detail`에 `if metric == "booked": return await _booked_detail(...)` 분기가 있고, `_booked_detail`이 `appointments.created_at` 기준으로 명단을 뽑는다(docstring이 옛 `to_status='booked'` 빈 명단 버그를 명시하며 고침). else 경로로 가지 않는다. `stats_service.py:230·277`.
  - ~~**갭: `max_daily_appointments` 미강제**(2026-08-29 S10 발견): 하루 최대 인원(요구사항 3.7 관리자 상한)이 표시 전용.~~ ✅ **해소(2026-08-29, A5 · `8749122` 백엔드·`49305fa` 프론트)** — 실제 강제로 전환. 창구는 「경고 후 허용」, walk-in 예외, 취소 제외. 규칙 `SCHED-CAP-01·02·03`. (환자앱 자가예약 하드차단 `SCHED-CAP-03`은 환자앱 단계.)
  - ~~**갭: 오류 로그 「서비스 전체 장애」 구분 필드 부재**(2026-08-29 S16 발견): `/error-logs` 계약이 `id·occurred_at·feature·summary`만 내려보내 **장애 종류(서비스 전체 장애 vs 일반)를 시각 구분할 수 없다**.~~ ✅ **해소(2026-08-28, A2)** — 마이그 `00070_system_error_service_outage`가 `system_error_log.is_service_outage boolean not null default false` 신설. ①`log_error`가 `is_service_outage=` 선택 인자로 적재(기본 False, 향후 SMS/push 디스패처=배포 T30이 채움) ②`error_logs.py` select·DTO row에 실기 ③프론트 `ErrorLogRow` 필드 + `ErrorLogPage` 행에 amber(`--color-warn`) 삼각형+「서비스 전체 장애」 배지 ④시드 §17 15건 중 문자·푸시 2건 true. **끝단 검증**: API `is_service_outage` 반환·shot 배지 렌더·가드 테스트 `[ERRADM-NOTI-02]`. ⛔ 데모의 4칸 상태 모음판은 규칙 근거 없어 계속 생략(배지만). 업체 오류코드→분류 매핑은 `ERRADM-NOTI-02`가 여전히 「구현 계약」(디스패처). 번호 배정: 챗봇·배포를 안 밀려고 데모병합 후속 신설 밴드 `00070`–(→ `MIGRATION-LEDGER`).
  - ~~**갭(S6 의사콘솔, 2026-08-29 커밋 `12ca74e`)**~~ ✅ **3건 전부 해소(2026-08-28, A3 · 커밋 `e7bc960`) — 무마이그**:
    - ~~**순번 동점키 `DOCTOR-QUEUE-03`**: `queue_position`이 null이라 「–」.~~ ✅ **`get_doctor_queue`에 `row_number()` 표시순번(`display_position`) 추가** → queue_position 비어도 1·2·3. 정렬 동점키(`a.id` 최종)는 이미 완비였다(정렬 부분은 갭 아님).
    - ~~**주의 표시 `DOCTOR-QUEUE-02`**: urgent 필드가 큐 DTO에 없음.~~ ✅ **`is_urgent_flag`는 `00005`부터 있던 칸**(핸드오프의 "urgent 칸 마이그" 오판 — 원문 grep으로 정정). `get_doctor_queue` DTO에 실어 `QueuePanel`이 amber 「⚠️ 주의 표시」 텍스트 배지. 시드에 오늘 3건 urgent. ⚠️ 「누가 언제 켰는지」 표시자·시각은 별개 갭(`QUEUE-URG-06`, S2)로 남음 — 여긴 boolean만.
    - ~~**과거 진료기록 미배선 `DOCTOR-HISTORY-01`**: `HistoryPanel`이 `records={[]}` 하드코딩.~~ ✅ **`get_console_history`(dashboard_service) 신설**(완료만·현재예약 제외·최신순·과·의사) + `GET /doctors/console/patients/{id}/history`. care-continuity RLS(`doctor_can_view_appointment`)로 담당 중 환자의 타과 완료기록까지. 라이브 검증 58건.
