# 플랜 밖으로 넘긴 미결 — 이관 원장

> ⚠️ **이 파일은 사람이 읽으라고 있는 것이 아니라 `plan-prefix-check.py`가 읽는다.**
> 검사기가 매 태스크마다 이 표를 훑어, **받을 플랜에 그 규칙 ID가 아직 안 들어갔으면 경고**한다.
> 들어가는 순간 저절로 조용해진다 — 그래서 「나중에 하겠다」가 약속이 아니라 **기계가 지키는 것**이 된다.
>
> **왜 필요한가**: 한 플랜을 쓰다가 *"이건 저쪽 플랜에서 정하는 게 맞다"*고 넘기면, 그 순간 **아무 검사기의 시야에도 없는 상태**가 된다.
> 넘긴 쪽 플랜은 이미 다 썼으니 안 보고, 받을 쪽 플랜은 아직 안 썼으니 모른다. `SUPPORT-CAL-*` 14개가 정확히 그렇게 **어느 배정 표에도 없이** 남아 있었다(2026-08-15, Task 14에서 발견).
>
> **행 추가 형식**: `| 규칙 ID | 넘긴 곳 | 받을 영역 | 정해야 하는 것 | 후보 |`
> - **규칙 ID는 백틱 없이** 정확히 쓴다 — 검사기가 그대로 문자열 검색한다.
> - **받을 영역은 파일 이름이 아니라 영역 이름**(`ai-chatbot`·`patient-app`·`staff-web`·`deployment`). 검사기가 `plans/*<영역>*.md`를 **전부** 훑으므로, 플랜이 재작성돼 날짜가 바뀌어도 이 표는 낡지 않는다.
>
> ⛔ **받았다고 이 행을 지우지 말 것.** 받는 순간 검사기가 저절로 조용해진다. 지우면 「넘겼다는 사실」이 사라져 다음 사람이 처음부터 다시 발견해야 한다.

| 규칙 ID | 넘긴 곳 | 받을 영역 | 정해야 하는 것 | 후보 |
|---|---|---|---|---|
| PTDET-SUPPORT-03 | staff-web Task 10(`/patients/:id`) | ai-chatbot | **환자 상세의 상담 문의 카드를 무엇으로 정렬하나 — 특히 동점일 때.** 규칙은 *"최신 생성 시각 위, 동점 시 티켓 ID를 마지막 키"*라고 정해 뒀지만 **`support_tickets` 표 자체를 4단계가 만든다.** 서버가 그 정렬을 보장하지 않으면 새로고침마다 순서가 흔들리고, 여러 문의 중 어느 것을 먼저 볼지가 매번 달라진다 | ⭐ 후보를 새로 만들 필요가 없다 — **Task 13이 이미 쓰는 `paginate`의 안정 동점 키 방식**(`created_at desc, id desc`)을 그대로 쓰면 된다. ⚠️ 규칙이 말하는 「티켓 ID」가 uuid면 시간순과 무관하므로 **반드시 `created_at`을 첫 키로** 두어야 한다 |
| SB-22 | staff-web Task 24(`/patients`) | staff-web | **「넓은 검색」(조각 하나로 결과 N명 이상)을 관리자 기록장에 ⚠로 특별 표시하나 — 그 판정 기준 N.** 데이터(결과 건수·조각 수)는 `SEARCH-LOG-06`으로 지금(Task 6·24) 이미 남긴다. 남은 것은 **표시 여부와 N 값**뿐인데 C등급(병원 내부 감사 정책)이라 이월했다(2026-08-17, A안 확정) | ⭐ 데이터가 이미 쌓이므로 **켜기만 하면 된다** — 관리자 기록장(Task 15 `ALOG-*`)에서 `search` 사건 중 `조각 수=1 and 결과 건수>=N`인 줄에 ⚠ 배지를 다는 조건 한 줄. N은 운영 데이터를 본 뒤 병원이 정한다. ⚠ 「몇 명 이상」이 아니라 반드시 **「조각이 하나이고 결과 N명 이상」**이어야 한다(이어 친 것은 이미 좁히는 중) |
| SUPPORT-CAL-DUP-01 | staff-web Task 14(`/calendar`) | ai-chatbot | **한 예약에 상담 기록이 여러 개 붙었을 때 예약 블록의 `⚠` 하나가 무엇을 대표하나.** 규칙이 정한 것은 *"⚠를 여러 개 겹쳐 그리지 않는다"*까지고, 대표 선정과 묶는 방법이 열려 있다 | ①**가장 최근 기록**을 대표로 하고 패널에 `상담 3건` 개수를 함께 적기 ②**아직 처리 안 된 것 중 가장 오래된 것** — 「먼저 온 사람 먼저」와는 맞지만 **최신 요청을 못 본 채 처리**할 수 있다. ⚠️ `support_tickets`의 상태·담당 모델이 확정돼야 갈린다 |
| CARD-LATE-10 | patient-app Task 8·17(이력·홈 카드가 부도 표시를 이 배치에 의존) | deployment | **예약확정인 채 시각이 지난 예약을 `예약부도`로 전환하는 「자정 배치」의 구현 주체·함수 계약**(갭 #28). 화면·시점·문구는 2026-08-01에 이미 확정됐고, **찍는 주체(배치)만 비어 있었다**(`CARD-LATE-10`·`TODAY-NOSHOW-02`가 "찍는 주체 없음"으로 미결). 환자앱은 부도를 「표시」만 하고(Task 8 이력=방문하지않음, Task 17 홈 카드), 「전환」은 시스템 배치라 환자 세션 서비스와 안 맞아 넘긴다 | ⭐ **㉮ 확정(2026-08-17, 사용자)**: 공용 SQL 함수 `mark_overdue_no_shows() returns int`(`SECURITY DEFINER`) — `status='예약확정' and slot_date < current_date`(자정 넘겨 어제까지) → `'예약부도'`. ⛔ `도착`·`진료대기`·`진료중`은 제외(사람이 `/today` `TODAY-YDAY`에서 처리) · `예약신청`도 제외(이력 `확정되지않음`, `HIST-ROW-09`). 자정 KST 크론으로 호출(리마인더 잡 `app.jobs.reminders` 옆, deployment Task 6·16 선례). ⚠️ 배포가 확정할 세부: **시스템 행위자 이력 기록**(`log_appointment_status_change` 트리거는 staff·patient 행위자가 없으면 이력을 건너뛴다 — 배치 전용 행위자 경로 필요) · 부도 후 `booking_code` 처리(`CARD-OK-03`: 당일 부도 전엔 null 안 됨). ⚠️ 갭 #28의 **나머지 절반 = 직원 수동 부도 버튼**은 staff-web 몫으로 별개(원문 `ui-design-decisions.md:984`) |
