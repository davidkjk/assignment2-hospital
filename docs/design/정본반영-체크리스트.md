# 정본 반영 체크리스트 — 데모에서 확정한 것을 정본(screen-behaviors·결정로그·목업)에 반영

> 원본 추적 = `demo/DEMO-REVIEW-NOTES.md`(특히 D절·G절·「⚠️ 정본 반영 대기」). 이 파일은 **TDD 구현 착수 전에 하나씩 결정을 확정하며 체크**하는 실무 목록. 결정이 끝나면 `design-merge` 스킬 절차로 실제 병합.
>
> 상태 표기: `[ ]` 미결정 · `[x]` 반영하기로 확정 · `[-]` 반영 안 함(데모 자체 시스템/DESIGN-NOTES에 둠)
>
> ⚠️ **병합 대상 = `screen-behaviors.md`(동작) + 결정로그(왜)뿐. 목업은 반영 제외**(사용자 2026-08-24 — 목업은 참고 자료라 규칙이 정본, 67장 갱신 생략).
>
> **병합 진행**: (가) 정본 병합 → (나) 플랜 배선(범주6) → TDD. 진행 상태는 이 파일 맨 아래 「병합 진행 로그」에 기록.

## 범주 1 — 규칙 뒤집기 (정본 규칙이 이미 있고 데모가 반대로 **사용자 확정**함 → 정본 수정 + 뒤집힌 옛 항목에 역참조 필수)

> ✅ **2026-08-24 사용자 일괄 확정 — 다섯 건 모두 「이대로 정본 반영(역참조 포함)」.**

- [x] **세 문(등록·접수·예약)** — `SHELL-HDR-01`·`SHELL-ACT-01`(SB-43 두 버튼)을 뒤집음. `QUEUE-WALK`(당일 방문)=등록+접수 분해. (근거=DEMO-REVIEW F-4)
- [x] **홈 전부 히어로** — `HOME-CARD-02`(2건+ 줄 목록)을 「전부 히어로」로 뒤집음. (C-4)
- [x] **전체 상담 기록 → 기록 그룹(관리자 전용)** — `SHELL-NAV-01/02` 개정(업무 그룹서 제거·기록 그룹 추가·접수직원 비노출). (F-1)
- [x] **도착↔진료대기 하이브리드** — 미도착에 [진료 대기]/[도착] 두 버튼, 도착(보류)은 예약시각에 자동 전환. `QUEUE-ORDER`에 예약시각 우선 + 「예약시각 자동 전환」 규칙 신설. (G절 해소)
- [x] **의사 화면 진료기록 배치** — 작성 칸을 위로/과거 기록을 아래로. `DOCTOR-HISTORY-01`「오른쪽 위」와 어긋남 → 갱신·역참조. (G-7)

## 범주 2 — 새 동작/규칙 신설 (정본에 빈칸이거나 없음 → 넣을지 결정)

> ✅ **2026-08-24 사용자 「전부 반영해」 — 아홉 건 모두 정본 반영 확정.** ⚠️ 반영 시 성격 구분: 대부분 순수 추가지만 E-6·E-7·E-9는 기존 규칙과 부딪혀 **역참조/조정** 필요(아래 표시).

- [x] **이력 월 그룹**(연/월 가로줄·레일에서 월 제거) — `HIST-*`·목업 `50-history-list-full`. (A-1) ✅ 확정
- [x] **이력 연도 바로가기** — `HIST-*`에 규칙 추가. (A-2) ✅ 확정
- [x] **대기목록 탭별 정렬 방향** — 대기 계열=이른 순 / 끝난 계열=최신순 / 전체=시각 내림차순. `QUEUE-*`. (E-5·D절) ✅ 확정 (순수 추가)
- [x] **오늘현황 넓은 화면 2열** — 넓으면 오른쪽 sticky 레일, 좁으면 세로 스택. ⚠️`TODAY-LAY-01`(요약 아래 세로 명시)에 「넓은 화면 2열」 추가·조정. (E-6) ✅ 확정
- [x] **접수 헤더 패널화** — 사이드바 전체화면 라우트 → 헤더 패널. ⚠️`CHKIN-*` 패널 전제로·헤더 버튼 2→3개(세 문과 묶임). (E-7) ✅ 확정
- [x] **캘린더 현재 시각 자동 스크롤** — `CAL-*`에 「오늘 열면 현재 시각으로 스크롤」 추가(지금 선의 짝). (E-8) ✅ 확정 (순수 추가)
- [x] **헤더 = 화면 제목** — ⚠️`SHELL-HDR-01`(병원명)을 화면 제목으로 확정(병원명은 사이드바 워드마크). ~~역참조 필요~~ ✅ **정본 반영 완료(2026-08-28)** — `SHELL-HDR-01` 재개정 + 딸린 `STAFF-SHELL-02`(본문 제목 제거)·`NAV-SHELL-12`(클릭 대상이 사이드바 워드마크로) 개정, 근거는 결정 문서 「세 문 라벨·헤더 왼쪽·사이드바 아이콘을 데모에 맞춘다」. 실 구현도 반영. (E-4·E-9) ✅ 확정
- [x] **환자 고른 뒤 왼쪽=환자 상세 카드** — `PANEL-WORK-02`(검색표/캘린더만)에 추가. (반영대기 2) ✅ 확정 (순수 추가)
- [x] **안내자료 키워드 검색** — 정본 KBADM은 분류·상태 필터만. `SEARCH-BOX-01` 결로 추가. (반영대기 6·F-8, 미구현) ✅ 확정 (순수 추가)

## 범주 3 — 라벨/문구 변경 (문자열만, 저위험)

> ✅ **2026-08-24 사용자 「전부 반영」.** 단 기간 프리셋 라벨 = **"30일·90일" → "1개월·3개월"**(7일·1개월·3개월·1년·전체).

- [x] **오늘현황 카드 제목 줄임** — 장기 대기/미접수·시각 경과/전일 미완료/확인 필요 예약. ⚠️`TODAY-NOSHOW-01`이 제목 문자열 명시(함께 갱신). (E-3) ✅ 확정
- [x] **병원 설정 라벨** — 「알림」→「자동 알림」, 「문자로도」→「문자도 발송」. `HSET-MSG-01/02`. (반영대기 3) ✅ 확정
- [x] **메뉴명 「진료 일정 관리」** — 사이드바 라벨. `SCHED-*`. (반영대기 4) ✅ 확정
- [x] **안내자료 상태명** — 공개→승인됨, 검토 중→승인 대기. `KBADM-LIST-03`. (반영대기 5) ✅ 확정
- [x] **「상담봇 기록」 이름** — "전체 상담 기록"→"상담봇 기록". `SHELL-NAV-01/02`·`CHATLOG-*`. (반영대기 8) ✅ 확정
- [x] **기타 라벨** — 탭 "미도착", 버튼 "진료 대기", "상담봇 문의함", "상담봇 관리", 메뉴 순서(업무→기록→상담봇 관리→설정), "대표 검토". (G절) ✅ 확정
- [x] **기간 선택기 프리셋** — 7일·**1개월·3개월**·1년·전체 + 직접 입력. 정본에 기간 옵션 규정 있으면 대조. (반영대기 7) ✅ 확정

## 범주 4 — 확인 대기 (정본 근거 못 찾음, 데모가 판단 → 정본에 넣을지)

> ✅ **2026-08-24 사용자 결정.**

- [x] **오답신고 "반려"** — **반영**: 삭제 아님, 처리완료(반려)+사유 로그 유지. `KBADM/BADINBOX-*`에 신설. (F-8) ✅
- [x] **병합이력 "감사메모 저장"** — ✅ **정정 확정(사용자 2026-08-24 "그대로 두자")**: 노트의 "근거 없음"이 틀렸음(원문=`MHIST-LOCK-02`·`MHIST-NAV-08`·결정17, 잠긴 병합의 감사 기록). **정본 무변경.** ✅ **데모 코드 정합(2026-08-24)**: 데모가 감사메모를 전부 지웠던 것을 **되돌림불가(잠김) 케이스에만** `[감사메모]` 인라인으로 복구(`MergeHistory.tsx` — 대상 환자 열기 옆). tsc·vitest 58 통과.
- [x] **품질/현황 기간 선택** — **반영**(7일 고정 → from/to 기간 선택, `STAT-SCOPE-01`과 일관). (F-8) ✅

## 범주 6 — 시각(겉모습)을 실 구현에 연결 (사용자 지적 2026-08-24 — 자동으로 안 따라감)

> DESIGN-NOTES.md·데모 컴포넌트는 규칙이 아니라 "시각 레퍼런스"라 TDD 테스트가 안 지킨다. **플랜/브리프에 화면마다 명시적으로 링크를 박아야** 워커가 본다. 안 박으면 누락.

- [x] **플랜 각 화면 태스크에 「시각 레퍼런스」 링크 추가** — 각 태스크에 ①데모 컴포넌트 파일 경로 ②`demo/DESIGN-NOTES.md`의 해당 절. (환자앱·직원웹·챗봇 플랜 전부) ✅ **완료(2026-08-24)**: 3개 현행 플랜 상단에 「시각 레퍼런스」 절 신설(surface 규약 + 태스크→데모 컴포넌트 매핑표). 코디가 브리프 만들 때 해당 행을 붙이는 방식.
  - ⭐ **surface별로 문구가 다르다(스택 확인 2026-08-24)**: **직원 웹·웹 상담창(React+TS)** = "이 데모 컴포넌트를 **기본 틀로 포팅**"(구조·레이아웃 그대로, 가짜데이터→React Query, 하드코딩 토큰→tokens.json). **환자 앱·앱 상담 탭(Flutter)** = "이 데모 화면을 **시각 기준으로 Flutter 재현**"(코드 못 옮김, 보이는 것만 일치). 환자 앱은 어느 길로 가도 데모=시각 명세(웹/RN 전환 비추천).
- [x] **브리프에 frontend-design 스킬 호출 명시** — ✅ **완료(2026-08-24)**: 3개 플랜 「시각 레퍼런스」 절에 `invoke frontend-design` 명시 규약을 박음(별도 플러그인이라 자동 호출 안 됨 · Codex 워커는 텍스트로 넣거나 Opus로).
- [~] **(선택) 검사기** — 각 플랜 절에 "화면 태스크마다 브리프에 `시각 레퍼런스:` 줄이 있는지 grep" 규율을 문서로 명시함. **실제 grep 스크립트는 대상(브리프)이 아직 없어 보류** — 브리프 생성(TDD 착수) 단계에서 추가.
- [x] **(선택) 규칙 취지 구체화 승격** — ✅ **`TICKET-DETAIL-CONV-01` 대화 좌우분리(환자=왼쪽/AI·직원=오른쪽) 반영**(정본에 없던 확정 데모 결정). `QUEUE-ORDER-04` 삽입선은 **범주5(반영 불필요·데모 시스템)** 분류라 규칙화 안 함.

## 범주 5 — 반영 불필요 (정본대로거나 「취지 내 개선」 → 데모 시스템/DESIGN-NOTES에 둠, 정본 규칙 안 건드림)

- [-] 로그인 화면·풍부한 카드·색배지·QR·Solid 아이콘·예약탭 얇은줄·8의사 규모·종일 캘린더·지금 선·시간축 확대(CAL-ZOOM)·대화 좌우분리·글자 위계·삽입선 순번칩·그림자/흰색 규칙·Do Hyeon 서체·의사 일러스트 아바타 등.

---

## 병합 진행 로그

> 방식: 데모는 정본의 mirror가 아니라 새 초안 → `diff` 아니라 **규칙을 정본 절에 직접 심기**(design-merge §1-⭐). 순서 = 결정로그(왜) → screen-behaviors(동작). 목업 제외.

- **① 세 문** ✅ **병합 완료 (2026-08-24)**
  - 결정로그(`2026-07-31-ui-design-decisions.md`): `~~SB-43 해소~~` 뒤집힘 배너 + **새 결정 「세 문」** 블록 + 요약표 SB-43 행 갱신.
  - screen-behaviors: `SHELL-HDR-01`(세 문 구성) · `SHELL-ACT-01`(세 문+역참조) · **신설 `SHELL-DOOR-01~06`** · `QUEUE-WALK-01`(분해 역참조).
  - ⚠️ **남은 스윕(①)**: 산문에서 옛 라벨 "`[＋ 새 예약]`·`[＋ 당일 방문]`"을 참조하는 곳 — `PTDET-STATUS-03`·`PTDET-VISIT-06`·`PTDET-ACTION-05`·`CHKIN-HEAD-01`·`SEND-DOOR-01`·결정로그 `:1841`. (규칙 ID는 유효, 라벨만 낡음 → 세 문 라벨로 정리.)
- **범주3 독립 라벨** ✅ **병합 (2026-08-24)** — `TODAY-NOSHOW-01`(미접수·시각 경과, 무책망 유지) · `KBADM-LIST-03`(⭐데모 원문 확인=**승인됨/임시저장** 2상태, "승인 대기"는 낡은 노트였음) · `HSET-MSG-01`(자동 알림)·`HSET-MSG-02`(문자도 발송)·`HSET-NAV-01`(자동 알림).
- **범주3 「대표 검토」 + 범주4 오답 반려** ✅ **병합 (2026-08-24)** — `[대표로 검토]`→`[대표 검토]` 라벨 4곳 일괄(프로세스명과 일치) · `BADINBOX-REVIEW-05` 반려="확인 필요"→**사유 필수·삭제 아님·처리완료(반려)+사유 로그**(조용히 사라짐 금지).
- **E-3 카드 제목** ✅ **(2026-08-24)** — `TODAY-WAIT-01`(장기 대기)·`TODAY-YDAY-01`(전일 미완료) 업무용 줄임 반영(NOSHOW-01은 앞서 완료). (확인 필요 예약은 정본 "확인 필요"와 유사, 후순위.)
- **HSET-SMS 라벨 스윕** ✅ **(2026-08-24)** — `HSET-SMS-01·02·02b·02c·05`의 옛 「알림」 줄→「자동 알림」 줄, 「문자로도」→「문자도 발송」 일괄. 검증: 옛 라벨 0 잔존(대기실 `HSET-WAIT-02`의 일반어 "알림"은 별건, 유지). 라벨 공존 없음.
- **⚠️ 얽힘 발견**: 범주3 사이드바 라벨(진료 일정 관리·상담봇 기록·상담봇 관리·메뉴 순서)은 **범주1 #3(상담기록 그룹 이동)과 같은 규칙 `SHELL-NAV-01/02`** → **한 번에 처리**(다음 배치).
- **⭐⭐ 2차 배치 ✅ 완료 (2026-08-24, 미커밋)** — 핸드오프 🔜 1~5번 죽 진행:
  1. **기간 프리셋(#7)** ✅ — `PERIOD-BOX-01~05` **전역 규칙 신설**(날짜칸 상시·프리셋 7일/1개월/3개월/1년/전체·직접 입력 자동표시·「직접 지정」 항목 폐기). `STAT-SCOPE-01`·`UNRES-CLUSTER-01`·`QTOP-RANK-01`·`QUALITY-REPORT-01`·`BOTSTAT-DASH-01`에 역참조. **범주4 품질/현황 기간**도 함께 해소(정본은 이미 from/to였고 데모만 7일 고정이었음). 결정로그 블록 신설.
  2. **사이드바 뭉치** ✅ — `SHELL-NAV-01/02/04/09` 개정: 그룹 순서 `업무→기록→상담봇 관리→설정`(설정 맨 아래) · **상담봇 기록(옛 전체 상담 기록) 업무→기록 이동**(접수직원 비노출) · 개명(상담봇 기록·상담봇 문의함·진료 일정 관리·상담봇 관리) · 접수는 헤더 문으로(업무 6개). 라벨 스윕: 「전체 상담 기록」→「상담봇 기록」·「문의 티켓함」→「상담봇 문의함」 전체 치환(역참조 3곳 보존)·`SHELL-NAV-08` 8→6개. 결정로그 블록 신설.
  3. **범주1 남은 3건 뒤집기** ✅ — **홈 히어로**(`HOME-CARD-02` 줄묶음→전부 히어로, 결정로그 역참조) · **도착↔진료대기 하이브리드**(`QUEUE-ARRIVE-01~07` 신설: 미도착 두 버튼 `[진료 대기][도착]`·예약시각 자동 전환·두 카운트 유지 / `QUEUE-BTN-01·02`·`QUEUE-ROW-06`·`QUEUE-ORDER-01`·`TODAY-BTN-02`·`CHKIN-RESULT-01·03·04`·`SEARCH-ACT-02`·`PICK-ACT-01c` 정합 / 탭 「아직 안 옴」→「미도착」 전체 개명 / 결정로그 블록) · **의사화면 배치**(`DOCTOR-HISTORY-01` 오른쪽 위→작성 칸 아래, `DOCTOR-RECORD-01`·`DOCTOR-SHELL-04` 정합, 결정로그 블록).
  4. **세 문 라벨 스윕** ✅ — 옛 「`[＋ 새 예약]`·`[＋ 당일 방문]`」 참조 정리: `SHELL-HDR-05`·`PTDET-STATUS-03`·`PTDET-VISIT-06`·`PTDET-ACTION-05`·`CHKIN-HEAD-01`·`DOCTOR-SHELL-03`·`SEND-DOOR-01`·`QUEUE-EMPTY-02`(→`[＋ 접수]`) + 결정로그 QUEUE-EMPTY 가이드.
  5. **범주6 플랜 배선** ✅ — 현행 3플랜(staff-web·patient-app·ai-chatbot) 상단에 「시각 레퍼런스」 절 신설(surface 규약·frontend-design 명시·태스크→데모 컴포넌트 매핑표).
  - **기계 검증**: 신설 ID 정의 유일(`PERIOD-BOX-*`·`QUEUE-ARRIVE-*`), 탭 7개 유지, 옛 라벨은 역참조 자리에만 잔존, 플랜 3개 절 존재 — 통과.
- **⭐ 마무리 배치 ✅ (2026-08-24)** — 남은 것 처리:
  - **범주4 감사메모** ✅ — 정본 무변경(정본이 옳았음). **데모 코드 정합**: `MergeHistory.tsx` 되돌림불가(잠김) 케이스에만 `[감사메모]` 인라인 복구(`MHIST-NAV-08` 준수). tsc·vitest 58 통과.
  - **범주6 (선택) 규칙 취지 구체화** ✅ — `TICKET-DETAIL-CONV-01`에 대화 좌우분리(환자=왼쪽/AI·직원=오른쪽) 한 줄 승격. 삽입선은 범주5라 제외.
  - **범주6 (선택) 검사기** — 브리프가 아직 없어 보류(TDD 착수 시).
- **🔜 다음**: 이 배치 커밋 → TDD 착수(범주6 매핑표대로 브리프에 시각 레퍼런스 주입).

## 구현 병합(데모-우선) 이월 — S2 `/queue` (2026-08-29)

- ⏳ **응급 표시자 `QUEUE-URG-06`** — 「누가 언제 켰는지」(예: `박간호 님이 켰습니다`)를 데모는 확인창에 띄우나, 백엔드 `get_queue`/`QueueRow`에 표시자·시각 필드가 없다. 붙이려면 ①`set_urgent_flag`가 표시자·시각을 기록 ②`get_queue`가 함께 실어 보냄 ③`QueueRow`에 필드 추가. 그때 `UrgentDialog`에 블록 복구.
- ⏳ **대기시간 컬럼** — 데모 진료대기 줄의 「52분 대기」. 실 `QueueRow`에 대기분이 없다(진료대기 진입 시각 필요). 붙이려면 상태이력 `changed_at`에서 계산해 실어 보냄.
- ⏳ **채워진 진료대기·도착 눈 대조** — 새벽(04:43) 촬영이라 순번·응급바·되돌리기·드래그 삽입선을 실 렌더로 못 봄. 낮(09~18)에 `tools/shot/shot-queue.mjs`로 재대조.
- ✅ **`QUEUE-BTN-03` 문구 낡음 해소 (2026-08-29, 사용자 확정 「수정」)** — `QUEUE-BTN-03`에 `[되돌리기](UNDO-*, →도착)` + `~~「…만」~~ ✅해소` 역참조를 박고, `UNDO-BTN-01` 쪽에도 `/queue 적용=QUEUE-BTN-02·03` 역참조를 남겨 양방향 링크로 만듦. 코드는 이미 되돌리기를 넣어 둔 상태.

## 구현 병합(데모-우선) 이월 — S3 `/calendar` (2026-08-29)

- ⏳ **상담 요약 배선 (`TODAY-RESCHED-25`·`SUPPORT-CAL-*`)** — 실 `AppointmentPanel`은 `support?: SupportSummary` prop과 `cal-support-summary` 읽기전용 섹션을 **이미 규칙대로 구현**해 뒀으나, `CalendarPage.openAppointment`가 `support`를 넘기지 못한다. 원인: `api/calendar.ts`의 예약 bar에 `support_requested_at`·`request_type`(+담당·연결시각)이 없다 = 블록. 붙이려면 ①백엔드 `_calendar_bar`가 support 필드를 실어 보냄 ②`api/calendar.ts` 타입·`CalendarData` 확장 ③`openAppointment`가 bar→`SupportSummary`로 매핑해 전달. **S1 `/today`의 상담행·일정변경 이월과 같은 support-티켓 데이터 묶음.**
- ⏳ **`[상담 전체 보기]` 버튼 (`TODAY-RESCHED-26`)** — 데모 `TodayContextPanel`엔 있으나 실 `AppointmentPanel`엔 없다. support 배선 시 함께 추가: 상담봇 문의함의 해당 티켓 넓은 상세로 이동(뒤로 = 캘린더 날짜·필터·스크롤·패널 복원). **⛔ 데모의 별도 `TodayContextPanel`을 만들지 않는다** — `NAV-STFSUP-14`가 「캘린더에 상담용 두 번째 패널 복제 금지」라 요약은 `AppointmentPanel` 안에만 둔다(판정 2026-08-29).
- ⏳ **낮 브라우저 눈 대조(S6 시점 몰아서)** — 지금 선 rose 라벨/점·빈 시간 스냅 호버는 오늘·데이터가 있어야 실렌더된다. 열 경계 삼각형 아티팩트·주간뷰·8번째 의사 가로스크롤도 함께.

## 구현 병합(데모-우선) 이식판정 — S4 `/patients/:id` (2026-08-29)

⭐ **큰 발견: 실 `/patients/:id`는 이미 완성·배선·테스트(25 GREEN)·rule-complete.** `App.tsx:90`에 역할가드로 라우팅됨. 실이 **데모보다 규칙이 많다**(dead-number·번호복사·역할칩·독립섹션실패·실제 문진응답·중립문구). → **restyle-real 방식 확정**(데모 단일파일 복사였으면 ① 27건 전부 잃음). 데모는 시각 폴리시(카드는 이미 거의 동일)만 기여.

- **① 이식 후보 27건(실만 표기) = 전부 실이 이미 구현, 유지.** HEAD-02~06(관계칩·역할칩·열람기록·dead-number·번호복사)·LOAD-02(섹션 독립 실패, 각 섹션 자기 쿼리)·NOTE-01~05·QNR-01/04(의사전용 게이트+실제 응답)·RECORD-01/05·STATUS-01·SUPPORT-01·VISIT-01/05(중립문구)/06·FAMILY-01/02/04·ACTION-03~06. 코드에서 직접 확인함.
- **② 데모만 2건 = 실이 이미 커버.** `PTDET-RECORD-03`(수정·삭제 버튼 없음) = `RecordSection`이 설계상 읽기전용(주석이 RECORD-03·04 인용). `PTDET-VISIT-03`(정렬 방향) = 서버 정렬 소관 → **데이터 있는 S6에 눈 확인**.
- **③ 무표기 32건 = S6 브라우저 검수**(PTDET-STATUS-02~05·VISIT-02/04/07/08·SUPPORT-02~05·FAMILY-03/05·LOAD-01/03/04·ACTION-01·QNR-02·RECORD-02/04 + PTSUP-SECT-* 12건).
- **⏳ S6 데모 시각 폴리시(코드 소량, 브라우저 대조 필요)**: ⓐ헤더 아바타 원(`UserRound` bg-primary/10) ⓑ상태 배지를 공용 `StatusBadge`(색+글자, `DISP-COLOR-01` 만족 — 실은 현재 무색 텍스트, 데모는 색배지; 둘 다 규칙 만족이라 **어느 look이 이길지 S6 눈판정**) ⓒ섹션 순서(`PTSUP-SECT-ORDER-01` = ③ 브라우저판정이라 지금 안 바꿈) ⓓ전화변경: 실=PanelHost 사이드패널 vs 데모=헤더 인라인 2단계 — 실 사이드패널이 스태프웹 패턴, 유지.
- **BLOCKED(백엔드 대기, 정상 stub)**: 전화변경 OTP(#19, 501 거절)·가족연결 대상검색(Task 13)·상담문의 4단계(0건).

## 구현 병합 스윕 결과 — 전 화면 이미 구축됨 (2026-08-29)

⭐⭐ **핵심: S1~S18 모든 실 화면이 이미 구축·라우팅(App.tsx)·테스트됨.** 밤샘 「화면 만들기」는 no-op. Wave 2 병합의 실질 = 데모 **시각** 정렬 + ③ 무표기 규칙 **브라우저 검수** = **대부분 S6 낮**.
- 스윕(2026-08-29): doctor(20파일/7테스트)·messages(11/3)·checkin(6/2)·admin/schedule(18/7)·settings(9/1)·staff(15/7)·questionnaires(3/1)·errors(2/1)·merge(8/3) 전부 존재 + 라우팅됨.
- **S5 `/patients` 판정**: 27 GREEN·라우팅됨·① 31건 실 커버(테스트가 SEARCH-ACT/RESULT/RUN/WHY 소진). ② `SEARCH-MATCH-01`(이름 부분일치)=**서버 매칭 behavior**, 실은 `WhyBadges`(이름/전화/생일 일치)로 근거 표시 → 프론트 갭 아님(서버 substring은 S6/백엔드 확인). ③ 21건 S6. 살아있는 갭 `SEARCH-LOG-06`(N 판정)=병원 정책, `HANDOVERS.md` 이월.
- **살아있는 갭 3건 = 밤샘 블라인드 대상 아님**: #128 의사 도착화면(의도적 이연, SHELL-NAV 모델과) · 백엔드 계약갭 2건(백엔드) · SEARCH-LOG-06(정책). 정본 = 실행플랜 §9.
