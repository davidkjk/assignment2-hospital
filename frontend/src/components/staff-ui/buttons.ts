/** 주 버튼 / 보조 버튼 클래스 (일관 사용).
 *  데모 `text-primary-foreground`는 실 토큰에 미별칭 → `text-white`로 대체(딥틸 위 흰 글자). */
export const btnPrimary =
  'inline-flex items-center gap-1.5 rounded-lg bg-primary px-3 py-2 text-sm font-medium text-white shadow-sm hover:bg-primary/90 disabled:opacity-50'
export const btnGhost =
  'inline-flex items-center gap-1.5 rounded-lg border border-border bg-card px-3 py-2 text-sm font-medium hover:bg-muted'
export const btnLink = 'text-xs font-medium text-primary hover:underline'
