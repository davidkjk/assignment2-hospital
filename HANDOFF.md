# 핸드오프 메모

## 진행 상태 (갱신: 2026-07-30)

**요구사항 4.3(마감 후 취소→상담 연결) 재설계 반영 완료.** A~F 6개 문서 전부 커밋됨:

- **A(4단계 스펙)** `1b7828e`: 사전문진_카드보내기/예약취소_카드보내기 신규 도구, RAG 실패 재시도, 가족 도구 확장.
- **B(4단계 계획)** `3bab9c0`: Task 9(도구 5→7개), late_cancellation_ticket_service, Task 10/11(RAG 재시도), Task 18/19(카드 컴포넌트), Task 21(옛 프리필 방식 폐기).
- **D(3단계 스펙)** `c4aaca5`: 마감 후 취소 버튼→챗봇 리다이렉트, 배지 문구 정정, 사전문진 섹션 5 서술 갱신.
- **F(3단계 계획)** `c92a2c7`: Task 20 Flutter 챗봇 리다이렉트, Task 27 삭제, Task 21에 `loadExistingAnswers` 추가.
- **E(2단계 스펙)** `c44ab5c`: 섹션 10 문구 정정 — 대기열은 4.3의 유일한 완결점이 아니라 채널 무관 수렴점.
- **C(배포계획)** `582c854`: `scenario-checklist.md` 상담봇 갈래별(안내/문진/행동형3/인계/RAG재시도) 시나리오 구체화, `seed_demo.py`에 상담 대화 예시 7건 추가.

**환자 노출 문구 원칙**(전 단계 공통): "취소 요청이 접수/등록됐다" 표현 금지, "상담(직원 확인)으로 연결됐다"만 사용.

## 진행 중: 요구사항 원문 vs 스펙 문서 한 줄씩 대조 검토 (2026-07-30)

사용자가 `docs/고객요구사항.txt`를 절별로 붙여가며 스펙 문서(`staff-web-design.md`, `ai-chatbot-design.md` 등)와 한 줄씩 대조 중. 지금까지 3.9절(상담 문의 관리)까지는 전부 반영 확인 완료. **3.10절(운영 통계)에서 갭 2건 발견, 아직 스펙에 미반영:**

1. **"오래 기다린 사례"(개별 건수) 통계 없음** — `staff-web-design.md:85` 운영 통계에는 "평균 대기시간"만 있고, 기간별로 "오래 기다린 사례가 몇 건 있었는지" 집계가 빠져 있음. 실시간 화면(3.2 오늘의 현황)에는 "오래 대기 환자" 카드가 있지만, 이건 기간 통계가 아님.
   - **결정**: "평균 대기시간" 옆에 "기준시간(예: 30분) 초과 사례 건수" 통계 추가.
2. **"많이 들어온 질문"(전체 FAQ 순위) 범위 축소** — 요구사항은 "자주 들어오는 질문 전체"를 원하는데, 스펙엔 "미해결 질문 모아보기"(`ai-chatbot-design.md:298,306`)만 있어서 `no_answer`/`repeated` 사유로 **답을 못 찾은 질문만** 집계됨. 답을 잘한 질문이 반복돼도 집계 안 됨.
   - **결정**: 답변 성공 여부와 무관하게 전체 챗봇 질문을 대상으로 "가장 많이 들어온 질문 TOP N" 통계를 신규 추가. 미해결 질문 모아보기와 동일한 방식(질문 임베딩 유사도 클러스터링)을 재사용해 비슷한 질문끼리 자동으로 묶어 건수 내림차순 표시.

이후 절(3.11~ 또는 4장 등)도 이어서 대조 예정.

**4.5절(나의 예약과 방문 상태) 대조 완료, 갭 1건 발견(결정됨):**

- 요구사항 원문은 홈 화면 "가장 가까운 예약" 카드에 "변경·취소" 버튼을 항목으로 나열하지만, `patient-app-design.md:66`의 카드 구성 요소 목록(일시/진료과/의사/병원위치/확정여부/사전문진 작성여부/QR·예약번호)엔 변경·취소가 빠져 있음. 변경·취소 로직 자체는 line 52~55에 상세히 있으나, 그게 홈 카드에서 바로 노출되는지 `/appointments/:id` 상세화면에서만 제공되는지가 스펙상 불명확.
- 나머지 항목(도착확인/대기/내앞대기인원/진료중/진료완료/"예상 대기시간은 변동될 수 있습니다" 문구)은 전부 반영 확인됨(line 68~69, ERD `queue_position`/`patient_queue_position_ahead_count()` RPC).
- **결정**: 홈 카드 구성요소 목록에 "변경·취소" 버튼을 추가한다. 홈 카드 버튼(빠른 실행)과 `/appointments/:id` 상세화면은 배타적이지 않음 — 카드 버튼을 누르면 상세화면/확인 절차로 이어지는 구조로 유지. 다음 반영 대상: `patient-app-design.md:66` 카드 구성요소 목록에 "변경·취소" 추가.

**4.6절(방문 이력) 대조 완료, 갭 1건 발견(결정됨):**

- 문장1(과거 방문일/진료과/의사/공개 안내 조회)은 완전 반영 확인 — `patient-app-design.md` 섹션 7, `patient_visible_notes` 뷰 분리 설계까지 포함.
- 문장2 "의사가 진료 완료 시 환자 공개용 안내를 구분해서 작성" 부분: **기능은 실행계획엔 있는데 설계 문서 화면 설명에 빠짐.** `staff-web-design.md` 섹션 6(의사 진료 화면)의 "작성란" 설명이 "증상/진단/처치 + 문구 불러오기 + 임시저장/완료 버튼"으로만 적혀 있어 환자 공개용 안내문 입력란이 언급 안 됨. 반면 실행계획(`staff-web.md:1444`, `foundation-auth-data-model.md:1726` 등)의 `saveDraft`/`reviseRecord` API에는 `patient_visible_notes` 필드가 이미 존재 — 기능·DB·API는 다 있고, 상위 설계 문서 문장만 누락된 상태.
- **결정**: `staff-web-design.md` 섹션 6에 "환자 공개용 안내문(`patient_visible_notes`, 완료 시 환자 앱 노출)" 한 줄 추가(코드 변경 불필요·문서만 보완).

**4.4절(사전문진) 대조 완료, 갭 1건 발견(결정됨):**

- 6개 문항(증상/시작시점/복용약/알레르기/임신가능성 등 확인항목/의사에게 전할 말), 진료과별 질문 커스터마이징, 진료 전까지 수정 가능, 의사 화면 확인, "챗봇이 대화 중 받은 내용은 저장 전 재확인" 요구는 모두 반영 확인.
- **갭**: 챗봇 `사전문진_카드보내기`가 자동으로 채워주는 항목은 `ai-chatbot-design.md:238`에 "증상·시작 시점·복용약·알레르기" **4개뿐**으로 명시돼 있음 — "임신 가능성 등 확인항목"과 "의사에게 전할 말"은 챗봇 자동 프리필 대상에서 빠짐(환자가 앱 화면에서 직접 입력하는 건 가능, 챗봇 자동수집 범위만 좁음).
- **결정**: 현행 유지 — 4개(증상/시작시점/복용약/알레르기)만 챗봇 자동수집, "임신가능성 등 확인항목"·"의사에게 전할 말" 2개는 항상 환자가 앱 화면에서 직접 입력. 이유: 임신가능성은 민감 항목, 의사전달말은 자유서술형이라 챗봇이 잘못 해석해 자동 반영하면 오히려 위험.

**5.2절(상담봇이 처리할 문의) 대조 완료, 갭 1건 발견(결정됨):**

- 9개 항목 중 "진료시간·휴진일/진료과·의사 안내/예약 변경·취소 규칙/현재 예약 확인(로그인)/가능한 예약시간 조회" 5개는 도구(`진료과의사_조회`/`내예약_확인`/`예약가능시간_조회`) 또는 RAG 처리 대상 예시(line 195)로 명확히 반영 확인.
- **갭**: "대중교통", "예약 방법(절차)", "준비물", "병원 이용 안내" 4개 항목은 `ai-chatbot-design.md`에 예시로 전혀 언급 안 됨. KB `category`가 진료시간/위치·주차/예약규칙/검사준비/기타 5개 고정값(line 89)이라 관리자가 해당 카테고리로 문서만 등록하면 기능적으로는 답변 가능(콘텐츠 기반 RAG라 카테고리 구조 자체는 포괄적) — 즉 기능 갭이 아니라 **spec의 예시 목록 불완전성** + 데모 KB 시드 데이터 작성 시 누락 위험.
- **결정**: `ai-chatbot-design.md:195`의 "처리 대상" 예시 목록에 5.2절 9개 항목(대중교통/예약방법/준비물/병원이용안내 포함)을 빠짐없이 나열(문서만 보강, 기능 변경 없음).

**6.1절(정보 정합성) 대조 완료, 갭 1건 발견(결정됨, 우선순위 높음 — 개인정보 접근제어):**

- "앱 예약→웹 즉시 반영" / "직원 변경→앱 반영" / "상담봇 예약도 동일 관리" / "직원 도착처리→앱 방문상태 반영" 4개 항목은 모두 반영 확인 — `appointments` 테이블 공유 + Supabase Realtime 구독(`staff-web-design.md:19,93`, `patient-app-design.md:21,68`), 상담봇도 3단계 예약 API를 그대로 직행 호출(`ai-chatbot-design.md:247`).
- **갭**: "사전문진은 해당 의사만 진료 화면에서 볼 수 있어야 한다" — `foundation-auth-data-model-design.md:29~33`의 RLS 정책 목록엔 `medical_records`(진료기록)를 "본인이 작성한 것만" 제한하는 정책만 있고, **`questionnaire_responses`(사전문진 답변)를 담당 의사로 한정하는 RLS 정책이 문서에 없음**. 접수직원이나 다른 진료과 의사도 조회 가능한 상태로 방치돼 있을 위험 — 개인정보 관련이라 다른 갭보다 우선순위 높게 볼 것.
- **결정 및 반영 완료(2026-07-30)**: `foundation-auth-data-model-design.md:31` 아래에 "의사: 본인이 담당하는 예약(appointment)의 사전문진(`questionnaire_responses`)만 조회 가능, 다른 과 의사·접수직원은 조회 불가 (요구사항 6.1)" RLS 정책 문장 추가 완료.

**6절 "직원용 웹" 대조 완료, 갭 1건 발견(결정됨):**

- "오늘 처리할 환자·문제 우선 표시"(섹션 2 `/today` "지금 처리할 것" 카드), "색뿐 아니라 글자로도 상태 구분"(섹션 3 line 43, 요구사항 원문 그대로 인용), "의사 화면 3단 레이아웃으로 환자정보·과거기록·오늘기록 한눈에"(섹션 6) 3개는 반영 확인.
- **갭**: "접수 직원이 빠르게 쓸 수 있도록 검색과 키보드 입력이 편해야 한다" — 환자 검색 자체는 있으나(line 75), 자동완성/단축키/Tab 이동/Enter 처리 등 "빠른 입력" UX 규정이 `staff-web-design.md` 전체에 전혀 없음.
- **결정**: 세세한 규칙(자동완성 후보 개수, Tab 순서 등)은 지금 스펙에 못박지 않고 구현 단계에서 확정. 단, 완전히 누락되지 않도록 `staff-web-design.md` 환자 검색 관련 섹션(:75 인근)에 "빠른 입력 UX 필요(예: 자동완성, 단축키, Enter로 다음 필드 이동 등) — 구현 시 확정" 한 줄 메모만 남겨 나중에 잊지 않도록 함.

**5.3절(진료과 선택 도움) 대조 완료, 갭 없음** — `ai-chatbot-design.md` 섹션 3 "갈래② 문진 체인"(199~204행)에 6개 원칙(진단 금지/약·치료법 추천 금지/확정적 표현 금지/최종 선택은 환자/응급 표현 시 119·응급실/긴급판단 완벽보장 표현 금지) 전부 반영. 응급 감지는 AI 호출 전 규칙 기반 ⓪단계로 별도 분리해 놓치는 경우를 구조적으로 차단.

**6.2절(중복·실수 방지) 대조 완료, 갭 없음** — 5개 항목 전부 반영. DB UNIQUE 제약(`doctor_id, date, start_time`)·조건부 UPDATE·`VALID_TRANSITIONS`+DB 트리거·낙관적 잠금(409)·동명이인 재확인(접수직원 화면 한정, 근거 명시)까지 "프론트 1차 방어 + DB가 최종 심판" 이중 구조로 설계됨.

**6.5절(개인정보) 대조 완료, 갭 1건 발견(결정됨):**

- 로그인 미인증 차단(RLS)/역할별 접근 분리/비밀키 미노출(`.env.example`, 배포 전 유출 스캔)/테스트 시 실제 환자정보 금지(`seed_demo` 가상 인물) 4건은 전부 반영 확인, 특히 뒤 2건은 `deployment-design.md`에 "요구사항 6.5"로 명시 태그까지 되어 있음.
- **갭**: "관리자도 실제 비밀번호를 못 봄" 문장이 어느 설계 문서에도 없음. 기능상으론 이미 충족(비밀번호는 Supabase Auth가 해시로만 저장 — 이 시스템 관리자 화면엔 애초에 평문을 보여줄 경로가 없음)이지만, 요구사항과 대조할 근거 문장이 문서에 없음.
- **결정**: `foundation-auth-data-model-design.md` 섹션 1에 "Supabase Auth가 비밀번호를 해시로만 저장하므로 관리자 포함 누구도 평문 조회 불가(요구사항 6.5)" 한 줄 추가(코드 변경 불필요).

**7절(디자인 요청 — 환자용 앱) 대조 완료, 갭 1건 발견(결정됨):**

- "핵심 행동 1개"/"큰 글씨·버튼"/"홈 화면에 예약이 의료진 사진보다 먼저"는 `patient-app-design.md:66,86`에 반영 확인.
- **갭**: "예약 변경과 취소처럼 중요한 행동은 실수하지 않도록 한 번 더 확인" 요구가 앱 자체 흐름에 없음. `:52~55`(변경/취소 로직)엔 확인 팝업 언급이 없고, 마감 전 취소는 "**즉시** 환자취소로 전환"이라고 돼 있어 확인 없이 바로 실행됨을 시사. 문서 전체에서 "확인 팝업"은 회원탈퇴(`:36`)에만 있음. (챗봇 채널의 취소는 `예약취소_카드보내기`의 확인 카드로 이미 확인 단계가 있음 — 이번 갭은 앱 자체 화면 한정)
- **결정**: 앱의 변경/취소 버튼에도 확인 다이얼로그("정말 취소하시겠습니까?") 추가(요구사항 문구가 예시로 직접 지목한 항목).

**시나리오 2(상담봇을 통한 예약, 8장) 대조 완료, 갭 1건 발견(결정됨):**

- 단계별(병원 이용 문의→갈래① RAG / 진료과 안내→갈래② 문진체인+`진료과의사_조회` / 시간 안내→갈래③ `예약가능시간_조회` / 최종확인→`예약제안_카드보내기` [확정]→3단계 예약 API 직행 / 앱 "나의 예약" 표시→같은 `appointments`+Realtime)는 전부 스펙에 반영되어 있고, 라우터가 매 메시지 재분류하는 구조(line 183)라 갈래 전환 자체는 아키텍처상 지원됨.
- **갭**: `deployment.md` Task 20(line 2439~)의 시나리오 2·7 검수 대본은 안내형/문진형/행동형 3건을 **각각 따로** 테스트함. 행동형 "신규예약흐름" 테스트는 "내과 예약하고 싶어요"처럼 **진료과를 이미 아는 채로 시작** — 고객이 요구한 "진료과 모름→문진체인 추천→그 추천으로 바로 예약까지 이어지는" **갈래 간 연계(문진→행동형) 연속 시나리오는 검수 대본에 없음**. 각 갈래 개별 통과와 별개로, "문진 체인이 추천한 진료과 정보가 행동형 체인으로 실제로 넘어가는지"는 명시 검증 대상이 아님.
- **결정**: `deployment.md` Task 20 시나리오 2·7 목록에 "문진→행동형 연계 1건"(증상 호소로 시작 → 문진체인 진료과 추천 → "그 과로 예약해주세요" → 행동형이 이어받아 예약 완료) 테스트 케이스 추가.

**"상담봇" 절(UI/화면 요구사항) 대조 완료, 갭 2건 발견(결정됨):**

- "자연스러운 채팅+무엇을 안내 중인지 알 수 있음"(`:270` 진단 배너)과 "예약 후보는 날짜·시간 버튼"(`:269` 카드형 메시지, `:216` `예약제안_카드보내기`)은 반영 확인.
- **갭①**: "의료 안내와 일반 병원 안내를 시각적으로 구분"이 스펙에 없음. 있는 건 "진단 vs 진료과 안내" 배너(270행, 5.3 안전장치용)뿐이라, "의료적 내용(검사 전 준비사항 등) vs 일반 병원 운영 정보(진료시간·위치·주차 등)"를 색·아이콘·라벨로 구분하는 화면 규칙은 섹션 4 어디에도 없음.
- **갭②**: "직원 연결 상태·예상 답변 시간 표시"가 인계 시 1회성 문구("담당 직원이 확인 후 안내", 234행)뿐. 티켓 상태(`pending`/`in_progress`/`answered`)는 직원 웹 화면(281행)에만 실시간 노출되고, **환자 쪽 화면(266~277행)엔 이 상태를 보여주는 요소가 없음** — 환자는 인계 후 "직원이 지금 확인 중인지"를 계속 볼 방법이 스펙상 없음. 예상 답변 시간도 업무시간 외 "다음 영업일"이라는 대략적 안내만 있고 업무시간 내 케이스엔 없음.
- 둘 다 백엔드 데이터(`support_tickets.status`)는 이미 있어 구현 난이도는 낮음 — 환자 화면에 상태 배지 하나, 시각 구분 규칙 한 줄 추가 수준.
- **결정**: `ai-chatbot-design.md` 섹션 4의 "환자 앱 AI 상담"/"웹 상담창" 항목에 (a) 티켓 상태 배지, (b) 예상 답변 시간 문구, (c) 의료/일반 안내 시각 구분 규칙 3줄 추가.

**시나리오 6(의사 일정 변경) 대조 완료, 갭 없음** — 관리자 특정일 휴진 지정(`staff-web-design.md:79`) → 영향받는 예약 경고+선택 컴포넌트(섹션3과 공유, `:80`) → 환자별 변경/취소 처리(섹션3 사이드패널) → 앱 반영(Realtime 공유) + 알림 발송("변경/병원취소" 대상, `patient-app-design.md:81`) 4단계 전부 확인.

**시나리오 10(하루 운영 보고) 대조 완료, 갭 1건 발견(결정됨):**

- 예약/방문/취소/부도/평균대기시간은 `/today`(실시간) + `/admin/stats`(기간별, `staff-web-design.md:85`)에 반영. 상담봇 문의·직원연결 현황은 `/admin/stats`에 안 합쳐지고 `ai-chatbot-design.md:320` "상담봇 처리 현황" 별도 화면으로 제공(내용 자체는 충족).
- **갭①**: `/admin/stats`엔 CSV 다운로드 버튼이 있는데(`:86`), "상담봇 처리 현황" 화면엔 다운로드 기능이 스펙에 전혀 없음(`ai-chatbot-design.md` 전체에 CSV/다운로드 언급 없음). 요구사항은 확인한 목록 전체의 다운로드를 요구.
- **갭②**(3.10절 재대조 중 추가 발견): `/admin/stats`는 "숫자 클릭 시 해당 목록으로 이동"이 명시(`:86`)돼 있는데, "상담봇 처리 현황"은 "숫자 카드 수준"(`ai-chatbot-design.md:320`)이라고만 돼 있어 클릭 시 상세 목록(예: 인계 건수 클릭 → 해당 티켓 목록)으로 이동하는 기능이 있는지 불명확. 갭①과 원인 동일 — 상담봇 통계 화면이 `/admin/stats`와 같은 수준의 상호작용(다운로드·드릴다운)을 갖추지 못함.
- **결정**: 상담봇 처리 현황 화면에 (a) CSV 다운로드 버튼, (b) 숫자 카드 클릭 시 관련 목록으로 이동하는 기능 2가지를 `/admin/stats`와 동일 패턴으로 추가.

**시나리오 9(권한 확인) 대조 완료, 갭 1건 발견(경미, 결정됨):**

- "접수직원 진료기록 임의 수정 불가"는 설계(`foundation-auth-data-model-design.md:30`)+테스트 이중 확인(`test_scenario_09_role_boundaries_at_api_level`의 403 + `test_scenario_09_rls_blocks_direct_table_access`의 RLS 직접조회 차단). "관리자는 계정·운영현황 확인 가능"은 `test_scenario_10_stats_reflect_created_appointments`에서 `admin_h`로 `GET /stats` 정상 응답 확인. 둘 다 반영 확인.
- **갭**: "의사는 다른 의사의 직원 관리 메뉴를 사용할 수 없다" 테스트가 `POST /staff`(직원 초대, 쓰기) 403만 확인하고, `GET /staff`(목록 조회, 읽기) 자체를 의사 토큰으로 시도해 막히는지는 `test_scenario_09_permissions.py`에 없음. 요구사항 원문은 "메뉴를 사용할 수 없다"는 포괄적 표현이라 조회 차단도 포함해야 더 정확.
- **결정**: `test_scenario_09_permissions.py`에 "의사 토큰으로 `GET /staff` 조회 시도 → 403" 케이스 추가.

**3.7절(진료과·의사 일정 관리) 대조 완료, 갭 1건 발견 → 결정됨(반영 필요):**

- 나머지 7개 항목(진료과 추가·수정·중지/의사별 요일·시간/슬롯 기본시간/점심시간/특정일 진료시간 변경/하루 최대 예약 인원/예약마감시간/저장 전 경고)은 전부 반영 확인.
- **갭**: "휴진일"이 `doctor_schedule_rules`(요일별 정규 스케줄)에는 반복 필드가 없고, "특정일 예외"에서 날짜를 매번 하나씩 등록해야 하는 구조. "매주 수요일 휴진"처럼 반복되는 휴진 요일을 관리자가 매번 개별 날짜로 등록해야 해서 번거로움.
- **결정**: 반복 가능하게 만들기로 함 — `doctor_schedule_rules`에 요일별 "정기 휴진 여부" 플래그(예: `is_day_off boolean`)를 추가해, "의사별 스케줄" 탭(섹션 7)에서 요일별로 "휴진"을 체크할 수 있게 한다. "특정일 예외"는 그대로 남겨 "이번 주만 특별히 하루 더 쉬는" 등 비반복 케이스를 처리.
- 다음 세션 반영 대상: `foundation-auth-data-model-design.md`(`doctor_schedule_rules` 테이블 정의에 `is_day_off` 필드 추가) + `staff-web-design.md` 섹션 7("의사별 스케줄" 탭 설명에 요일별 휴진 체크박스 반영) + 실행계획(`foundation-auth-data-model.md`/`staff-web.md`)의 관련 폼·API에도 필드 반영 필요.

**3.1절(로그인·직원별 권한) 대조 완료, 갭 없음** — 모든 직원 로그인 필수(RLS 기반 구조)/역할별 사이드바 메뉴(`:24`)/관리자의 직원·의사 계정 생성·중지(`/admin/staff`)/의사 자가입 불가/30분 무활동 자동 로그아웃/전화번호·생년월일 목록 마스킹/`access_audit_log`(요구사항 3.1 태그) 7개 전부 반영.

**3.4절(환자 도착·대기 관리) 대조 완료, 갭 없음** — 9개 예약 상태(요구사항 문구 순서 그대로 `status` 컬럼에 정의, 요구사항 3.4 태그)/워크인 등록(전화번호·생년월일 조회 또는 신규등록→담당의사 선택)/대기순서 조정+변경자·사유 기록(`appointment_status_history`)/응급표시(`is_urgent_flag`, "의학적 응급도 판정 대신하지 않음" 요구사항 문구 그대로 인용) 전부 반영.

**3.8절(병원 안내·상담봇 지식 관리) 대조 완료, 갭 2건 발견(결정됨, 2026-07-30):**

- 핵심 흐름(관리자 승인 전 비공개 `status: draft/approved/archived`, 승인 시 자동 재임베딩, 이전 내용·수정이력 `kb_document_revisions`)과 위치·주차/예약규칙/검사준비/답하면 안 되는 내용(`is_restricted`)은 전부 반영 확인, 다수가 "요구사항 3.8" 태그.
- **갭①**: "진료과와 의사 소개"가 KB `category`(5개 고정값: 진료시간/위치·주차/예약규칙/검사준비/기타) 어디에도 없음 — 대신 `진료과의사_조회` 도구가 실시간 DB를 조회해 답하는 구조라 관리자는 KB가 아니라 `/admin/schedule` 화면에서 관리. 기능은 오히려 더 견고(글 안내문이 실제 운영과 어긋날 위험 없음)하지만 요구사항이 말한 "하나의 지식관리 화면" 그림과는 다름.
  - **결정**: 하이브리드로 병행한다 — ① 실시간 정보(오늘 진료 여부·남은 예약 슬롯·스케줄 변경 등 자주 바뀌는 값)는 지금처럼 `진료과의사_조회` 도구가 DB를 그대로 조회해 답변(변경 없음). ② 소개성 정보(의사 전공·경력·인사말, 진료과 소개 문구처럼 자주 안 바뀌고 글로 쓰는 게 자연스러운 내용)는 KB에 신규 category "진료과·의사소개"를 추가해 관리자가 직접 작성·승인하는 방식으로 관리.
- **갭②**: "자주 묻는 질문"도 전용 category가 없어 "기타"에 흡수되거나 기존 카테고리에 나눠 들어갈 것으로 보임 — 5.2절에서 지적한 category 목록 불완전성과 같은 뿌리.
  - **결정**: KB에 "자주묻는질문" category를 추가한다. 이번 세션 3.10절 갭2 결정으로 "전체 질문 순위 통계"(임베딩 유사도 클러스터링 기반)를 별도로 만들기로 했지만, 그건 "관리자가 사후에 통계로 보는" 용도이고, 이 category는 "관리자가 자주 묻는 질문을 직접 정리해서 안내문으로 미리 등록해두는" 용도라 역할이 다름 — 통계 확인 후 여기에 등록하는 흐름으로 연결.

**1.3절(의사) 대조 완료, 갭 없음** — 오늘 예약/대기 환자, 과거 진료기록, 사전문진, 진료기록 작성 4개 요소 모두 `staff-web-design.md:67~75`(의사 진료 화면 3단 레이아웃)에 반영 확인.

**3.5절(환자 정보) 대조 완료, 갭 1건 발견(미확정 — 경계 사례):**

- 8개 항목 목록은 기존 확인 내용과 동일(갭 없음, 상담봇 인계내용 탭만 2단계때 누락됐다가 보완된 이력 있음). "생년월일·연락처 함께 표시"도 `:61,64`에 반영 확인.
- **갭(불명확)**: "저장 전에 이름과 생년월일을 다시 확인" 요구 — 동명이인 재확인 장치 자체는 있으나(`:75`) "접수직원 화면(캘린더 모달/워크인 등록)에만 적용"으로 범위가 못박혀 있고, 의사화면은 명시적으로 재확인 없음(`:71`). 정작 **환자 상세화면(`/patients/:id`) 자체에 어떻게 진입하는지(이름 검색 등 별도 경로가 있는지)가 스펙에 안 나와 있어**, 그 경로에 재확인이 필요한지 여부를 판단할 근거가 없음. 라우트 목록(`:25`)엔 `/patients/:id`만 있고 검색 목록 화면이 없음.
- **결정**: `staff-web-design.md`에 전역 환자 검색 화면/기능(이름·생년월일로 검색 → 결과 목록 → `/patients/:id` 이동)을 신규 명시하고, 그 검색 결과 선택 시점에도 동명이인 재확인 팝업을 동일하게 적용.

## 다음 액션

**갭 17건(3.10절 2건, 3.8절 2건 포함) 전부 사용자 결정 완료(2026-07-30).**

**Group A(DB/스키마) 완료(2026-07-30):**
- 6.1 RLS(사전문진 담당의사 한정) — 스펙 문구 커밋 완료(`bd0fe96`, 코드는 `supabase/migrations/00007_questionnaire.sql`에 이미 정상 반영돼 있었음)
- 3.7 `is_day_off`(정기 휴진 요일 반복설정) — spec(`foundation-auth-data-model-design.md`) → plan(Task 18) → 마이그레이션 `00009_doctor_schedule_day_off.sql` → 스키마 테스트 2건 전부 TDD로 반영, 백엔드 전체 테스트 97개 PASS 확인, 커밋 완료(`b213974`)
- Group A의 나머지 2단계 UI 반영분(휴진 체크박스 화면)은 Group B(`staff-web.md`) 몫으로 이월

**Group B(직원웹) 완료(2026-07-30):**
- 스펙(`staff-web-design.md`) 3.5/3.7/3.10 반영 커밋(`bc2b314`): `/patients` 전역 환자검색 화면 신설(섹션 4.5), "의사별 스케줄" 탭 정기휴진 체크박스, `/admin/stats`에 오래대기 사례 건수
- 계획(`staff-web.md`) Task 23~25 추가 커밋(`53bbc00`): Task 23(휴진 슬롯 생성 제외), Task 24(전역 환자검색 백엔드+화면), Task 25(오래대기 통계 집계)
- 2단계는 `frontend/`·`schedule_service` 등 실제 코드가 아직 없어 TDD 미실행 — 계획 문서에 절차만 코드로 명시. 실제 구현 시 `subagent-driven-development`/`executing-plans`로 Task 1~25 순서 실행 필요

**Group C(환자앱) 완료(2026-07-30, 커밋 `5aaee12`):**
- 스펙(`patient-app-design.md`) 섹션 4/6 반영: 취소 확인 다이얼로그("정말 취소하시겠습니까?") 문장 추가, 홈카드 구성요소에 변경·취소 버튼 추가
- 계획(`patient-app.md`) Task 22 홈 화면 예시 코드에 변경/취소 버튼 2개 추가(새 로직 없음 — `/appointments/:id`로 이동만, 실제 확인 다이얼로그·변경 로직은 Task 20에 이미 존재)
- 3단계는 `mobile/` 코드가 아직 없어 TDD 미실행 — 실제 구현 시 Task 20~22 절차대로 진행

**Group D(챗봇) 완료(2026-07-30, 커밋 `2722183`/`076850b`):**
- 스펙(`ai-chatbot-design.md`) 반영: 티켓 상태 배지·예상 답변시간·의료/일반 시각구분(섹션4), CSV 다운로드·드릴다운(섹션4 상담봇 처리 현황), `chat_messages.question_embedding` + "전체 질문 순위" 관리자 화면 신설(섹션2/4), `kb_documents.category`에 "진료과·의사소개"(하이브리드 원칙 포함)·"자주묻는질문" 추가(섹션2)
- 계획(`ai-chatbot.md`) Task 22~25 추가: Task22(답변시각구분+티켓상태 API, `rag_chain`/`chat_service`/`ticket_service`/`chat.py` 수정), Task23(CSV export+드릴다운, `bot_stats_service`/`admin_kb.py`/`BotStatsPage`), Task24(`chat_messages` 마이그레이션 직접수정 — Task1 파일에 `question_embedding` 컬럼 추가 + `question_cluster_service.list_top_questions`), Task25(KB category 프론트 선택지만 추가, DB 제약 없어 백엔드 변경 불필요)
- 4단계는 `backend/app/services/chat_service.py` 등 코드 자체가 아직 없어 TDD 미실행 — 실제 구현 시 Task 1~25 순서대로(Task24는 Task1 마이그레이션을 직접 수정하므로 Task22보다 먼저 실행 권장, 계획 문서 "실행 순서와 의존 관계" 절 참고)

**다음 세션: Group E(검수·테스트, `deployment.md`)부터 시작.** 대상: 시나리오2 문진→행동형 연계 테스트 케이스 추가(Task 20), 시나리오9 `GET /staff` 403 테스트 추가(예시 코드 단계). 이후 Group F(가벼운 스펙 문장 보강 — 4.6/5.2/6.5/직원웹UX메모, plans 변경 불필요)로 마무리하면 갭 17건 전부 문서 반영 완료.

**참고**: 로컬 Supabase가 이번 세션에서 시작됐고(`supabase start`), `backend/.env`·`backend/.venv`가 새로 생성됨(둘 다 gitignore 대상, 커밋 안 됨). 다음 세션에서 백엔드 테스트를 돌리려면 `supabase status`로 살아있는지 먼저 확인.

**구현 단계(코드 존재 여부) 확인 결과(2026-07-30 검증):** 실제 코드가 존재하는 건 1단계(`backend/`, `supabase/migrations/`)뿐. 2~4단계(직원웹/환자앱/챗봇) 코드는 디렉토리조차 없음 — 전부 문서 단계. `test_scenario_09_permissions.py`도 아직 실존 파일이 아니라 `deployment.md` 안의 예시 코드일 뿐(검증완료).

**작업 종류별로 쓸 스킬을 명확히 구분한다 — 셋을 섞지 말 것:**

| 수정 대상 | 사용할 스킬 | 적용 시점 |
|---|---|---|
| **specs 문서**(`docs/superpowers/specs/*-design.md`) — "무엇을 하는가" | `superpowers:brainstorming` | 문장·조항을 고치기 전에 먼저 실행 |
| **plans 문서**(`docs/superpowers/plans/*.md`) — "어떤 화면·API·DB 필드를 어떤 작업 단위로 만드는가" | `superpowers:writing-plans` | specs 반영 후, Task를 새로 추가/수정하기 전에 먼저 실행 |
| **실제 코드**(마이그레이션·백엔드·프론트, 지금은 1단계뿐) | `superpowers:test-driven-development` | plans의 Task를 실행할 때, 실패하는 테스트부터 작성 |

이 셋은 각각 별도 단계이며, 순서상 specs → plans → 코드 순으로 내려간다. 여러 서브에이전트를 병렬로 띄우게 될 경우, 전역 지침(`~/.claude/CLAUDE.md` "서브에이전트 실행 규칙")에 따라 **개수·범위·예상 작업량을 먼저 보고하고 사용자 승인 후에만 Agent 호출**.

**갭별 반영 범위 — 어떤 문서를 어떤 스킬로 고쳐야 하는지:**

| 갭 | specs 수정(brainstorming) | plans 수정(writing-plans) | 실제 코드(TDD) | 비고 |
|---|---|---|---|---|
| 6.1 RLS(사전문진 접근제한) | ✅ 반영 완료(`foundation-auth-data-model-design.md`) | 불필요 | 불필요 — 코드 이미 정상(검증완료) | `supabase/migrations/00007_questionnaire.sql`의 `assigned_doctor_can_read_responses`가 이미 담당의사·관리자로 제한 중 |
| 4.5 홈카드 변경·취소 버튼 | ✅ 완료(`patient-app-design.md` 섹션6) | ✅ 완료(Task 22) | 3단계 미구현, 해당없음 | |
| 4.6 환자공개 안내문 입력란 | 필요(`staff-web-design.md` 섹션6) | 불필요 — 이미 있음 | 해당없음 | `patient_visible_notes`는 plans에 이미 존재 |
| 4.4 챗봇 자동수집 범위 | 변경 없음(현행 유지 결정) | 변경 없음 | 해당없음 | |
| 5.2 챗봇 처리대상 예시 나열 | 필요(`ai-chatbot-design.md:195`) | 불필요 | 해당없음 | 예시 목록 보강뿐 |
| 직원용 웹 빠른입력 UX | 필요(한 줄 메모, `staff-web-design.md`) | 구현 시 확정 | 2단계 미구현, 해당없음 | |
| 6.5 비밀번호 문구 | 필요(`foundation-auth-data-model-design.md` 섹션1) | 불필요 | 해당없음 | |
| 7 변경·취소 확인 다이얼로그 | ✅ 완료(`patient-app-design.md` 섹션4) | ✅ 이미 있었음(Task 20, 2026-07-28 별개 검토) | 3단계 미구현, 해당없음 | |
| 시나리오2 문진→행동형 연계 테스트 | 해당없음 | 필요(`deployment.md` Task 20) | 배포단계 미구현, 해당없음 | |
| 상담봇 UI 티켓상태·시각구분 | ✅ 완료(`ai-chatbot-design.md` 섹션4) | ✅ 완료(Task 22) | 4단계 미구현(코드 없음), TDD는 향후 구현 시 Task 22 절차대로 | |
| 시나리오10 CSV·드릴다운 | ✅ 완료(`ai-chatbot-design.md` 섹션4) | ✅ 완료(Task 23) | 4단계 미구현(코드 없음), TDD는 향후 구현 시 Task 23 절차대로 | |
| 시나리오9 GET /staff 403 테스트 | 해당없음 | 필요(`deployment.md:530,541` 예시 코드) | 배포단계 미구현, 해당없음 | |
| 3.5 전역 환자검색 + 재확인 확대 | ✅ 완료(`staff-web-design.md` 섹션 4.5) | ✅ 완료(Task 24) | 2단계 미구현(코드 없음), TDD는 향후 구현 시 Task 24 절차대로 | |
| 3.10 갭1 오래대기 건수 통계 | ✅ 완료(`staff-web-design.md:85`) | ✅ 완료(Task 25) | 2단계 미구현(코드 없음), TDD는 향후 구현 시 Task 25 절차대로 | |
| 3.10 갭2 전체질문 순위 통계 | ✅ 완료(`ai-chatbot-design.md`) | ✅ 완료(Task 24) | 4단계 미구현(코드 없음), TDD는 향후 구현 시 Task 24 절차대로 | |
| 3.7 휴진일 반복설정(`is_day_off`) | ✅ 완료(`foundation-auth-data-model-design.md` + `staff-web-design.md` 섹션7) | ✅ 완료(1단계 Task 18 + 2단계 Task 23) | 1단계 완료(마이그레이션 `00009`, 테스트 2건, 97개 전체 PASS). 2단계(슬롯생성 반영·체크박스 UI)는 코드 없음, TDD는 향후 Task 23 절차대로 | |
| 3.8 갭① 진료과·의사소개 KB 하이브리드 | ✅ 완료(`ai-chatbot-design.md` KB `category` 목록에 "진료과·의사소개" 추가 + 하이브리드 근거 서술) | ✅ 완료(Task 25) | 4단계 미구현(코드 없음), TDD는 향후 구현 시 Task 25 절차대로 | `진료과의사_조회` 도구(실시간)는 유지, 소개성 글만 KB로 병행 |
| 3.8 갭② 자주묻는질문 KB category | ✅ 완료(`ai-chatbot-design.md` KB `category` 목록에 "자주묻는질문" 추가) | ✅ 완료(Task 25) | 4단계 미구현(코드 없음), TDD는 향후 구현 시 Task 25 절차대로 | 3.10 갭2 "전체질문순위 통계"(사후 확인용)와 역할 다름 — 통계로 파악 후 이 category에 수동 등록하는 흐름 |

**세션별 작업 단위(컨텍스트 관리용) — 문서(파일) 단위로 묶음, 한 세션당 한 그룹 권장:**
- **Group A** DB/스키마(`foundation-auth-data-model.md` + `-design.md`): 6.1 RLS(완료), 3.7 `is_day_off`(Task 18 + TDD)
- **Group B** 직원웹(`staff-web.md` + `-design.md`): 3.5 전역 환자검색, 3.10 갭1 오래대기 건수 통계, 3.7 휴진일 체크박스 UI — ✅ 완료(2026-07-30, 커밋 `bc2b314`/`53bbc00`)
- **Group C** 환자앱(`patient-app.md` + `-design.md`): 4.5 홈카드 변경·취소 버튼, 7 확인 다이얼로그 — ✅ 완료(2026-07-30, 커밋 `5aaee12`)
- **Group D** 챗봇(`ai-chatbot.md` + `-design.md`): 상담봇 UI 티켓상태·시각구분, 시나리오10 CSV·드릴다운, 3.10 갭2 전체질문순위 통계, 3.8 갭①·② KB category 2건(진료과·의사소개 하이브리드, 자주묻는질문) — ✅ 완료(2026-07-30, 커밋 `2722183`/`076850b`)
- **Group E** 검수·테스트(`deployment.md`): 시나리오2 연계테스트(Task 20), 시나리오9 `GET /staff` 403 예시 코드
- **Group F** specs 문장만 가벼운 보강(plans 변경 불필요, 자투리 시간에 처리 가능): 4.6/5.2/6.5/직원웹UX메모
- 3.8절(지식관리 category)은 2026-07-30 결정 완료 — 더 이상 보류 아님(위 표 참고). 갭 17건 전부 결정 완료 상태.
- 요구사항 원문 대조는 3.10절까지 완료. 3.11절 이후(또는 4장 등)는 아직 미착수 — 이어서 진행.
- 위 표의 문서 반영이 모두 끝나면, 구현 착수 시 `superpowers:writing-plans` → `subagent-driven-development`/`executing-plans`로 넘어가기 전에 1~5단계 스펙·계획이 모두 문서 단계이고 아직 코드 구현 전이라는 점 재확인.

## 참고 파일 경로

- 4단계 스펙: `docs/superpowers/specs/2026-07-27-ai-chatbot-design.md`
- 4단계 계획: `docs/superpowers/plans/2026-07-27-ai-chatbot.md`
- 배포 계획: `docs/superpowers/plans/2026-07-27-deployment.md` (Task 9: 데모 시드, Task 21: 최종 검수)
- 3단계 스펙: `docs/superpowers/specs/2026-07-27-patient-app-design.md`
- 3단계 계획: `docs/superpowers/plans/2026-07-27-patient-app.md`
- 2단계 스펙: `docs/superpowers/specs/2026-07-27-staff-web-design.md` (섹션 10)
- 요구사항 원문: `docs/고객요구사항.txt` (4.2/4.3/4.4/4.5/4.6/5장/9장 참고)
