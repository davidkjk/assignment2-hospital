# 핸드오프 메모

## 진행 상태 (갱신: 2026-07-30)

- **A(4단계 스펙) 완료·커밋됨** (`1b7828e`): 사전문진_카드보내기/예약취소_카드보내기 신규 도구, RAG 실패 재시도, 가족 도구 확장, 도구화 원칙, late_cancellation 사유 반영.
- **B(4단계 계획) 완료·커밋됨** (`3bab9c0`): Task 9(도구 5→7개+가족+취소카드+문진카드), late_cancellation_ticket_service+엔드포인트 신규, Task 10/11(RAG 재시도), Task 18/19(CancellationCard/QuestionnaireCard 컴포넌트), Task 21(옛 프리필 방식 폐기 → 예약완료 시 자동 카드 부착으로 대체). 스펙의 `chat_messages.message_type` enum도 함께 동기화.
- **다음 시작점: D(3단계 스펙) → F(3단계 계획) → E(2단계 스펙) → C(배포계획)** — 아래 "다음 액션" 순서 그대로, brainstorming(D,E)/writing-plans(F,C) 스킬 사용.
- 사용자 지시로 이번 세션은 B까지만 진행하고 중단.

## 지금 상태 (이전 세션 스냅샷 — 아래는 A/B 반영 전 브레인스토밍 내용, 참고용으로 유지)

4단계(AI 상담봇) 스펙·계획은 이미 작성 완료 상태에서, 이번 세션은 빠진 부분·개선점을 찾는 브레인스토밍이었음. **문서에는 아직 아무것도 반영 안 됨** (전부 대화로만 확정, 세션 중 스펙 파일을 직접 수정했다가 되돌린 적 있음 — 수정은 반드시 스킬로 한꺼번에 하기로 합의됨. 절대 Edit 도구로 바로 고치지 말 것).

**중요**: 세션 후반에 4단계 범위를 넘어 **2단계(직원 웹)·3단계(환자 앱) 문서까지 고쳐야 하는 것을 발견**함(요구사항 4.3 관련, 아래 "핵심 발견" 참고). 이번 반영은 4단계뿐 아니라 2·3단계 문서도 함께 건드리는 작업임. **사용자가 2·3단계도 이번에 함께 반영하기로 확정함.**

### 작업 원칙: 스펙 문서 수정 시 계획 문서도 항상 같이 확인

이번 세션에서 반복 확인된 패턴 — 스펙(설계) 문서를 고치면, 그 설계를 코드로 구현하는 계획 문서에 반드시 영향이 있었음(A→B, D→F가 그 예). **앞으로 어느 세션에서든 `*-design.md` 스펙 문서를 수정할 때는, 같은 단계의 `*-plan.md` 계획 문서에 그 변경으로 인해 추가/삭제/수정해야 할 태스크가 있는지 그 자리에서 바로 확인**하고, 있으면 함께 반영 대상에 포함시킨다(별도 발견까지 미루지 않음). 이 원칙은 메모리에도 저장해 다음 세션에도 유지됨.

### 반영 방식 확정 (스킬 배정)

- **스펙 문서(A, D, E)** → `superpowers:brainstorming` 스킬로 반영. 이미 결정은 끝났으니, 결정 내용을 정리해 스펙에 쓰는 마무리 단계로 사용.
- **계획 문서(B, F, C)** → `superpowers:writing-plans` 스킬로 반영. 스펙 변경을 코드 수준 구현 태스크와 동기화하는 단계라 이 스킬이 맞음.

### 반영 대상 문서 확정: 원래 A~E 5개 → **6개**로 확장 (F 추가)

D(3단계 스펙: 마감 후 취소 버튼→챗봇 리다이렉트)를 반영하면, **3단계 계획 문서(`docs/superpowers/plans/2026-07-27-patient-app.md`)도 함께 고쳐야 함**을 확인함. 이 문서 Task 9 부근(6001~6254줄)에 "예약 취소" 버튼이 마감 후 `cancellation_requested: true`를 받으면 **앱 화면에서 직접 "취소요청 접수됨"을 보여주는** Flutter 구현 코드·테스트가 이미 계획돼 있음 — 이걸 "챗봇 화면으로 이동"으로 바꿔야 함. 백엔드 `cancel_appointment` 서비스(마감전후 분기 로직)는 그대로 재사용, 변경 없음.

반대로 **E(2단계 스펙)는 계획 문서 변경이 필요 없음** — 스펙 섹션 10은 서술만 정정(기능 변경 없음)이라 대기열 화면 구현(Task 16)은 그대로 유효함.

### 1단계(foundation) 영향 확인 완료: 영향 없음

이미 구현·병합된 1단계 코드에 이번 변경들이 영향을 주는지 확인함:
- A-4(가족 예약 도구)가 재사용하는 `patient_family_links`, `account_patient_id`, `for_patient_id`는 전부 **1단계 계획 Task 4에서 이미 정의·구현된 것**을 그대로 쓰는 것 — 새 스키마 변경 없음.
- `cancellation_requested_at`은 1단계가 아니라 **3단계 계획 Task 2에서 추가되는 컬럼** — 역시 1단계와 무관.
- 결론: A/D/E/F 어느 것도 이미 구현된 1단계 코드나 마이그레이션을 건드리지 않음. 안심하고 진행 가능.

---

## 핵심 발견: 요구사항 4.3 "마감 후 상담으로 연결"이 문서 간 어긋나 있었음

- 3단계(환자 앱) 스펙(07-27)엔 "이건 임시조치, 원래는 상담봇을 거쳐야 하는데 그때 챗봇이 없어서 지름길로 처리했다"고 스스로 적어둠.
- 2단계(직원 웹) 스펙(07-28, 정합성 검토 중 추가)은 그 지름길("취소요청 대기열" 화면)을 만들면서 "요구사항 4.3을 실제로 완결"이라고 적어 **최종 해결책처럼 마무리**해버림 — 3단계의 메모를 놓친 것으로 보임.
- **사용자가 고객 요구사항 원문대로(B안) 가기로 확정**: 마감 후 취소는 앱이 조용히 처리하지 않고, 반드시 챗봇 상담을 거쳐야 함.
- 대기열 화면(승인/반려 기능) 자체는 없앨 필요 없음 — 요청이 들어오는 "입구"만 앱의 조용한 버튼에서 챗봇 대화로 바뀌는 것. 직원은 여전히 같은 대기열 화면에서 처리.
- 퇴근 후 문제 없음: 취소 요청 접수는 직원의 실시간 응대가 필요 없는 비동기 DB 기록이라, 봇이 영업시간 무관하게 즉시 접수 가능(승인/반려만 다음 영업일).

---

## A. 4단계 스펙 문서 반영 대상
`docs/superpowers/specs/2026-07-27-ai-chatbot-design.md`

1. **`사전문진_카드보내기` 도구 추가** (최초안 `사전문진_반영제안`에서 전면 재설계, 기존 계획 Task 21 완전 대체) — 요구사항 4.4 보완("...내용을 보여주고 저장 여부를 다시 확인받아야" + "진료 전까지 내용을 수정할 수 있고").
   - **호출 시점**: (a) 예약 신청(`create_booking`) 완료 시 코드가 무조건 1회 자동 호출(LLM 자율판단 아님 — 반복·누락 방지), (b) 이후 환자가 "사전문진 확인/수정하고 싶어요" 등 요청 시 언제든 재호출(다른 대화 세션이어도 무방, `for_patient_id`/`appointment_id` 기준 조회). 대상 예약이 여러 건(본인+가족)이면 A-4와 같은 패턴으로 먼저 확인.
   - **수집 범위**: 문진 체인(Task 8) 태그 여부와 무관하게 **대화 전체**에서 파악되는 증상/시작시점/복용약/알레르기. 파악 못 한 항목은 빈칸(지어내지 않음).
   - **UI**: 신규 채팅 카드 컴포넌트 `QuestionnaireCard`(`BookingCard`/`ContactForm`과 같은 패턴 — 채팅창 안에 입력 가능한 폼). 각 필드에 파악된/기존 저장값이 미리 채워지고, **환자가 자유롭게 직접 고쳐 쓸 수 있음**. 버튼: [저장](누르면 즉시 반영, 앱·웹 채널 무관 동일 동작) / [나중에 할게요](저장 안 하고 다음에 이어서).
   - **로그인 필요**.
   - **왜 Task 21을 대체하는지**: 기존 Task 21은 (1) 문진 체인 태그 붙은 메시지 앞뒤 1개씩만 봐서 수집 범위가 좁고, (2) 결과를 **앱의 사전문진 화면(Task 27)에서만** 보여줄 수 있어 **웹 상담창(Task 18)에는 편집 화면 자체가 없어 웹 전용 사용자는 사전문진을 아예 못 씀**, (3) 예약신청 시 1회성이라 이후 수정 경로가 없음. 새 도구는 이 세 가지를 모두 해결(범위 확장/채널 공통/반복 편집 가능).
   - **제약사항**: 3단계 스펙 섹션 5("방문·도착 처리 전까지 자유롭게 수정 가능, 도착 이후엔 읽기 전용")를 챗봇 경로에서도 동일하게 지켜야 함 — 환자가 병원 도착 처리된 뒤에는 이 도구도 저장을 거부(앱과 동일한 잠금 규칙, 우회 구멍 방지).
2. **RAG 실패 시 행동형 1회 재시도** — 유사도 매우 낮으면 인계 전에 1회 한정 행동형 재시도. **라우터 재분류가 아니라 코드가 직접 행동형으로 전환하는 하드코딩된 흐름**(무한루프 방지). 행동형→RAG 반대방향 로직 불필요(행동형이 이미 RAG 검색을 포함하는 상위호환 갈래).
3. **도구화 원칙 + KB 문서 작성 원칙 명시** — (a) 조회성/확인버튼 실행만 도구화, 개인정보 생성·타인정보·되돌리기 어려운 행동은 KB 안내문으로 (b) 이미 도구로 처리되는 것엔 경쟁하는 KB 안내문 만들지 않기(RAG 오답 확정 역설 방지)
4. **가족 예약 확인/신규/취소 도구 3종** — `내예약_확인`을 가족까지 확장, `예약제안_카드보내기`도 가족 선택 포함, 신규 `예약취소_카드보내기`(본인+가족 공통). **반드시 기존 데이터 모델 재사용**: `patient_family_links`, `patient_owns()`, `account_patient_id`/`for_patient_id` (정합성 검토 P0 R5-01, P2 R5-02에 이미 정의됨 — 새로 만들지 말 것).
5. **`예약취소_카드보내기` 도구 — 마감 후 취소 요청 자동 분기** — 마감 전=즉시 취소 확정, 마감 후=취소 요청 접수(`cancellation_requested_at`).
   - **[결정 번복]** 세션 초반엔 "기존 취소 API가 마감전후 분기를 이미 처리하니 새 인계 사유는 불필요"라고 정리했으나, 최종 확인 과정에서 **재확정 — 마감 후 취소는 인계 티켓도 함께 생성**하기로 바꿈. 이유: 직원이 대기열 화면(표: 환자명·예약일시만)만으로는 판단 근거가 부족하고, 요구사항 4.3의 "상담으로 연결"이라는 취지를 살리려면 직원이 **대화 맥락(왜 취소하려는지 등 요약)**을 확인한 뒤 처리해야 함.
   - **새 인계 사유 추가**: `support_tickets.reason` CHECK 제약(현재 6종: `no_answer`/`medical_judgment`/`unhelpful`/`data_mismatch`/`complaint`/`repeated`)에 **`late_cancellation`** 추가(7종). 마감 후 분기 시 `cancellation_requested_at`을 채우는 것과 **동시에** 인계 티켓도 생성(요약 필드에 취소 사유 등 대화 요약 포함, `summary_staff_todo`엔 "취소요청 대기열에서 승인/반려 처리 필요" 명시).
   - **문구 원칙(중요, 재정정)**: 요구사항 원문은 "상담으로 연결"이지 "취소 요청 접수"가 아님. 환자에게 보이는 봇 응답은 **"취소 요청이 접수/등록됐다"는 말 자체를 하면 안 됨** — `cancellation_requested_at` 기록·인계 티켓 생성은 내부 처리일 뿐, 환자에게는 오직 "상담(직원 확인)으로 연결됐다"는 사실만 전달. "요청이 들어갔다"는 문구는 환자가 "자동으로 곧 취소되겠지"라고 오해하게 만들어 "직접 취소 못 하게 하고 상담으로 연결하라"는 요구사항 취지와 어긋남.
   - 예시 문구: "상담으로 연결해 드렸습니다. 담당 직원이 내용을 확인한 후 안내해 드립니다(영업시간 외엔 다음 영업일)." (❌ "취소를 요청해 두었습니다", ❌ "취소 요청이 접수되었습니다" 같은 표현 사용 금지)
6. **대기 순번 조회는 도구화 안 함** — 앱에 이미 표시(4.5)돼 중복. KB 안내문 "실시간 대기 순번은 앱에서 확인 가능"으로 대체.

### 최종 도구 목록 (기존 5개 → 7개)

1. `search_hospital_info` (기존)
2. `list_departments_doctors` (기존)
3. `list_available_slots` (기존)
4. `get_my_appointments` (기존, **가족 확장** — A-4)
5. `propose_booking_card` (기존, **가족 확장** — A-4)
6. `예약취소_카드보내기` (**신규** — A-5)
7. `사전문진_카드보내기` (**신규** — A-1, 저장+수정 통합)

## B. 4단계 계획 문서 반영 대상
`docs/superpowers/plans/2026-07-27-ai-chatbot.md`

- **Task 9(에이전트 도구)**: 도구 5개→7개 확장. `get_my_appointments`/`propose_booking_card`에 가족 파라미터 추가, `예약취소_카드보내기`/`사전문진_카드보내기` 신규 구현+테스트. "5개 도구" 단언 테스트 문구 전부 수정.
- **Task 10/11(라우터/오케스트레이션)**: A-2(RAG 실패 시 1회 재시도, 하드코딩된 전환) 반영.
- **Task 18(웹 위젯)/Task 19(Flutter 앱)**: `QuestionnaireCard` 렌더링 컴포넌트 신규 추가 — 웹은 React(`webchat/src/QuestionnaireCard.tsx`), 앱은 Flutter(`mobile/lib/features/chat/questionnaire_card.dart`) 각각 구현.
- **Task 21(문진 프리필 API, `get_questionnaire_prefill` 등)**: **삭제** — A-1 신규 도구(`사전문진_카드보내기`)로 완전 대체.
- writing-plans 스킬 사용(27만 자 분량, 신중하게).

## C. 배포 계획 문서 반영 대상
`docs/superpowers/plans/2026-07-27-deployment.md`

7. **`scenario-checklist.md`(Task 21) 행동형 도구별 질문 구체화** — 안내형1 / 문진형1 / 행동형3(신규예약흐름, "제 예약 확인해주세요", 증상·복약 언급) / 인계1(👎) / RAG실패→행동형 재시도1.
8. **`seed_demo.py`(Task 9)에 상담 대화 예시 데이터 추가** — 갈래별 상담 대화 미리 심어 관리자 통계 화면이 비어있지 않게 함. 강사 언급 "데이터 시딩으로 4단계 실증 검증" 요구사항 대응.

## D. 3단계(환자 앱) 스펙 수정 대상 — 이번에 새로 발견, 4단계 범위 밖
`docs/superpowers/specs/2026-07-27-patient-app-design.md`

9. **마감 후 취소 버튼 → 챗봇으로 리다이렉트** (섹션 4, 53~56번째 줄 전체 재작성)
   - **54번째 줄(마감 전)**: 그대로 유지, 변경 없음.
   - **55번째 줄(마감 후)**: 현재 "`cancellation_requested_at`을 채워 직원용 웹으로 전달 + 앱에 '취소 요청됨 · 직원 확인 중' 배지 표시"를 → "'마감이 지나 상담으로 연결해드립니다' 안내 후 AI 상담 화면으로 이동, 실제 접수·인계 티켓 생성은 4단계 챗봇 도구(A-5)가 담당"으로 교체. 앱이 직접 필드를 채우는 경로는 제거. **배지 문구도 "취소 요청됨" → "상담 연결됨 · 직원 확인 중"으로 수정**(환자에게 "취소 요청이 들어갔다"는 인상을 주면 안 됨 — 아래 원칙 참고). 반려 알림 문구("취소 요청이 반려되었습니다")는 직원이 실제 검토를 마친 뒤의 사후 통보라 유지 가능하나, 일관성을 위해 함께 정리할지는 A(스펙) 반영 시점에 재확인.
   - **56번째 줄(괄호 메모) — 삭제**: "상담봇이 아직 없는 3단계에서는 최종 목적지인 직원 연결로 바로 처리"라는 전제가 이제 거짓(4단계 챗봇이 생김)이므로 이 메모를 남겨두면 문서가 실제 동작과 다시 어긋남. 3단계 스펙을 처음 개발 착수한 시점보다 4단계까지 나중에 만들어지면서 생긴 임시조치 흔적이라, 이번에 완전히 걷어냄.
   - **환자 노출 문구 원칙**: 앱 화면·챗봇 응답 어디에서도 "취소 요청이 접수/등록됐다"는 표현 대신 **"상담(직원 확인)으로 연결됐다"**는 표현만 사용 (A-5와 동일 원칙 — 요구사항 원문이 "상담으로 연결"이지 "취소 요청 접수"가 아니므로, 자동 처리를 암시하면 안 됨).
13. **섹션 5(사전문진) "[해결됨]" 서술 갱신** — (최종 정합성 확인 중 새로 발견) 현재 서술은 옛 설계(`field_key` 태그로 프리필 → 앱 화면에서 제출)를 "해결됨"이라고 적어둠. A-1(사전문진_카드보내기 도구)로 대체됐으므로, "화면 쪽은 Task 27, 대화 추출 쪽은 ai-chatbot.md"라는 옛 역할 분담 서술을 새 설계(챗봇이 채팅 카드로 저장·수정까지 완결, Task 27은 조회·직접작성용 화면으로 축소)로 정정. "도착 이후 읽기 전용" 규칙은 챗봇 도구에도 동일 적용됨을 명시.

## E. 2단계(직원 웹) 스펙 수정 대상 — 이번에 새로 발견, 4단계 범위 밖
`docs/superpowers/specs/2026-07-27-staff-web-design.md`

10. **섹션 10 문구 정정** — "요구사항 4.3을 실제로 완결"이라는 서술이, 대기열 화면 단독으로 4.3이 완결되는 것처럼 읽힘. 정정 핵심: **마감 후 취소 요청은 입구 채널이 무엇이든(앱 취소 버튼→챗봇 리다이렉트, 웹 상담창, 앱 내 챗봇 등) 전부 이 대기열 하나로 귀결되어 직원 승인을 거쳐야 한다**는 게 요구사항 4.3의 본질임을 명시. 이 채널 무관성은 A-5(예약취소_카드보내기 도구)가 앱/웹 어느 채널에서 호출되든 동일한 백엔드 `cancel_appointment` API를 호출해 동일한 `cancellation_requested_at` 필드에 기록하기 때문에 설계상 이미 보장됨 — 서술은 이 사실을 명시적으로 밝히는 정정. 대기열은 그 채널 무관 요청들이 모이는 "마지막 처리 단계"(직원 승인/반려)를 담당. **기능 자체는 변경 없음, 서술만 정정.** (계획 문서 변경 불필요 — 위 확인 참고)

## F. 3단계(환자 앱) 계획 문서 반영 대상 — D 반영 시 함께 필요, 새로 발견
`docs/superpowers/plans/2026-07-27-patient-app.md` (Task 9 부근, 6001~6254줄)

11. **"예약 취소" 버튼 마감 후 분기 로직 변경** — 현재 계획: `cancelBooking` 호출 결과 `cancellation_requested: true`면 앱이 그 자리에서 "취소요청 접수됨" 화면/문구를 보여줌(6018~6032줄 테스트 포함). 변경 후: 그 대신 "마감이 지나 상담으로 연결해드립니다" 안내 후 AI 상담 화면(4단계 챗봇)으로 이동. 백엔드 `cancel_appointment` 서비스(2302~2428줄, 마감전후 분기 로직) 자체는 그대로 재사용 — 변경 없음. 마감 전 즉시취소 흐름과 확인 다이얼로그(6001줄 2026-07-28 재검증 사항)는 건드리지 않음.
   - **구현 순서 메모(코드 아님, 문서 노트)**: 3단계는 "AI 상담 화면으로 이동"까지만 책임지고, 실제 취소 접수·인계 티켓 생성은 4단계(A-5)가 담당하는 구조라 — **4단계가 아직 구현 안 된 시점에 3단계를 먼저 구현해도 3단계 자체 테스트(버튼→화면 이동 확인)는 통과 가능**하다. 종단 간(취소가 실제로 대기열·인계함에 뜨는지) 테스트는 4단계 완성 후에나 가능. 별도의 임시 폴백 코드는 불필요.
12. **Task 27(사전문진 프리필 기능) 사실상 통째로 삭제** — (정정: "역할 축소" 아님) 3단계 계획엔 사전문진 화면이 Task 21(기반 화면, `questionnaire_screen.dart` 직접 작성·저장 — 전혀 안 건드림)과 Task 27(그 위에 얹은 `field_key` 프리필 기능만 추가)로 나뉘어 있음. B에서 4단계 Task 21(프리필 API, 이름이 같지만 다른 문서의 다른 태스크)이 삭제되고 `사전문진_카드보내기` 도구로 대체됨에 따라, 3단계 Task 27의 존재 이유(`prefill:`/`field_key` 매칭 로직, 7461~7472줄 등) 자체가 없어짐 — Task 27을 통째로 삭제. 사전문진 화면 자체(기반 화면, Task 21)는 무변화.
14. **스펙 커버리지 요약표 갱신** — (최종 정합성 확인 중 새로 발견) 7602줄 부근 "상담봇발 사전문진 자동반영은 화면 쪽 절반을 Task 27이, 나머지 절반을 ai-chatbot.md가 담당하도록 나눠 반영했다(2026-07-28)"는 서술이 옛 역할분담 기준. D-13과 함께 갱신.
15. **Task 21(기반 화면)에 "기존 저장값 불러오기" 기능 신규 추가** — (F-12 논의 중 발견된 더 근본적인 문제, 챗봇과 무관하게 원래 계획에 있던 구멍) 현재 계획된 `QuestionnaireController`(6259~6437줄)는 `loadTemplate`(빈 질문 양식 조회)과 `submit`(저장)만 있고, **기존에 저장된 답변을 불러와 화면에 채우는 기능이 아예 없음**(화면 코드도 항상 빈 `TextEditingController()`로 시작). 이대로면 "환자는 진료 전까지 수정할 수 있다"(요구사항 4.4)를 못 지킴 — 한 번 저장하면 재방문 시 항상 빈 화면. 또한 이 기능이 없으면 챗봇(A-1)이 저장한 사전문진 내용도 앱 화면을 열었을 때 안 보이는 문제가 생김(DB엔 있는데 화면엔 안 뜸). **추가할 것**: `QuestionnaireController`에 `loadExistingAnswers(appointmentId)` 같은 조회 함수 신규 추가, 화면 진입 시 `loadTemplate`과 함께 호출해 기존 저장값이 있으면 입력칸에 채움(누가 썼든 — 챗봇이든 예전에 앱에서 직접 썼든 — 항상 최신 저장값 표시). 이 기능이 생기면 Task 27이 하려던 것(챗봇 저장 내용 화면에 보이기)도 자동으로 해결되므로, Task 27을 굳이 되살릴 필요는 없음(F-12 삭제 결정 유지).

---

## 결정: 이번엔 추가 안 하는 것

- 대기 순번 조회 (A-6에서 KB 안내문으로 대체)
- 가족 등록(신규 생성), 본인 회원가입 — 개인정보 생성이라 앱/모달로 안내(원 스펙 결정 유지)
- 사전문진 내용 재조회 — 앱에 이미 있어 중복
- 더 이상의 행동형 도구 후보 탐색 — 요구사항 4·5장 재검토 완료, 추가 후보는 추측성이라 중단. 실제 운영 후 "미해결 질문 모아보기"(3.9/3.10) 데이터로 재확인 예정.

## 다음 액션

1. **A(brainstorming, 4단계 스펙) → B(writing-plans, 4단계 계획) → D(brainstorming, 3단계 스펙) → F(writing-plans, 3단계 계획) → E(brainstorming, 2단계 스펙) → C(writing-plans, 배포계획)** 순서로 문서에 정식 반영 — 사용자 승인 완료(2·3단계 범위 포함, 스킬 배정 포함)
   - 순서 근거: A 확정 즉시 짝인 B로 동기화(같은 4단계 문서군) → D는 A에서 챗봇 취소 도구(A-5)가 실제로 생긴 뒤에만 안전 → F는 D 직후(같은 3단계 문서군, D가 바꾼 스펙을 계획에 즉시 동기화) → E는 D/F가 끝나야 "무엇으로 4.3이 완결되는지" 정확히 쓸 수 있음(단, E는 계획 문서 변경 없음, 스펙만) → C(검수체크리스트·데모시드)는 전부 맞물린 최종 상태를 검증하는 문서라 맨 마지막
   - 1단계(foundation) 영향 없음 확인 완료 — 안심하고 진행
2. 반영 완료 후 세션 태스크 목록(1~15번) 정리/삭제

## 참고 파일 경로

- 4단계 스펙: `docs/superpowers/specs/2026-07-27-ai-chatbot-design.md`
- 4단계 계획: `docs/superpowers/plans/2026-07-27-ai-chatbot.md`
- 배포 계획: `docs/superpowers/plans/2026-07-27-deployment.md` (Task 9: 데모 시드, Task 21: 최종 검수)
- 3단계 스펙: `docs/superpowers/specs/2026-07-27-patient-app-design.md`
- 2단계 스펙: `docs/superpowers/specs/2026-07-27-staff-web-design.md` (섹션 10)
- 정합성 검토 근거 문서: `docs/superpowers/specs/2026-07-28-p0-policy-decisions-design.md`(R5-01), `2026-07-28-p1-scheduling-design.md`(마감시각 검증), `2026-07-28-p2-notification-queue-family-design.md`(R5-02)
- 요구사항 원문: `docs/고객요구사항.txt` (4.2/4.3/4.4/4.5/4.6/5장/9장 참고)
