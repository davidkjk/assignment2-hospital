# 핸드오프 메모

> **이 파일에 무엇을 적나**: **지금 상태**와 **다음에 할 일**만. 결정 내용·근거는 결정 문서, 규칙은 `screen-behaviors.md`가 원본이다(→ `CLAUDE.md` 「기록 규칙」).
> 📌 문서가 어디에 무엇을 담는지는 **`CLAUDE.md` 「문서 지도」** 참고. 이전 세션 상세는 `.claude/HANDOFF-archive-2026-08-06.md`(gitignore).

---

## ⭐ 최신 (2026-08-15 · ③ 스펙 재작성 — 4개 전부 완료 + 문서층 검증) — 여기부터

**반영 절차 ③단계(스펙 재작성) 완료. 공용 데이터 모델 절(1단계 섹션4) + 환자앱 + 직원웹 + 상담봇 스펙 다 재작성됨. 세 스펙 문서층 검증 통과. 다음 = ④ 마이그레이션.**

### 이번 세션에서 끝난 것
- ✅ **상담봇 스펙 재작성**(`ai-chatbot-design.md`) — 8섹션 + 폐기표. 옛 `chat_conversations`→`chat_threads` 통합(3-A 8테이블 + 보조 2: `support_ticket_assignment_history`·`chat_message_sources`). `support_tickets.appointment_id`→1단계 섹션4 ① `appointments`. `notification_log` 공용 dispatcher·`NotificationRecipient` 참조. 보존기간 `retention_class` migration은 **BLOCKED**(법무 게이트 = 직원웹 #14와 한 묶음). 품질검토 상태 테이블명·MR2-08 한도는 대상 미확인(플랜 확정).
- ✅ **세 스펙 문서층 검증** — 잔재 0(옛 표현은 폐기표·부정문 안에서만)·결정번호 커버리지(SD-01~09·주요 갭)·8테이블 명명·폐기 항목 반영. **깊은 규칙ID↔원문 정합은 별도로 안 함** — ⑤ 플랜 재작성의 규칙별 `test()`로 흡수됨(사용자 확인). `coverage-check.py`(결정로그↔색인)는 별개 ⑥단계이며 통과 상태.
- ✅ **환자앱 폐기표 형식 통일** — 인라인 폐기를 끝에 요약 표로 모음(상담봇·직원웹과 통일).
- 📌 (앞서 완료) 1단계 섹션4 신설 · 환자앱 · 직원웹 재작성. 과정 상세는 각 스펙 문서가 원본.

### 스펙 형식(견본 — 4개 공통)
1. 머리말(입력 원본·"계약 층까지만, 규칙 전수는 플랜 몫"·공용은 1단계 섹션4 참조·`00010_` 번호) 2. 화면 목록 정본 3. 각 섹션 연관 갭/SD 번호 4. BLOCKED/미결 그 자리에 5. 끝에 폐기·교체 표

### 다음 세션이 바로 할 일
1. ⭐ **④ 마이그레이션**(`00010_`부터). ⚠️ **착수 전 정찰 먼저**: `00001~00009` 실제 적용 상태(`supabase status`) + 공용 `notification_log #115·#119` 대기분이 이미 파일로 있는지 확인 → 3-A가 *"미적용이면 한 계열로 합치고, 적용됐으면 후속에서 FK만 확장"*의 두 갈래를 지정(`ui-design-decisions.md:4646~4651`). 공용(1단계 섹션4)+3-A 상담 8테이블+`appointments` 확장을 논리 단위로 `00010`,`00011`…로 분할.
   - ⛔ **DB reset 금지** — 공용 DB, 다른 세션 신호 받기 전엔 reset 안 함(파일 작성은 무방).
2. → ⑤ 플랜 재작성(규칙ID `test()`) → ⑥ 기계검증(`coverage-check.py`) → ⑦ 구현.
3. Git 병합: `merge/design-integration` → main(디자인·스펙 다 끝난 뒤 한꺼번에 커밋).

### 미커밋 (커밋은 디자인·스펙 다 끝난 뒤 한꺼번에 — 사용자 지시)
- `specs/`: `foundation-auth-data-model-design.md`(섹션4) · `patient-app-design.md`(재작성+폐기표) · `staff-web-design.md`(재작성) · `ai-chatbot-design.md`(재작성) · `_review-index.html`(삭제 표시, 커밋 전 확인)

## (이전) 2026-08-15 새벽 · 설계 완료 → 스펙·플랜 재작성 준비

**PLCHECK 5개 리포트 검토 완료. 설계 단계 종료 확인. 스펙·플랜 재작성 직전 — 결정로그 영역별 색인이 선행 과제.**

### 이번 세션에서 끝난 것
- ✅ **PLCHECK 5개 리포트 수거·검토**: blocker 0(상담봇 4채널) + blocker 15(직원웹, 전부 기존 BLOCKED). **새 기능 갭 0건, 새 결정 필요 0건.**
- ✅ **"설 땅이 있나" 판정**: 4층(DB·서버조회·API·다음단계) 전부 확인 → 모든 결정이 구현 가능. 결정 자체를 다시 해야 하는 것 없음.
- ✅ **결정로그·screen-behaviors 추가 기록 불필요 확인**: 새 발견 0건이라 기록할 것 없음.
- ✅ **설계 단계 열린 항목 점검**: B-8/B-9/B-10/C-4 전부 이미 결정됨(핸드오프가 낡았을 뿐). C-5(PASS)는 병원 운영 정책이라 설계 밖.

### ⚠️ 이번에 발견한 것 — 결정로그 후반부에 "스펙에 이거 넣어라" 항목 누락

결정로그의 **초기(환자 앱, 7/31~8/8)**에는 "Task 22에 이거 추가해라" 식의 체크박스(`- [ ]`)가 191건 있다. **후반부(직원웹 33건 결정 + AD-050~071 + 상담봇 MR2-01~10 + 역대조 D1~7 + R2-3A + 통합 스키마 7건)**에는 결정 내용은 잘 기록됐으나 **"어느 스펙 파일의 어느 부분을 어떻게 고쳐라"가 빠져 있다.**

이걸 안 메우면 스펙 갱신할 때 초기 191건만 반영하고 후반부 결정이 통째로 빠진다.

### 문서 역할 정리 (이번 세션에서 정리됨)

| 문서 | 스펙·플랜 재작성에서의 역할 |
|---|---|
| 결정로그 | **왜** 그렇게 정했는지, 최신 결정·폐기·미체크 항목의 원본 |
| `screen-behaviors.md` | **무엇을 어떻게** 동작시키는지, 조건·상태·오류·권한과 규칙 ID의 원본 |
| `docs/design/spec-index/SPECINDEX-*.md` | **어디까지** 반영해야 하는지 — 기능 갭·구조 결정·폐기·원문 줄의 추적성 manifest. 스펙 3개에 1:1(patient-app/staff-web/ai-chatbot). 커버리지 검산 `coverage-check.py` |
| 기존 스펙·플랜 | 재작성의 레거시 입력. 최신 세 문서와 충돌하면 권위가 아님 |

- 스펙과 플랜은 결정로그·`screen-behaviors.md`·해당 색인·요구사항을 함께 읽는다.
- `screen-behaviors.md`는 스펙에 빠진 서버조회·상태 전이·오류·권한 갭을 발견하는 재료다. 규칙 전체를 스펙에 복사하는 것이 아니라 필요한 구현 계약을 추출한다.
- 스펙은 "화면에 안 보이는 것"(테이블·API·상태 전이)을 확정하고, 플랜은 그 계약과 규칙 ID를 구현 지시·테스트 문장으로 변환한다.
- 플랜 각 태스크에는 `test('[규칙ID] …')`를 직접 넣어야 한다.

### 다음 세션이 바로 할 일

✅ **색인 완료·통합·검증 (2026-08-15)** — 영역 색인 5개를 **스펙 문서에 맞춰 3개로 통합**하고 git 추적 위치로 이동: `docs/design/spec-index/SPECINDEX-{patient-app,staff-web,ai-chatbot}.md`. 옛 `.claude/codex-work/spec-index/`(메인5+위성 감사파일16 = 21개)는 삭제. 코5 AD-052~061 「대상 미확인」은 감사 진행기록(active-ledger)으로 확인돼 해소. **커버리지 기계검산 통과**(기능갭 126=126, AD·MR2·역대조·R2·SD 전부): `python3 docs/design/spec-index/coverage-check.py` (종료코드 0). 남은 「미결」은 결정 미결이 아니라 재작성·구현 to-do + 안 지운 옛 레거시뿐.

1. ⭐ **③ 스펙 재작성** — 입력 = 결정로그 · `screen-behaviors.md` · `docs/design/spec-index/`(해당 영역 1개) · 요구사항. **스펙 1개당 색인 1개** 대응(patient-app / staff-web / ai-chatbot). foundation `00010_`은 세 색인에 `grep 00010`으로 모음. deployment는 설계 결정 0.
2. → **④ 마이그레이션**(`00010_`부터) → **⑤ 플랜 재작성**(규칙 ID `test()` 포함) → **⑥ 기계 검증**(`coverage-check.py` 포함) → **⑦ 구현**
3. Git 병합: `merge/design-integration` → main (설계 완료 확인됨)

### PLCHECK 리포트 위치 (완료)
- `.claude/codex-work/mgcheck/PLCHECK-{web,app,staff,admin,staffweb}-report.md`
- 종합: blocker 15 / gap 67 / note 15 = 97건, 전부 기존 추적 항목. 새 발견 0.

### 반영 절차 원본
- 결정로그 4839~4900줄 「📋 반영 절차」 — ①결정정리 → ②screen-behaviors → ③스펙 → ④마이그레이션 → ⑤플랜 재작성 → ⑥기계 검증 → ⑦구현
- ⑤는 패치 아닌 **재작성** 확정 (4865줄)
- 워커 핸들: 코1=`17193150` 코2=`e02758dd` 코3=`6694c8bd` 코4=`966a493a` 코5=`79084ccd`

### 환자 앱 재작성 입력 고정

환자 앱 스펙·플랜을 만들 때 아래 문서를 모두 읽고 서로 대조한다.

1. `docs/superpowers/specs/2026-07-31-ui-design-decisions.md` — 결정의 이유와 최신 결정/폐기 원본
2. `docs/design/screen-behaviors.md` — 환자 앱 묶음 0~8 및 앱이 소비하는 공용 규칙 원본
3. `docs/design/spec-index/SPECINDEX-patient-app.md` — 환자 앱 범위·갭·원문 줄·누락 체크리스트
4. `docs/고객요구사항.txt` 및 기존 `patient-app-design.md`/`patient-app.md` — 요구사항과 레거시 입력

작성 순서는 **결정로그 정리 → screen-behaviors 확정 → 색인으로 누락 대조 → 스펙 재작성 → `00010_`부터 migration → 플랜 재작성 → 기계 검증 → 구현**이다. 기존 스펙·플랜은 패치 대상이 아니라 재작성의 입력이며, 플랜에는 관련 규칙마다 `test('[규칙ID] …')`를 남긴다. 색인은 결정로그나 화면 규칙을 대신하지 않는다.

## (이전) 2026-08-14 밤 · 견본 검토 + 목업↔규칙 대조 완료, 판정 대기

**견본 2장 확정 → 워커 4개로 목업↔규칙 대조(규칙-먼저 「규칙↔그림」 대조) 완료. 리포트 4개 나옴.**

### 이번 세션에서 끝난 것
- ✅ **견본 2장 검토·방향 확정** — `120-sample-chatbot-sidebar.html`(AD-069 사이드바 IA: 진료화면 단독+업무·기록·설정·상담봇 4그룹, 240px, 토큰타이포, 이모지0, 운영시간 읽기만) · `121-sample-ticket-detail-layout.html`(MR2-09: 담당이관→전체대화→답변(보내기)→별도 종료, 보내기/종료 분리). 둘 다 정본대로 잘 나옴.
- ✅ **Item2(MR2-09·10 규칙 반영) 검증 통과** — `TICKET-DETAIL-LAYOUT-01`(5311) · `TICKET-DETAIL-CLOSE-SEP-01`(5347, 핸드오프의 `CLOSE-SEP-01`은 접두어 누락) 신설 · 결정로그 MR2-09·10 완비(5088·5089) · `SUPPORT-CAL-*` 15개 존치. ⚠️ 작은 미결: 목업109 폐기 낡음표식이 screen-behaviors·README엔 없음(결정로그엔 있음).
- ✅ **목업↔규칙 대조 완료(코1~4, terra-xhigh)** — 상담봇 목업 29장 채널 4분할. **리포트 = `.claude/codex-work/mgcheck/MGCHECK-{web,app,staff,admin}-report.md`**. 총 **약 41건**(모순10·누락14·유령0·불일치17). ⚠️ **아직 판정 안 함**(플레이북: 수집≠판정, false-positive 거르기가 내 몫).
- 📌 방법 원본 = `~/.claude/mockup-design-session-playbook.md` 「또 다른 역방향: 규칙↔그림 대조」 절 + ⑦플랜대조 4층. 브리프 = `.claude/codex-work/briefs/MGCHECK-*.md`.

### ⚠️ 이번에 걸린 함정 2개 (다음 세션 주의)
- **내 Bash가 샌드박스라 워커의 실제 디스크 쓰기를 못 본다** — 워커가 파일 썼는데 `ls`엔 안 떴다. **워커 산출물은 Read/Grep 도구로 보거나 `dangerouslyDisableSandbox`로 봐야 한다.**
- **워커 첫 지시에서 파일 저장이 누락**됐다(터미널에만 요약 출력) → 클리어 없이 "파일로 실제로 써라" 재지시로 회수. **브리프에 「출력=파일」을 더 강하게 박을 것.**

### 리포트 요지 (다음 세션 판정 입력 — 대체로 두 갈래)
- **(가) D단계 낡음 = 이미 아는 것**(사용자가 대량갱신 안 하기로 함, 낡음표식 대상): 사이드바 옛 그룹(관리자 110~117 거의 전부·직원 104~108 `SHELL-NAV`) · 티켓상세 레이아웃(104·105 담당이관 위치·보내기/종료 인접) · 109/116/118 폐기 낡음표식 없음. → **판정에서 「낡음 확정」으로 빠르게 분류.**
- **(나) 진짜 새 판정 후보**(규칙-먼저라 대체로 규칙 수정 후보):
  - web: 100 `WEBANON-HANDOFF-03` 전화번호 **필수 vs 선택**(모순) · 98/103 다른기기 이어보기 문구(MR2-01) · 99 `WEBMOD-AUTH-08` 재확인카드 · 102 카드재사용 6항목 축약
  - app: 93 `CCARD-CANCELDONE-QNR-01` 문진보기 CTA 누락(카드사전 미결과 한 묶음) · 94 `CANCEL-REJ` 라벨 · 95 `MR2-04` 미반영 · 96 lateflow 문구 · 119 `RETICKET` 누락(의심)
  - staff: 104 `TICKET-DETAIL-SUM-01` 인계요약 5항목 중 3 · 107 `BADRPT` empty/err 복귀·재시도 없음 · 108 `TODAY-RESCHED` 버튼 누락
  - admin: 113 `UNRES-CLUSTER-04` 한계안내 위치 · 115 `QUALITY-REPORT-10·11`·`QAEX-LIST-10` 로딩·오류·재시도 누락

### 다음 세션이 바로 할 일
1. ⭐ **리포트 4개 판정** — 원문 대조로 (가)낡음/(나)진짜 갈라내고, (나)는 규칙 수정인지 그림 수정인지 사용자와 **하나씩**. 순서: staff·admin이 낡음 많고, web·app에 진짜 후보 몰림.
2. **B단계 플랜 대조** (판정 뒤) — ①직원웹 15화면 병합 때 ⑦대조 실제로 됐는지 확인 ②상담봇 규칙 전체 ⑦플랜 대조 전수(4층). **워커에게 시킬 것**(코1~5, 매번 클리어). 모델 terra-xhigh.
3. 대기(변함없음): Git `merge/design-integration`→main(디자인 전부 끝난 뒤) · 플랜 재작성.
- 📌 워커 handles: 코1=`17193150` 코2=`e02758dd` 코3=`6694c8bd` 코4=`966a493a` 코5=`79084ccd`(sweep, 미사용). 클리어하면 handle 유지되나 컨텍스트 날아감 → 자기완결 브리프로.
- 📌 모델: Opus 4.6 1M은 **크레딧 부족(429 `credits_required`)으로 전환 실패**. 사용자가 크레딧 사면 재시도. 안 사면 현재 모델로 진행(워커가 파일 읽어 하므로 무방).
- 📌 코5 sweep은 아직 안 씀 — A 판정 후 카드일관성(앱 정본↔웹 재사용)·견본 대조에 쓸 예정이었음.

## (이전) 2026-08-14 낮 · 상담봇 목업 감찰 R2 + 병합 감사

**사용자와 상담봇 목업 29건 감찰 완료 → 결정 MR2-01~10 확정·git 정본 병합. 그리고 codex-work↔git 병합 감사 실시.**

### 이번 세션에서 끝난 것
- ✅ **상담봇 감찰 결정 MR2-01~10 확정·git 병합**(원본=결정로그 `ui-design-decisions.md` 「목업 감찰 R2」 절 + `screen-behaviors.md`):
  01 익명 웹 다른기기 이어보기 폐기 · 02 이전상담 진입 `NAV-CHATAPP-10` 신설 · 03 인증후 복귀=재확인카드(`WEBMOD-AUTH-08` 해소) · 04 목업95 의사선택 강조 뒤집기+다음가능시간 제거 · 05 목업118 폐기(운영시간=직원웹 `SCHED-HOURS` 단일소스, 상담봇은 읽기만) · 06 목업116→117 흡수 · 07 사이드바 정본=`AD-069`(업무/기록/설정+**상담봇** 4그룹+진료화면 단독), `SHELL-NAV-01·02·04` 개정 · 08 `CHAT-LEN-01` 대화길이 넛지(한도값은 확인필요=플랜) · 09 티켓상세 레이아웃 `TICKET-DETAIL-LAYOUT-01`·`CLOSE-SEP-01`(보내기/종료 분리) · 10 목업109를 64/65 캘린더 상태로 흡수.
- ✅ **C 미결 67개 중 66개는 구현계약이라 플랜 이월**(사용자 확정) — 정렬·동점키·Realtime·트랜잭션·enum 등. 사용자 판단은 #11(MR2-03)뿐이었음.
- ⭐ **병합 감사(코덱스 워커 3+sweep) 결과**: ①규칙 **100% 병합됨**(`screens/04*`의 `DOCTOR-*`·`ALOG-*`·`MERGE-*` 등 전부 `screen-behaviors.md`에 있음, "게이트"는 DB·구현 `BLOCKED-BEFORE-MERGE`뿐=정상) ②결정로그 대부분 병합(AD-062~071 git에 있음, "미병합 45"는 대부분 병합·감사 프로세스 부기) ③**sweep 새 미병합 0**. **진짜 미반영 = D단계**(`AD-069` 사이드바·`AD-070` 타이포). 
- ⭐ **사용자 결정: D단계 대량 목업 갱신 안 함** — "구현할 때 목업 안 본다"(플레이북대로). **규칙 `SHELL-NAV-01`만 갱신(완료)** + 견본 몇 장으로 충분. 나머지 낡은 목업(사이드바 30장·폰트 126장)은 낡음 표식 대상.
- ⚠️ **기록 위치 교정(사용자 지적)**: 결정은 **codex-work(gitignore 작업본)가 아니라 git 정본**(`ui-design-decisions.md`+`screen-behaviors.md`)에 직접 쓴다. 이번에 codex-work 스테이징을 중단하고 git으로 직접 병합.

### 다음 세션이 바로 할 일
1. **견본 2장 검토**(워커가 생성 중): `docs/design/mockups/120-sample-chatbot-sidebar.html`(AD-069 사이드바 IA) · `121-sample-ticket-detail-layout.html`(#16 재배치). 눈으로 확인 → OK면 방향 확정.
2. **워커 산출 검증**: REC 워커(MR2-09·10 git 반영) 결과 `screen-behaviors.md`에 `TICKET-DETAIL-LAYOUT-01`·`CLOSE-SEP-01` 신설됐는지, 109 흡수 문구 들어갔는지 실검증(grep).
3. **대기(변함없음)**: ⑦플랜 대조(감찰 수정분) · Git `merge/design-integration`→main(커밋은 디자인 전부 끝난 뒤) · 플랜 재작성(`확인 필요`·`BLOCKED-BEFORE-MERGE` 다수 — merge/11 §4).
- 📌 감찰 추적 원본(임시): scratchpad `mockup-review-inventory.md`(29건) + `AUDIT-OUT-W1/W2/W3`·`AUDIT-OUT-SWEEP`(병합 감사). codex-work `MOCKUP-REVIEW2-DECISIONS`는 git 병합 완료라 참고용.
- 📌 워커: orca 코덱스 luna-xhigh 5개(17193150·e02758dd·6694c8bd·966a493a·79084ccd). 판정은 Claude, 워커는 대조·병합·목업 실행만.

## (이전) 2026-08-13 밤5 · ⑦플랜 대조 완료

**역대조 결정 8건 ① 정본 반영 → ② 목업 수정 → ⑦플랜 대조까지 완료. 남은 것은 Git 병합뿐.**

### 이번 세션에서 끝난 것 — ⑦플랜 대조(역방향 결정 8건)
- ✅ **4층 대조 완료** — 정본 기록처 = `ui-design-decisions.md` 「⑦플랜 대조 — 역방향 결정 8건」 절(역대조 8건 표 바로 뒤). 대조 대상 = `ai-chatbot.md`(티켓·배정·조회) + `staff-web.md`(`/today` 대시보드).
- ⭐ **결과: 성립 불가(blocker) 0건.** 8건 모두 설 땅 있음.
- ⚠️ **방식 충돌 1건(역대조-3·4B, ②·④층)** — `claim_ticket`(`ai-chatbot.md:3600`)이 `-> None`·`execute`라 **경쟁 패자에게 실패를 안 알린다.** `get_ticket_detail`이 claim 뒤 무조건 상세를 열어 **진 직원도 상세를 본다** → 4B의 *"늦은 직원은 목록 유지 + 이미 다른 직원이 맡았어요"*와 충돌. 설 땅은 있음(`... where status='pending' returning id`로 승패 판정+담당자 반환) → **claim_ticket 계약 개정이 재작성 몫.**
- ⭐ **역대조-4(D4)가 가장 큰 신설** — 전부 플랜에 아직 없다(설 땅은 다 있음, 재작성 몫): `support_tickets.appointment_id` 컬럼(현재 `summary_staff_todo` 텍스트에 박음 `:2344`) · `appointments.support_requested_at`·`request_type` 두 컬럼(양 플랜 0건) · `reason` enum에 변경상담 값 · `/today` 상담 행 조회(`get_today_summary`가 `pending_inquiries_count: 0` **하드코딩** `staff-web.md:4032`) · 양방향 내비. **수치 카드 폐기는 기존 집계 조회를 안 깬다**(애초에 집계 조회 없음).
- ⭐ **역대조-2(D2)** 정렬 방향은 맞고(`list_tickets` `order by t.created_at`) **동점 키 `id ASC`만 빠짐**(`, t.id` 한 줄) + 페이징 자체가 없음 — 재작성 몫.
- 나머지 4건(역대조-1·5·6·7)은 구현 방식 단정 없어 대조 신호 약함, 성립 문제 0.

---

**(이전) 2026-08-13 밤4 · 정본 반영 완료·검증 → 목업 수정 완료**

**역대조 결정 8건을 ① 정본 반영 완료·검증 → 코1~5 클리어 → ② 목업 수정 4워커 완료·전수 검증 통과.**

### 이번 세션에서 끝난 것
- ✅ **① 정본 반영 완료** — 원본 `RCHECK-DECISIONS-2026-08-13.md` 8건.
  - `screen-behaviors.md` 규칙: **D1**`CHAT-URGENT-EXC-01`(제목`안내`+확정본문) · **D2**`TICKET-INBOX-ORDER-02`(`created_at ASC, id ASC`) · **D3**`TICKET-DETAIL-ASSIGN-02`·`SCOPE-01`(별도 담당지정 폐기→자동배정) · **D4**`SUPPORT-TODAY-*`수치카드 폐기→`TODAY-RESCHED-23~28`신설+`NAV-STFSUP-08·14` · **D6**`LATEFLOW-POP-CLOSE/BUSY/ERR-01`(처리중 닫기 잠금) · **후속**`TICKET-INBOX-ROW-01`·`NAV-STFSUP-02·13`(분할 작업공간) · **정합 정정** `TICKET-DETAIL-LIVE-02`·`NOTIFY-02`(담당지정→자동배정). D5·D7은 이미 정합(무변경).
  - `ui-design-decisions.md`: **역대조 결정 8건 표** 추가(`역대조-1~7`+`4B`) + **R2-3 검색어 범위 보완**.
  - 순증 규칙 **+7**(`TODAY-RESCHED-23~28`·`NAV-STFSUP-14`). 총 2,938.
- ✅ **자체 검증 통과**(코1 대신 내가 — 좁은 diff): 유령ID 0 · 표 5열 무결(규칙 30여행+결정로그 8행) · 해소 `확인필요` 잔재 0 · 폐기 개념은 금지문구로만 존재 · 환자 노출 금지문구는 별개라 무관.
- ✅ **코1~5 클리어** (5개 코덱스 터미널 `/clear`).
- ✅ **② 목업 수정 — 4워커 전부 완료·전수 검증 통과**:
  - **코4** 115(검색어 복원 제거·설명 정정, `#i-search`는 조회버튼 유지)
  - **코2** 97(EXC 제목`안내`+확정본문, 감지상태 `긴급안내` 유지)·96(실패·시간초과 잠금복구 상태 신설)
  - **코1** 웹 무변경(웹은 앱 EXC 재사용, 102 `긴급안내`카드는 일반 GUIDE라 유지가 맞음 — 확인함)
  - **코3** 104(분할 작업공간 2열·`미배정`·접수순 확정·원자배정)·105(`미배정 담당지정`카드 삭제·`별도 담당지정 없음` 설명·`캘린더에서 예약 처리`·이관 유지)·108(수치카드 폐기→`확인 필요한 예약` 카드에 일정변경+상담 행 통합·상태보드 행모델)·109(패널닫기 `✕`+`aria-label="패널 닫기"`·`상담 전체 보기`). ⭐ 내가 108 낡은 주석 1건 정정.
  - **검증**: 7파일 전부 태그 균형·미정의 CSS 0·유령 ID 0·danger `#A02F3D`·진짜 이모지 0.

### 다음 세션이 바로 할 일
1. ⭐ **전체 ⑦플랜 대조 — 목업 감찰 뒤로 미룸(사용자 지시 2026-08-13)**. 순서: **①사용자가 목업 전체를 감찰 → ②「수정할 것」 목록 확정 → ③그 수정분에 대해 ⑦플랜 대조.** 지금 선제로 전수 돌리지 말 것.
   - **왜 남았나(사용자 직감이 맞았다)**: `screen-behaviors.md`의 `⑦대조` 표식은 **10개뿐이고 전부 1362줄 이하(직원 웹·환자 앱)**다. **상담봇 화면 절(`TICKET-*`·`SUPPORT-*`·`CHAT-*`·`KBADM-*`·`UNRES-*`·`QTOP-*`, 전부 5000줄대)에는 표식이 0개** — 이번 결정 8건 ⑦대조 말고는 **상담봇 화면별 대조를 안 돌렸다.** ⚠️ `grep '^⑦대조: ⬜'`=0은 "다 밟음"이 아니라 **표식 줄이 아예 없어서**다(오해 금지).
   - **직원 웹**: `/queue`·`/today`·`/admin/schedule`(SB-53)·`/admin/settings` 등은 ✅, `➖`(소급 안 함) 4건. ⚠️ **병합한 15화면(목업 76~85b)에는 ⑦대조 표식이 안 보임 — 그 절들 대조 여부 확인 필요.**
2. **Git main 병합**(사용자: 다음에) — `merge/design-integration` → main. **커밋은 디자인 전부 끝난 뒤 한꺼번에.**
- ⚠️ **미반영(의도적 연기)**: 위 결정 8건 ⑦대조가 짚은 것들(claim_ticket 승패 계약 · D4의 컬럼·조회·내비 신설 · D2 동점 키)은 전부 **플랜 재작성 단계 몫**이다 — ⑦대조는 「설 땅이 있나」 판정까지만 했다. RCHECK-DECISIONS의 「반영 대상」 중 "구현 계획에…" 항목들.
- 📌 브리프 4개: `.claude/codex-work/briefs/MOCKUP-FIX-{web,app,staff,admin}-2026-08-13.md`. 창 매핑: 코1=`17193150` 코2=`e02758dd` 코3=`6694c8bd` 코4=`966a493a` 코5=`79084ccd`.

## (이전) 2026-08-13 밤3 · 코5 결정 회의 완료 → 정본 반영 (완료됨)

**병합 검증 완료 → 역방향 대조 90건 → 코5가 판단 7건 회의 완료(+4번 후속). 지금 = 정본 미반영 상태, 새 세션에서 정본 일괄 반영 예정(사용자 지시).** 코1~4 22장 그림수정은 이미 완료·검증됨(아래).

### 이번 세션에서 끝난 것
- ✅ **정본 병합(`MERGE-CHATBOT`) 검증 통과** — 내가 직접 기계검증: **B1 규칙 증발 0**(원본5파일↔병합본 ID집합 **673=673**, 오염0) · **B2** 결정회의10건+7갭+`00010`대기 다 들어감 · **② 목업91~119 유령 ID 0**(430개 인용 전부 실재) · 표/태그 깨짐0. ⚠️ 워커가 pager에 멈춰 **B3(원본 아카이브 표시)만 미완이었고 내가 마저 완료**(behaviors 4 + card-catalog + decision-ledger 상단 `MERGED` 1줄).
- ⭐ **발견 — 보존기간 확정이 git 원본에 미반영**: 7번 방침은 `FINAL`(6클래스)인데 **git-tracked `ui-design-decisions.md` 7갭 표는 아직 `NEEDS-USER-DECISION`(거짓)**. 확정 내용이 gitignore인 `3A-schema` 448~461줄에만 있었다 — 「확정 즉시 두 곳에」의 git 승격 단계를 놓친 것(=codex-work 비대칭의 재발). → B안 병합이 이걸 바로잡는 중.
- ✅ **역방향 대조 완료·수거**(29장, 브리프 `RCHECK-CHATBOT`) — 어긋남 **90건**(모순33·누락50·유령4·카드불일치3). 리포트=`RCHECK-{app,web,staff,admin}-report.md`. ⭐ **90건 중 85건(94%)이 「그림 수정 후보」**=규칙이 맞고 그림이 못 따라옴 → **규칙-먼저 방식이 대체로 버텼다**(사용자의 spec-first vs mockup-first 평가 데이터). 규칙 자체 오류 후보는 5건뿐.
- ✅ **B안 스키마 병합 완료·검증**(코5, 브리프 `MERGE-SCHEMA`) — 3A-schema 상세 461줄 전체를 `ui-design-decisions.md` 상담봇 결정 절에 인라인(헤딩14개 누락0, +464줄) · 보존기간 7번 **`FINAL`로 갱신**(git 원본 거짓표시 해소) · 원본 아카이브 표시. ⚠️ 정합거리: 직원웹 #14(2계열·임시확정 BLOCKED) vs 상담봇7(6클래스·FINAL) 입도 차 — 나중에 통일.
- ✅ **코5 7건 결정 회의 완료 + 「4번 후속 보완」까지** — 브리프 `RCHECK-DECIDE`, 확정 원본 `RCHECK-DECISIONS-2026-08-13.md`(정본 직접편집 안 함, 반영 태스크만 모음). **기계검증 통과**: 인용 규칙ID 27개 전부 실재·유령0. ⚠️ **4번이 원안건(변경 독립카드 여부) 범위를 크게 넘어 대규모 재설계로 확정됨** — 수치카드 전면 폐기→`/today` 환자별 행 통합 + 문의 티켓함 분할화면(C안) 신설 + 캘린더 양방향 왕복 내비 신설 + `R2-3`에 문의함 예외·`TICKET-INBOX-ROW-01`·`NAV-STFSUP-02/13` 개정. 사용자가 코5에서 직접 결정한 것.

### 다음 세션이 바로 할 일
1. ⭐ **정본 일괄 반영 먼저(규칙-먼저 원칙) → 그 다음 목업** — 원본 = `RCHECK-DECISIONS-2026-08-13.md` 각 건 「반영 대상」. **① 정본 반영**: `screen-behaviors.md`(규칙 수정/신설) + `ui-decisions`(결정로그). 결정은 모아뒀다 스킬로 한꺼번에(방침). **② 목업 수정 코1~4 분배**: 국소 5건(앱97·직원104·109·앱96·관리자115)은 가볍고, **104·105·108·109 묶음(4번+4후속)만 재구성 수준**. ⚠️ 규칙 확정 전 목업만 고치면 역방향 어긋남 재발 → 반드시 ①→② 순서.
2. ✅ **22장(결정무관) 그림수정 완료·독립검증 통과**(코1~4 병렬) — **54행 반영**(앱21·웹20·직원3·관리자10), 4장(92·101·111·112) 이상0. **금지 7장 미수정 확인** · 유령0 · 태그균형 · 진짜이모지0(→는 관용). ⚠️ **보류 1건**: 앱93 `CCARD-CANCELDONE-QNR-01`(카드사전 §5가 문진보기 CTA를 `확인 필요`로 둬 임의반영 안 함 — 카드사전 미결과 한 묶음). **남은 목업수정 = 결정 걸린 7장**(코5 결정 후).
3. ⭐ **⑦플랜 대조 한 번 더**(사용자 지시) — 위 1·2 끝난 뒤. 이유 ①규칙 바뀌면 플랜 정합 재확인 ②지금 스키마 7갭은 ①DB층 중심, **②서버조회·④다음단계 층 덜 대조됨**(플레이북 4층 중 얇은 둘). 정본=`ui-design-decisions.md` 상담봇 ⑦대조 절.
4. **Git main 병합**(사용자: 다음에). `merge/design-integration` → main. **커밋은 디자인 전부 끝난 뒤 한꺼번에.**
- 📌 **방법 메모 추가됨**: 「규칙을 목업보다 먼저 쓴 경우 = 규칙↔그림 대조 추가」를 `~/.claude/mockup-design-session-playbook.md` 「또 다른 역방향」 절에(전역 파일).
- 📌 창: 코1~4=`orca terminal list`로 핸들 재확인 · 코5=`term_79084ccd`(결정 회의).
- 📌 창 매핑: `orca terminal list --json`으로 재확인(닫으면 핸들 바뀜). 정본: `STATE.md` · `decision-ledger-chatbot.md`(아카이브됨) · `W-02-retention-research` · 브리프 2개(`RCHECK`·`MERGE-SCHEMA`).

## (이전) 2026-08-13 오전 · 상담봇 R2 회의·반영 완료, #2 목업 착수

**상담봇 오케스트레이션은 `.claude/codex-work/orchestration/`가 정본.** 이 세션에서 R2 목업 완성 → 결정 회의 전부 → 결정로그·동작명세 반영 → 열린질문 3건까지 끝냈고, 지금 **#2 목업이 4채널에서 그려지는 중**이다.

### 이번 세션에서 끝난 것
- ✅ **R2 목업 91~118 전 28장 완성 + 기계검증 3겹** · **오류색 표준 = 빨강 `#A02F3D`/`#FFF0F1` 확정**(원본=목업 89 상태판. 확정 토큰 표에 danger가 통째로 없던 게 뿌리 — 이제 `ui-design-decisions.md`에 등재됨).
- ✅ **R2 결정 회의 전부(안건 0~5 + 신규 3-A)** — 원문=`decision-meeting-2026-08-12-R2C.md` 각 「결정 기록」. **결정로그 등재**(`decision-ledger-chatbot.md` 맨끝 R2표) + **동작명세 4채널 개정**(정의 규칙 647→677, 모순 3건 REPLY-04·05·DONE-02 개정, 유령ID 0).
- ✅ **3-A**(직원 라이브 상담·`[상담 종료]`=answered·종료후 AI분기·30분 만료·재문의 새티켓·알림 배칭·최소 라이브) + **열린질문 3건**(Q1 목록튕김·Q2 글자머리말·Q3 번호→문자+직원측 발송표시, 타이밍 검증). ⭐ 사용자 발견: 익명 웹 문자는 기존 등록환자 경로로 못 감 → `3A-schema-requirements-2026-08-13.md`에 별개 수신대상 모델 반영.
- ✅ **#1 토큰표 등재 · #3 흐름도(종료·만료 분기+B2/B3, 막다른0·끊긴0) · #4 스키마 요구 문서**.

### 다음 세션이 바로 할 일
1. **#2 목업 검토**(지금 그려지는 중): 코2 `119`앱 라이브+`91`, 코3 `105·106`직원 콘솔, 코1 `100`웹, 코4 `115`품질. → **기계검증**(태그·CSS·유령ID·danger #A02F3D) + **역방향 규칙↔그림 재대조** + 카드 일관성.
2. 뒷정리: 목업 README·흐름도 반영 확인. 
3. (플랜 재작성 단계에) `3A-schema-requirements` 스키마 신설 + 단일 토큰 파일·lint.
- ⚠️ 창 클리어 후 새 작업은 **자기완결 브리프**(문서 읽어 실행)로. 창 매핑은 `STATE.md`. 
- 📌 읽을 정본: `STATE.md`(국면) · `decision-ledger-chatbot.md`(R2표) · `decision-meeting-2026-08-12-R2C.md`(회의 원문) · `3A-scope`·`3A-schema-requirements`.

## (이전) 2026-08-12 밤 · 상담봇 목업 R2 착수

**Wave 1~3 + 결정 회의 + Round 1 반영 완료 → Round 2 목업이 방금 4채널 병렬로 착수됨.** 이 세션은 **감독**을 이어받는다(그리기는 코덱스가 함).

### 지금 상태 (끝난 것)
- **Wave 1·2·3 완료**: 동작명세 5개(카드73·관리자163·직원웹170·앱132·웹80) + 흐름도(`chatbot-flow.html`).
- **결정 회의 10건 완료** → `.claude/codex-work/orchestration/decision-ledger-chatbot.md` (A1~A3·B1~B5·E3·E4, 근거·기각안 포함).
- **Round 1(결정 원본 반영) 완료 + Claude 교차검증 통과**: 앱 프레임 20→19(`LATEFLOW-CONF` 삭제), 흐름도 막다른0·끊긴0, 결정로그 §4269에 10건 등재, 마이그레이션 00010_ 대기 2건(E3·B3) 추가.
- **Round 2 목업 착수(방금)**: 4채널 병렬, 각 창 **앵커 1장 먼저 그리고 멈춰 보고**하도록 지시함.

### ⭐ 이 세션이 이어서 할 일 — Round 2 감독
1. **각 창 앵커 보고 대기** → 앵커 목업을 **눈으로 확인**(SendUserFile로 사용자에게도). ⚠️ **카드 일관성 먼저**: 코2(앱)가 카드 정본, 코1(웹)이 재사용 — 두 채널 카드가 같은지.
2. 앵커 OK면 각 창에 **"계속"** 지시 → 나머지 화면.
3. **C 미결 5건을 사용자와 확정**(그리며 나온 `확인 필요` 주석): ①카드 레이아웃 ⑤빠른답변 로딩 ⑪상세 열기(모달/패널) ⑫품질 상세 ⑮문진 완료표현.
4. 완료 후 **기계검증**(태그·CSS·규칙ID 실재) + **역방향 규칙 재대조**(규칙↔그림 어긋남 — 이번 프로젝트는 규칙을 목업보다 먼저 썼으므로 이 대조가 핵심).
5. 뒷정리: 목업 README 갱신 · 결정 회의 후 남은 것.

### 창 매핑 (orca title엔 코1~4 안 뜸 — 핸들로)
| 별명 | 핸들 | 채널 · 모델 |
|---|---|---|
| 코1 | `term_17193150-8971-4b40-96be-def7a4704eb1` | 웹(98~103) · High |
| 코2 | `term_e02758dd-945b-48c4-b11e-ca92cfc0d7fd` | 앱·카드정본(91~97) · High |
| 코3 | `term_6694c8bd-ea57-4220-a323-b93f065e5186` | 직원웹(104~109) · medium |
| 코4 | `term_966a493a-efd5-4a23-a963-41ec70722720` | 관리자(110~118) · High |
- ⚠️ 창 닫고 새로 열면 핸들 바뀜 → `STATE.md` 「창 별명 매핑」 재확인. 지시: `orca terminal send --terminal <핸들> --text "..." --enter`. 상태: `orca terminal list --json`.

### 읽을 것 (이어받기)
1. ⭐ `.claude/codex-work/orchestration/STATE.md` — 지금 국면·창 매핑·작업큐
2. ⭐ `.claude/codex-work/orchestration/decision-ledger-chatbot.md` — 결정 10건·반영 목록·C/D 미결
3. `.claude/codex-work/briefs/R2-*.md` — 4채널 목업 브리프(무엇을 그리는 중인지)
4. 동작명세 5개(`docs/design/behaviors/*` + `chatbot-card-catalog.md`) + 흐름도 — **Round 1 반영 끝난 최신본**
5. `~/.claude/mockup-design-session-playbook.md`(역순 대조) · `CLAUDE.md`(비개발자 소통·기록 규칙·미결 하나씩)

### ⚠️ 방법 주의 — 규칙이 목업보다 먼저다(역순)
동작명세를 목업 전에 썼다(사용자 요청). 근거는 있으나 **그림으로 검증 안 됨** → 목업 감독 때 **규칙↔그림 재대조**가 핵심. 어긋나면 규칙을 고친다(초안이므로).

---

## 🎯 (이전) 앱·직원웹 상태

**직원 웹은 사실상 완료** — `merge/design-integration`의 `90ca0ca`가 15화면 병합(목업 76~85b). 환자 앱은 목업·규칙 완료.

| | 상태 |
|---|---|
| 환자 앱 44화면 | ✅ 목업 · 규칙 · 흐름도 완료 |
| 직원 웹 16화면 | 🔄 `/today` · `/queue` · `/patients` · `/calendar` · **`/admin/schedule`**(67·69·70) · **`/admin/settings`**(68) 완료 / ✅ **`/messages` 신설 — 목업 73·74** · ✅ **`/admin/settings` 다섯째 줄 — 목업 75** / 나머지 미착수 |
| 챗봇 | ⬜ 미착수 — **전용 화면 규칙이 0개**다 |
| 규칙 | **2,009개** (`grep -o '[A-Z]\+-[A-Z]\+-[0-9]\+' … | sort -u | wc -l` 기준) — ⚠️ **접미사(`-02b`·`-07c`)까지 별개로 세면 2,089.** 이 표의 숫자는 **위 명령 기준**이다. 두 방식을 섞지 말 것 |
| 기능 갭 | **126건** · ✅해소 누계에 **#102** 추가 (원본: 결정 문서 「기능 갭」 절) — ⚠️ **#99~#104 + #112·#115·#117~#123·#125·#126이 알림·발송 묶음** |
| 구현 | 1단계만 (`backend/`, `supabase/migrations/` 00001~00009) |
| ⚠️ 화면 수 | **16개 명단이 맞는지 확인 필요** — 양방향으로 어긋났다: **명단에 없는 것**(`/cancellation-requests` 갭 #113 · `/messages` 신설) + **명단에만 있는 것**(`/admin/errors` 갭 **#124** — 스펙·플랜 0건) |

---

## ⭐ 다음 세션이 바로 이어서 할 일

1. ⭐ **알림·발송 묶음 — ✅F·D·C·B 완료. 남은 것: E + #100 조치**
   - ✅ **D(갭 #101·#112) 완료** — 목업 **72**(발송 패널) + **73**(제1문 `/messages`). `SB-52` **완전 해소**.
   - ✅ **C 완료 — 목업 74 신설 + 73·66 갱신까지 끝.** 규칙 **56개 신설**(`SEND-RESULT` 15 · `SEND-FAIL` 14 · `SEND-DEAD` 9 · `SEND-RETRY` 6 · `SEND-BADGE` 6 · `TODAY-RESCHED-17~22`).
   - ⚠️ **정정(2026-08-08)** — 핸드오프가 C를 *"갭 #100·#103"*으로 묶어 뒀으나 **C가 닫은 것은 발송 결과·실패의 표시·재시도·명단**이다. 두 갭의 상태는 이렇다:
     - **#103 — 화면은 이미 있다.** `HSET-SMS-01` ②(*"어떤 종류를 문자로도"*)가 그 자리다. **열린 것은 그 값을 읽는 서버 코드**뿐(플랜 조치). ⚠️ 내가 처음 *"아직 열려 있다"*고 적었다가 원문을 열어 고쳤다 — **「없다」 단정의 재발.**
     - **#100 — 조치 2건이 열려 있다.** 죽은 토큰 ①줄 삭제 ②그 자리에서 문자 재발송. C는 *"예외가 난다"*(`SEND-RESULT-03`)까지만 정했다.
   - ✅ **B(갭 #102) 완료 — 목업 75 신설.** `/admin/settings` **다섯째 줄 「알림」**(종류·문구·문자로도 세 열). 규칙 **`HSET-MSG-*` 34개** + `HSET-NAV-01`·`HSET-SMS-01·02·05` **개정**(세 칸 → 두 칸). ⭐ **#103의 자리도 함께 확정.**
   - ⭐ **E가 다음 — 예약 발송**(⚠️ 갭 **#118** — 담을 표가 없다). 그리고 **#100 조치 2건**(죽은 토큰 삭제·문자 재발송)이 남았다 — 🔗 **`notify_patient` 한 함수가 문구·채널·죽은 토큰을 모두 만지므로 한 번에 손봐야 한다**(`HSET-SMS-05`가 경고한 자리).
   - 🔗 **#72·#100·#103·#104는 한 몸** — 따로 만들면 **채널 고르는 코드가 네 군데로 갈라진다.**
   - 🆕 **갭 #99** — `[먼저 안내 보내기]`(`TODAY-RESCHED-15`)가 **보낼 문구가 서버에 없다.**
2. 🕳 **흐름이 끊긴 곳 — ①②가 닫혔다**
   - ✅ **① 일정 변경 → 영향받는 환자 안내** — **해소(2026-08-07)** · `SCHED-*` 23개 · `TODAY-RESCHED-04~11` · **목업 66**.
   - ✅ **② 두 직원이 같은 자리를 동시에** — **해소(2026-08-07)** · `CAL-RACE-*`·`CAL-LIVE-*` · 목업 65 ⑥절.
   - **③ 화면 규칙이 0개인 것 셋** — 내부 메모(`patient_internal_notes` 조회 코드 있음) · `/admin/staff`(라우트 있음) · 챗봇. 📌 `/admin/schedule`은 `SCHED-*`가 생겨 **이 목록에서 빠졌다.**
   - ⚠️ **`/admin/staff`가 더 급해졌다** — `SCHED-DEPT-05`가 관리자를 그리로 보내는데(진료과를 끄려면 먼저 의사를 끈다) **그 화면 규칙이 0개**이고, **의사를 끄면 그 예약이 어떻게 되는지도 안 정해졌다**(빈칸 `SB-48`).
3. ⏳ **검색 화면 남은 빈칸 3건** — **SB-19**(맞은 자리를 굵게? ⚠️가려진 자리가 굵어진다) · **SB-21**(1명일 때 자동 선택) · **SB-22**(넓은 검색 표시 — 병원 감사 정책)
4. ⏳ **빈칸 B-13** — 의사 필터가 걸린 채 순서를 끌 수 있나. **여섯 세션 연속 미착수.**
5. ⏳ **셸 빈칸** — B-8·B-9·B-10·C-4·C-5(PASS 도입 — 병원 비용·계약)
6. ⬜ **다음 화면** — `/checkin` → 남은 직원 웹 → **챗봇 목업**(전용 화면 규칙이 아직 0개)
   - 📊 **남은 양(2026-08-08 세어 봄)** — 직원 웹 **18화면 중 7개 끝 · 11개 남음**(무거운 것 셋: `/doctor/console` · `/patients/:id` · `/admin/staff`, 나머지는 목록 화면). 챗봇은 **목업 17~19장**(환자앱 4~5 · **웹 상담창 3** · 직원웹 상담관리 4 · 관리자 6~7).
   - ⚠️ **웹 상담창(React)은 세 번째 채널이다** — 지금까지 그린 것은 **모바일 앱 + 직원 웹** 둘뿐이다. 병원 홈페이지에 얹는 위젯이라 **1단계(스타일·토큰 확정)를 한 번 더** 해야 할 수 있다.
   - ⭐⭐ **챗봇의 무게는 화면이 아니라 구조다** — 스펙 368줄 중 **화면 구성은 65줄뿐**이고 나머지는 **신규 테이블 7개 + 3갈래 체인 + 인계 감시**다(플랜 **7,098줄**). 그리고 **`예약취소_카드보내기`가 `CANCEL-*` 44개와, `사전문진_카드보내기`가 `QNR-*` 95개와 같은 판단을 해야** 한다 — **「같은 규칙을 채팅이라는 다른 그릇에서 지키기」**가 진짜 결정 덩어리다.
   - ⭐ **미리 보이는 결정 2건**(챗봇 단계에 가서 화면과 함께 정할 것 — 지금 정하지 말 것)
     - **「승인」이 되돌릴 수 없다** — 안내자료를 `[승인]`하면 **그 순간 봇이 그 내용을 환자에게 말한다**(자동 재임베딩). 관문 자체는 요구사항 3.8 `:201`이 요구해 확정이지만, **잘못 승인했을 때 되돌리는 경로**가 필요하다. ⭐ 재료는 있다 — 같은 문장이 *"이전 내용과 **수정 이력**도 남겨주세요"*를 요구했다.
     - **「답하면 안 되는 내용」이 섞이면 안 된다** — 스펙은 체크박스 하나로 풀었다(*"봇이 직접 답변하지 않고 **이 문구만 그대로** 보여줍니다"*). ⚠️ 그 자료가 검색에 걸렸을 때 **다른 자료와 섞어 답하면 그 문구의 의미가 사라진다** — 채팅에서 어떻게 보장하나. 근거: 요구사항 3.8 `:198`
   - ⭐ **순서 요령 — `/patients/:id`를 두 번 열지 않기.** 챗봇 스펙이 **2단계로 돌려보내는 것 2건**을 이미 적어뒀다: ①`/patients/:id`에 **「상담 문의」 섹션** ②`/today`의 **「확인 필요 상담 문의」 카드**를 실제 건수에 연결(지금은 **항상 0건 고정**). 📌 **`/patients/:id`를 그릴 때 ①을 같이 넣으면** 나중에 다시 열지 않는다.

### ⛔ 하지 않기로 한 것 (2026-08-06 결정)

- **소급 ⑦플랜 대조** — 직원 웹 기존 규칙 139개(`QUEUE` 85·`TODAY` 41·`SHELL` 13)
- **환자 앱 ②서버조회·④값전달 층 재점검**

→ **플랜 재작성이 전수를 보장하므로 불필요**(약 1.5세션 절약). 근거는 결정 문서 「⑤ 플랜은 패치가 아니라 재작성한다」.
⚠️ **단, 앞으로 그릴 화면의 ⑦플랜 대조는 계속한다** — 목적이 다르다(전수 확인이 아니라 「이 결정이 설 땅이 있나」를 지금 갈라내는 것).

---

## 📌 목업이 전부 끝나면 할 일 (지금 하지 말 것)

결정 문서 「📋 반영 절차」의 ①~⑦을 순서대로. 핵심만:

- ⑤단계는 **패치가 아니라 재작성**으로 확정(2026-08-06). 대상 `staff-web`(9,659줄·태스크 25) + `patient-app`(7,595줄·태스크 26). `foundation`은 구현 완료라 제외(새 마이그레이션 `00010_`부터 추가), `ai-chatbot`은 목업 후.
- ⚠️⚠️ **`writing-plans`를 그냥 돌리면 규칙 1,540개가 증발한다** — 스펙만 보고 동작명세를 안 본다. `screen-behaviors.md`를 함께 입력으로 주고 `test('[규칙ID] …')`를 직접 넣으라고 명시할 것.
- **커밋은 디자인 작업이 전부 끝난 뒤 한꺼번에**(사용자 지시).

### ⚠️ 쌓여 있는 DB·플랜 조치 (⛔ 지금 손대지 말 것 — 플랜 재작성 때 한꺼번에)

**마이그레이션 `00010_` 대기 7건**

| 갭 | 무엇을 | 어느 표 |
|---|---|---|
| #115 | 발송자 · 대상 수 추가 | `notification_log` |
| #119 | 발송 결과 · 실패 이유 · 재시도 횟수 | 〃 |
| #121 | 유니크 인덱스가 `실패`인 줄을 비켜가게 | 〃 |
| #117 | `resource_type`에 `phone_reveal` 추가 | `access_audit_log` |
| #123 | 「문자 안 감」 · 「언제 확인했나」 두 칸 | `patients` |
| #118 | 예약 발송 표 신설 | 🆕 신규 |
| #126 | 알림 종류별 표(문구 · 문자여부) 신설 | 🆕 신규 |

⭐ **#115·#119·#121은 같은 표라 한 파일에서 함께.**

**플랜 조치 3건**(DB 아님) — **#120**(채널이 항상 `'push'`로 박힘) · **#122**(Twilio 상태 되알림 주소 0건 + **서명 검증 필수**) · **#125**(문구에 날짜·시각을 채울 조회가 없다).

🔗 **#100·#103·#120·#122·#125는 `notify_patient` 한 함수에 모인다** — 문구·채널·죽은 토큰·도달 반영을 **다 이 함수가 만진다.** ⚠️ 따로 손대면 `HSET-SMS-05`가 경고한 **「채널 고르는 코드가 갈라진다」**가 그대로 생긴다.
📌 **원본은 결정 문서 「기능 갭」 절**이다. 이 표는 **한 파일에 모인다는 사실**만 보여주는 색인이다.

---

## 🗂 최근 세션 (4회)

### 2026-08-08 (7) — **알림·발송 C·B 완료** · 목업 74·75 신설 · 73·66·68 갱신

> 결정 내용·근거는 **결정 문서** 「알림·발송 C — 발송 결과와 실패」·「알림·발송 B — 알림 문구 편집」 두 절, 규칙은 **`screen-behaviors.md`** 「발송 결과와 실패」·「알림」 두 절이 원본.

#### C(발송 결과·실패) · 목업 74

- ✅ **확정 5건** — ①**진짜 도달까지 본다**(Twilio 상태 되알림) ②**일시 실패만 자동 재시도 2번**(1분·5분) ③**사이드바 숫자 = 전화할 건수만** ④**죽은 번호는 환자 상세에 표식** ⑤**실패 명단은 탭 + 탭에 건수.**
- ⭐⭐⭐ **⑦대조 ②층에서 이 프로젝트 최악의 것이 나왔다(갭 #121)** — 플랜이 **기록을 발송보다 먼저** 넣고, 그 표에 **「한 번만」 유니크 인덱스**가 걸려 있어 **실패한 휴진 안내를 영구히 다시 못 보낸다.** 📌 **핸드오프가 적어둔 *"안 닿은 환자가 그날 온다"*가 정확히 그 코드였다.** ⭐ 조치는 **자물쇠를 없애는 게 아니라 「무엇을 보냈다고 볼 것인가」를 바꾸는 것** — **닿은 것만 「보냈다」**.
- ⭐ **갭 6건(#119~#124)** · **규칙 1,918 → 1,974개**(+56) · **열려 있는 새 빈칸 없음.**
- ⚠️ **채널 칸이 거짓말을 하고 있었다(#120)** — 코드가 `values (…, 'push')`로 **상수를 박는데** 같은 함수가 **문자로 폴백**한다. **돈이 나가는 숫자(`SEND-CH-04`)를 나중에 검증할 수 없다.**
- ⚠️⚠️ **사용자 지적 3건이 전부 내 설계를 고쳤다**
  - *"경고를 어디엔가 보내긴 해야 하지 않나"* → 나는 **광고 실패를 「할 일 없음」으로 너무 쉽게 넘겼다.** 죽은 번호는 **앞으로 휴진 안내도 못 받는다** → 실패가 두 갈래가 아니라 **세 갈래**였다(전화 / 번호 고침 / 할 일 없음).
  - *"매번 실패가 뜨겠네? 그 사람은 계속 안 고쳐지니까"* → **맞다.** 죽은 번호는 **발송마다 같은 명단이 다시 뜬다** → `SEND-FAIL-09`(접어두기 — `SEND-LIST-08`과 같은 장치).
  - *"없는 번호라 해도 번호를 볼 수 있게 해줘야 해. 확인 작업을 위해서는."* → 내가 *"걸 수 없으니 풀 필요 없다"*고 한 것은 **확인 작업을 빼먹은 것**이다 — 자릿수·오타는 **눈으로 잡는다.**
- ⚠️ **내가 탭을 기각했고 사용자 근거 한 줄에 뒤집혔다** — 내 이유는 *"두 번 눌러야 한다"*였는데 *"그래야 거기에 몇 건인지 표시도 해주지"*. **이름에 숫자가 있으면 안 눌러도 안다.** 📌 **`SEND-LIST-04`(탭 기각)와 모순이 아닌 이유를 규칙에 박았다**(`SEND-FAIL-03`) — 갈리는 선은 **「탭이 늘 떠 있나」**.
- ⭐⭐ **자체 발견 — 두 곳에서 세는 겹침**(`TODAY-RESCHED-21`). 휴진 안내 실패자는 **`/today` 카드에 이미 줄이 있어** 사이드바에 또 세면 **같은 2명을 두 군데서** 본다.
- ⭐ **어제 정한 「3,000줄」이 작아졌다** — 탭마다 따로 풀리므로 **푼 탭 인원수**만 남는다(`SEND-OPEN-07c` 개정). **원칙은 그대로, 숫자만 작아진다.**
- ⭐ **목업 ID 되돌려 박기가 처음으로 됐다** — 74의 **자기 화면 규칙 참조 58개**(73은 19개). 갭 #116이 지적한 *"그 목업에서 규칙을 처음 만든 화면은 0~1개"*를 넘긴 첫 사례다.
#### B(갭 #102 — 알림 문구) · 목업 75

- ✅ **확정 4건** — ①`/admin/settings` **다섯째 줄** ②⭐⭐**「문자로도 보낼 알림」을 이 줄로 합쳤다** ③**문구는 통째로 자유 + 저장 전 되묻기·미리보기** ④**넣을 값 = 이름·날짜·시각.**
- ⭐⭐ **미리 예약해 둔 자리가 값을 돌려줬다** — `HSET-NAV-02`가 세로줄을 고른 근거를 *"갭 #102가 오면 **줄 하나 추가**로 끝난다"*로 적어뒀고 **실제로 그랬다.** `SB-53`(70번에서 줄 하나)에 이어 **두 번째 사례.**
- ⚠️⚠️ **확정된 규칙을 개정해야 했다** — 「문자로도 보낼 알림」이 **같은 10종 목록**이라 두 곳에 생기는 것이 드러났다 → `HSET-SMS-01`(세 칸 → **두 칸**) · `HSET-SMS-02`(**잠기는 범위**) · `HSET-NAV-01`(네 줄 → **다섯 줄**). ⭐ 근거는 `HSET-SMS-05`가 이미 적어둔 *"따로 만들면 갈라진다"*.
- ⭐⭐⭐ **합치는 대가를 규칙에 못박았다** — ⛔**표를 통째로 잠그면 문자를 안 쓰는 병원이 푸시 문구를 하나도 못 고쳐 기능이 죽는다.** ⚠️ 그런 병원이 **소수가 아니다**(문자는 병원이 돈을 낸다). **잠기는 것은 「문자로도」 열 하나뿐**(`HSET-MSG-27~29`·`HSET-SMS-02b`).
- ⭐ **막지 않고 알려주는 쪽으로 갔다** — 금지어 검사는 **애초에 불가능**(`내과`를 막아도 *"소화기 진료"*는 안 막힌다). 선례 둘(`SEND-ADS-04`·`PUSH-BODY-09`)이 이미 같은 판단을 했다 — **병원은 막을 상대가 아니라 알려줄 상대.**
- 🆕 **문구에 날짜·시각이 하나도 없었다(갭 #125)** — `내일 예약이 있습니다`가 전부라 **환자가 앱을 열어야** 시각을 안다. 요구사항 4.8(중장년층)에 정면으로 불리. ✅ 꺼낼 수는 있다(`slot_id` → `appointment_slots`).
- ⚠️ **「11종」이 실제로는 10종** — 열한째(**상담 답변 도착**)는 플랜이 **4단계로 명시 연기**했다. 📌 세션 5의 「`HOURS-*` 13→12개」와 같은 자리라 세었다.
- ⚠️⚠️ **내 실수 둘** — ①**말로만 설명해 사용자가 못 알아들었다**(*"뭔 말인지 모르겠어;;;;;; 그림이 없어서 그런가"*). ⭐ **플레이북 2단계의 「①A/B/C 안을 그림으로」를 건너뛰고 ②선택으로 갔다** — 그리자 그 자리에서 결정됐다. ②**되돌리기를 맨 위에 그렸다가** 규칙을 쓰며 **기각안임이 드러나** 줄마다로 고쳤다.
- ⚠️ **「없다」 단정 재발** — *"#103은 아직 열려 있다"*고 핸드오프에 적었다가 **원문을 열어 고쳤다**(`HSET-SMS-01` ②가 이미 그 자리였다).
- ⚠️⚠️ **또 숫자를 틀렸다(세션 끝에 검산해서 잡음)** — 규칙 수를 **2,089**로 적었는데 실제는 **2,009**다. 세션 중간에 **접미사 변형(`-02b`·`-07c`)까지 별개로 세는 명령**을 써서 **80개를 부풀렸다.** 📌 **세션 5의 「`HOURS-*` 13개→12개」와 완전히 같은 실수**이고, 그때도 이번에도 **「세는 방법」이 문서와 달랐던 것**이 원인이다. ⭐ **핸드오프 표에 「어느 명령 기준인지」를 명시**해 재발을 막았다.

- ✅ **목업 68도 갱신했다**(사용자 지시 *"손봐야 할 거 다 해줘"*) — 세로줄 **3곳에 「알림」 추가** · **옮겨간 칸 제거** · 규칙표 개정 · **잠기는 범위가 두 화면으로 나뉜 것**을 적었다. ⭐ 자기 규칙 참조가 **0 → 5개**(갭 #116이 지적한 자리).
- ⭐ **목업 75 ①절 라벨도 고쳤다** — *"**지금** 이렇게 생겼다"*가 이제 거짓이라 **「개정 전 · 비교용」**으로. ⛔ **지우지는 않았다** — ②③절 설명이 *"바뀌기 전이 이랬다"*에 기대고 있어 지우면 **무엇이 어디로 옮겨졌는지** 알 수 없다.
- ✅ 기계 검증: 규칙 총수 **2,009**(1,918 + `SEND-*` 50 + `TODAY-RESCHED` 6 + `HSET-MSG` 34 + `HSET-SMS-06` 1 = 2,009 ✓ 검산 일치) · `⑦대조: ⬜` **0건** · 목업 **66·68·73·74·75 다섯 다** 태그 균형 정상 · CSS 미정의 클래스 **0건** · 인용 규칙 ID **전부 실재**(유령 0건) · 자기 화면 규칙 참조 **74=58 · 75=26 · 68=5개**.

### 2026-08-07 (6) — **알림·발송 D 완료(제1문 `/messages`) · 역할 원칙 신설 · 기록 감사** · 목업 73 신설

> 결정 내용·근거는 **결정 문서**(「SB-52 부분 해소 · 갭 #112·#114」 절), 규칙은 **`screen-behaviors.md`**가 원본.

- ✅ **`SB-52` 완전 해소** — ①제1문 = **사이드바 「안내 보내기」**(`/messages`) ②「전 환자」 발송 권한 = **접수직원·관리자 둘 다.**
- ⭐⭐ **규칙 원칙이 통째로 없던 것이 드러났다(갭 #114)** — *"관리자는 직원이 할 수 있는 것을 다 할 수 있다"*가 **1,800여 규칙 중 0건**이었다. **RLS는 `in ('receptionist','admin')`으로 알고 있는데 규칙이 몰랐다** → `ROLE-*` 11개 신설. 이제 **접수직원에게 준 권한은 관리자에게 자동으로 간다.**
- 🆕 **갭 7건(#112~#118)** · **규칙 1,867 → 1,918개**(`SEND-DOOR` 8 · `ROLE` 11 · `SEND-ALL` 12 · `SEND-LIST` 11 · `SEND-OPEN` 15).
- 🧰 **기록 감사를 처음 돌렸다**(사용자 요청) — 결정문서↔동작명세 **양방향 + 목업↔규칙 양방향**. **오탐 15건**(폐기·외부 ID·예시)을 걸러내고 **진짜 3건**을 고쳤다: `ROLE-READ` 결정문서 누락 · `SB-49`/`#108` 절에 **규칙 링크 없음** · 목업 68·71·72가 **자기 규칙 ID 0~1개**(갭 #116).
- ⭐ **목업 ID 원칙이 생겼다** — 규칙과 그림을 **동시에 만들면 「되돌려 박기」가 빠진다.** ⛔ **전수 소급은 안 함**(진본이 아니다). 앞으로 그리는 것에만 적용 — **73이 첫 적용**(자기 규칙 19개). 플레이북에 **(a)ID 박기 + (b)갱신 절차**를 짝으로 반영.
- ⚠️⚠️⚠️ **내가 네 번 틀렸고 전부 사용자 지적으로 고쳐졌다** — ①**권한과 표시를 뭉갰다**(`SCHED-WARN-04`가 관리자에게 덜 보여주는 것을 「권한이 없다」로 읽음 → `ROLE-SEE-*`) ②**없는 미결을 만들었다**(RLS 한 줄만 보고 「6.5와 긴장」이라 했으나 요구사항 `:82`가 이미 답이었다) ③**마스킹 근거를 잘못 댔다**(*"이미 기록이 남는데 마스킹?"* — 진짜 이유는 보안이 아니라 「필요 없어서」였다) ④**열람 기록을 빠뜨렸다**(*"마스킹 벗기면 기록해야지"* — 발송 이력에만 남기면 `/admin/access-logs` 조회에서 빠진다).
- ⭐⭐ **같은 표를 두고 정반대 결론을 냈고, 둘 다 맞다** — 갭 #117에서는 *"3,000줄을 쌓지 말자"*(발송했을 뿐 **안 봤다**), `SEND-OPEN-07c`에서는 *"3,000줄이 맞다"*(번호를 풀면 **진짜 봤다**). **가른 기준은 「봤나 안 봤나」.**
- ✅ 기계 검증(73): 태그 균형 정상 · CSS 미정의 클래스 0건 · **규칙 ID 40개 전부 실재** · 25.2KB.

### 2026-08-07 (5) — **관리자 화면 둘의 그릇 통일 + 운영시간 이식** · 목업 68·69·70 신설

> 결정 내용·근거는 **결정 문서**(「SB-50·SB-53」 절 · 「`SCHED-HOURS-01` 이행」 절), 규칙은 **`screen-behaviors.md`**가 원본.

- ✅ **빈칸 3건 해소** — **SB-50**(`/admin/settings` = 왼쪽 세로 메뉴 네 줄) · **SB-53**(`/admin/schedule` = 탭 4개를 왼쪽 세로줄로, 의사 목록은 위쪽 가로줄) · **`SCHED-HOURS-01` 이행**(운영시간을 다섯째 줄로).
- ⭐ **두 관리자 화면이 같은 부품을 쓴다** — 왼쪽 세로줄 + 줄마다 「지금 값」 부제목 + 맨 위 저장 하나 + 주황 점. **직원 교육 문서도 한 벌이면 된다.**
- ⭐⭐ **`SB-53`의 값이 바로 돌아왔다** — 70번에서 **줄 하나를 더하면 끝**이었다. 가로 탭이었다면 **다섯째에서 좁아지기 시작**한다.
- ⚠️ **정정 — 「`HOURS-*` 13개」가 실제로는 12개**였고 **`EXC` 4개는 `SB-49`에서 이미 폐기**돼 **옮긴 것은 8개**다. 📌 **세어 보지 않고 앞 문서의 숫자를 옮겨 적은 사례.** 이제 **`SCHED-HOURS-01~18`이 원본**이다.
- ⚠️⚠️ **내가 틀린 것 둘**(둘 다 사용자가 잡아냄) — ①*"이 세로 메뉴를 쓰는 화면이 여기 하나뿐"* → **67번이 이미 쓰고 있었다**(*"이거 이미 썼었어"*). ②**사용자 말을 오독**해 「설정 항목이 세로줄」로 그렸고, 거기서 나온 *"새 의사 등록이 번거롭다"*는 **내가 만든 유령**이었다 — 오독 위에 쌓은 장단점 분석을 통째로 버렸다.
- ⭐⭐ **덤으로 메워진 구멍** — `SCHED-SAVE-02`는 *"고친 **줄**에 `●`"*까지였는데 **의사를 옮기면 그 줄이 사라져 `●`도 사라지고**, `SCHED-SAVE-07`이 **묻지 않고 날린다** → 조용히 날아간다. 이제 **점이 세 층**(줄/의사 이름/세로줄)에 붙는다(`SCHED-SAVE-02b~02d`).
- ⭐ **저장 경고 팝업을 한 덩어리로 합쳤다**(`SCHED-HOURS-15·16`) — 옛 `HOURS-DOC-01`이 `SCHED-WARN-*`와 **같은 버튼에** 붙어 함께 터진다. ⛔ 순서대로 두 번은 **두 번째 창을 안 읽는다**(`SCHED-SAVE-04`·갭 #90과 같은 이유).
- 🆕 **갭 3건(#105·#106·#107)** — 전부 **⑦플랜 대조**에서. **#105** 플랜의 `SchedulePage`에 **의사가 `doc1`으로 박혀 있고 월요일만 받는다** · **#106** 폼 상태가 한 벌뿐이라 의사를 바꾸면 **저장 안 한 값이 덮인다**(#95와 한 몸) · **#107** ⚠️ **취소 마감 확인창의 「N건」을 셀 조회가 없다** — 소급을 「즉시 전면 적용」으로 정할 수 있었던 **유일한 근거가 그 숫자**인데, 없으면 **0으로 떠서 관리자가 영향이 없다고 믿는다**.
- ✅ **`SB-54`도 같은 세션에서 해소** — 운영시간을 **줄일 때**의 반대 방향 경고 → **㉯ 화면에 상시 표시**(사용자 확정). ⚠️⚠️ **내가 처음 적은 전제가 반쯤 틀렸다** — *"의사 3명이 운영시간 밖이 된다"*까지만 적고 **그래서 무슨 일이 일어나는지**를 안 적었다. 확인해 보니 **예약은 하나도 안 망가진다**(`hospital_hours`를 읽는 곳은 `is_open(at)` 하나뿐이고 손님은 **상담봇**) — **진짜 피해는 「상담봇이 잘못 말하는 것」**이었고, 그래서 **막을 문제가 아니라 보이게 할 문제**였다. → `SCHED-HOURS-17~17k`
- 🆕 **규칙 1,738 → 1,795개**(`SCHED-HOURS-*` **29개**) · **갭 104 → 107건** · **열려 있는 새 빈칸 없음**.
- ✅ **목업 67의 낡은 그림을 걷어냈다**(`㉰` 사용자 확정) — 화면 프레임에서 **탭 줄 5곳 · 의사 세로 레일 1곳**을 지우고 **경로 캡션**(`진료 일정 › 의사별 스케줄`)만 남겼다. ⭐ **그릇을 안 그리면 낡을 것이 없다** — 8장을 다시 그리면 69·70과 **같은 그림이 중복**되고 그릇이 또 바뀔 때 **또 8곳**을 고쳐야 한다. ⚠️ **예외 2곳**: ①-2절 `㉠ 탭 안에서` vs `㉡ 탭을 하나 더`는 **탭이 비교의 주제**라 남기고 낡음 표식을 달았다. 낡은 문구 3곳도 함께 고쳤다.
- ⭐⭐⭐ **갭 #108 — 가입에 「동의」가 통째로 없었다**(목업 71 신설 · 규칙 `CONSENT-*` **22개**). `SB-51`(광고 동의를 어디서 받나)을 풀려다 **붙일 그릇 자체가 없다**는 것이 드러났다 — `docs/` 전체 `약관` **0건** · 가입 목업 53 `동의` **0건** · `supabase/migrations/` `consent|agree|terms` **0건**. ✅ **확정 3건**: ①**맨 앞**(전화번호 받기 전 — 가입 **3→4단계**) ②**줄 넷**(민감정보는 **별도 동의** — 묶으면 무효) ③**「전체 동의」는 필수 3개만**. ⭐⭐ **`SB-51`의 딜레마가 저절로 사라졌다** — 필수 화면이 어차피 들어가므로 광고는 넷째 줄. 📌 **빈틈을 메우기 전에 「그릇이 있나」를 먼저 봐야 했다.**
- ⚠️ **흐름도 수정 대상이 하나 늘었다** — 가입이 **3단계 → 4단계**(`AUTH-SIGNUP-03·04`). `screen-flow.html`은 아직 3단계다.
- ⭐⭐⭐ **F(광고·발송) 완료 — 목업 71·72 신설.** 규칙 **1,795 → 1,867개**(`CONSENT-*` 22 · `PICK-*` 26 · `SEND-*` 29), 갭 **107 → 111건**.
  - **71 = 가입 동의**: `SB-51`을 풀려다 **그릇이 통째로 없다**는 것이 드러났다(갭 **#108**). 확정 3건 — **맨 앞**(3→4단계) · **줄 넷**(민감정보는 **별도 동의**) · **「전체 동의」는 필수 3개만**.
  - **72 = 안내 보내기 패널**: ⚠️⚠️ **사용자 되물음 넷으로 다시 쓰였고, 셋은 이미 있는 규칙이 답을 갖고 있었다** — `PANEL-USE-01`(그릇은 패널) · `PANEL-WORK-02`(받는 사람 칸 → 왼쪽이 검색 표) · `TODAY-RESCHED-12`(체크칸은 버튼을 누른 뒤에만). ⭐ **원칙 한 줄: 줄의 버튼 = 한 사람 / `[선택]` 모드 = 여러 사람.**
  - ⚠️ **내 정정 2건** — ①체크 순서를 거꾸로 그렸다 ②cron이 **하루 한 번**인 것을 **못 바꾸는 제약**으로 착각했다(사용자 *"다른 시간에 예약할 수는 없고?"*로 드러남).
- ✅ 기계 검증(67·68·69·70·71·72): 태그 균형 정상 · **CSS 미정의 클래스 0건** · 목업이 쓴 규칙 ID 전부 실재 · `⑦대조: ⬜` 0건.

### 2026-08-07 (4) — **SB-49 해소 · 알림·발송 묶음이 통째로 없다는 것이 드러남**(목업 없음 — 결정만)

> 결정 내용·근거는 **결정 문서** 「SB-49 — 병원 설정과 알림·발송」 절, 규칙은 **`screen-behaviors.md`**가 원본.

- ⭐⭐ **핸드오프가 범위를 좁혀 두고 있었다** — *"담을 것은 둘"*이라 적혀 있었으나 **갭 #32(2026-08-03)에 다섯 가지 + 운영시간이 이미 확정**돼 있었다. 📌 **앞 세션의 요약을 원문 삼아 읽은 것** — 이 프로젝트가 반복해서 겪는 실수다.
- ✅ **확정 5건** — ①**소급 즉시 전면 적용**(자동 알림 없음 + 저장 확인창에 영향 건수) ②**시간은 스케줄 · 규칙은 설정**(`hospital_hour_exceptions` 폐기 → `hospital_closures` 하나) ③**자유 텍스트 발송 만든다**(푸시 두 겹 · 문자는 미리보기+경고) ④**개별·그룹·전체·예약발송 전부**(공용 부품, 새 화면 없음) ⑤**광고는 안내와 갈라서 다룬다.**
- ⭐⭐ **사용자 근거 둘이 내 판단을 뒤집었다**
  - *"환자 정보 전체를 외부에서 처리하는 것도 문제야"* → **「전체 발송을 만들지 말자」는 내 추천이 기각됐다.** 나는 **오발송 위험만 보고 명단 유출 위험을 안 봤다.**
  - *"미리 공지도 하고 그랬을 거야"* → 마감 변경에 **자동 알림을 안 보내는** 근거가 됐다(`SCHED-WARN-11`과 같은 자리).
- ⚠️⚠️ **광고는 법이 따로 있다**(정보통신망법 50조 — 사전동의·`(광고)`표기·야간금지·수신거부·과태료 3천만원). **요구사항 언급 0건.** 정보성/광고성을 **시스템이 갈라야** 하고 **환자 앱 가입 화면까지 바뀐다** → 갭 **#104**.
- ⚠️ **정정 1건** — 내가 제안한 「푸시 두 겹」이 **`PUSH-BODY-04`(잠금화면 감추기 기각)와 충돌하는 것처럼 보였다.** 확인 결과 **다른 물건**이다 — 기각된 것은 「OS에게 가려달라고 부탁」이고 이건 「애초에 안 보내기」다. **혼동 방지 규칙을 `PUSH-BODY-07`로 박아 뒀다.**
- 🆕 **갭 6건(#99~#104)** · **규칙 1,727 → 1,738개** · **새 빈칸 3건**(`SB-50` 묶음 구조 · `SB-51` 동의 받는 자리 · `SB-52` 발송 부품 위치·권한).
- 📌 **목업은 그리지 않았다** — 범위가 화면 하나를 넘어 **A만 이번, F→D→B→C→E는 다음**으로 쪼갰다(사용자 확정).

---

## 📌 미뤄둔 것

- ⚠️ **번호 체계가 겹친다** — `screen-behaviors.md` 한 파일에 **직원 웹 빈칸 `B-11`**(해소)과 **환자 앱 결정 `B-11`**(알림 읽음)이 둘 다 있다. 새 빈칸부터 `SB-` 접두어를 쓴다. ⏳ 기존 직원 웹 `B-8~B-16`은 **직원 웹이 끝난 뒤 한꺼번에** 바꿀 것.
- **스펙 수정 목록 #30** — `staff-web-design.md:63`의 *"이름 + 생년월일(선택)로 검색"*이 낡았다.
- **ID 커버리지 스크립트를 아직 못 돌린다** — 테스트 코드가 없어서. 목업 67장 중 ID가 실질적으로 박힌 것은 14장.
- ⚠️ **화면 규칙이 0개인 것 3가지**(요구사항·DB엔 있는데 안 그린 화면) — `/patients/:id`의 **내부 메모**(`patient_internal_notes` 이미 적용) · **`/admin/staff`**(Task 19에 API까지 있음) · **챗봇 전용 화면**.

**미커밋 변경**: `screen-behaviors.md` · 결정 문서 · `HANDOFF.md` · `CLAUDE.md`(gitignore) · 목업 **62~75**(67~75 신규 · **73·66·68은 갱신됨**) · 목업 `README.md` · **직원 웹 워크플로우**(`staff-workflow.html`) · 직원 웹 화면 지도 · ⚠️ `docs/design/_review-index.html`(휴대폰 확인용 임시 — **커밋 전 삭제**) 등. **커밋은 디자인이 전부 끝난 뒤 한꺼번에**(사용자 지시).
📌 **프로젝트 밖 변경 1건** — `~/.claude/mockup-design-session-playbook.md`(목업 ID 박기 + 갱신 절차. 전역 파일이라 이 저장소에 안 들어간다).

**목업 서버**: `--project-dir`가 같으면 같은 포트. 이번 세션은 `55338`, 파일은 `.superpowers/brainstorm/81632-1786091190/content/`(gitignore). 목업 원본은 `docs/design/mockups/`에 두고 그쪽으로 **복사**한다.
